{
  "name": "@defenseclaw/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DefenseClaw embedded web dashboard. Built into internal/dashboard/dist by `make web`.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "lit": "^3.2.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
