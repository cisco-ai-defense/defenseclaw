#include "platform.h"

/* TLS engine wrapper — stub for Phase 1 Task 7 (Week 3-4).
 * Will wrap mbedTLS for mTLS handshake + session management. */

int dclaw_tls_init(void) { return -1; }
void dclaw_tls_shutdown(void) {}
