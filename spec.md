# DefenseClaw Observability v8 — Codex Execution Manifest

```yaml
spec_status: implemented-and-verified
goal_status: complete
current_phase: complete
target_config_version: 8
baseline_commit: fd13acedfcffc0cc431d5a72f329b56b50b22baa
last_verified_commit: a8dabc35f864d65e31f1c02e1cced6073cf4a2f3
last_updated: 2026-07-15
```

## Goal Contract

Implement DefenseClaw observability v8 end to end from the tracked normative
specification package, preserving PR #403 root/subagent Agent360 lifecycle and
traceability and PR #412 local-observability dashboard behavior, with one
authenticated POSIX resolver command automatically staging v7 through the published
`0.8.4` bridge into v8, and every applicable release gate passing.

This file is the execution ledger for the active Codex goal. It is not a fifteenth
normative observability contract. Normative behavior lives in
[the observability v8 specification package](docs/design/observability-v8/README.md).

The goal is complete only when every release-blocking work package and phase gate
is `DONE`, the final verification suite passes at the final commit, the PR is open
from this branch against `main`, all required CI/CD checks are green, review issues
are resolved, and no required work remains. Partial implementation, budget pressure,
or a plausible-looking dashboard is not completion.

## Current Execution Snapshot

| Field | Value |
|---|---|
| Active work package | None |
| Ready queue | None |
| Blocked | None |
| Next phase gate | None |
| Root coordinator | Primary Codex thread |
| Implementation branch | `codex/observability-v8-hard-cut` from current `main` |

The root coordinator updates this snapshot whenever ownership, active work, the
ready queue, or the next gate changes.

## Authority and Precedence

1. Repository and user instructions, including `AGENTS.md`.
2. This execution manifest for work ordering, ownership, and evidence.
3. The normative package under `docs/design/observability-v8/` for product and
   implementation behavior.

Every package document is normative according to its own scope. If implementation
reveals an ambiguity or infeasible requirement, amend the affected normative
document, the D-/S-/P- decision log, the traceability appendix, and acceptance tests
before changing behavior. No agent may silently reopen a locked decision, weaken a
release gate, or create a second configuration/telemetry pipeline.

The specification passed `P0-GATE` at commit `963b1bc9f`. Behavior-changing
implementation follows the phase order and locked decisions below. A later
ambiguity requires an explicit spec amendment and traceability update; it does not
silently return the package to draft or authorize implementation divergence.

## Normative Source Map

| Contract | Authority |
|---|---|
| [Architecture and requirements](docs/design/observability-v8/01-architecture-and-requirements.md) | Goals, pipeline, invariants, mandatory floor |
| [Taxonomy and data model](docs/design/observability-v8/02-taxonomy-and-data-model.md) | Fourteen buckets, envelopes, severity, finding semantics |
| [Configuration contract](docs/design/observability-v8/03-configuration-contract.md) | v8 YAML, defaults, capabilities, routes, validation, reload |
| [Redaction contract](docs/design/observability-v8/04-redaction-contract.md) | Profiles, detector groups, field classes, hash contract, failure behavior |
| [Storage, retention, and delivery](docs/design/observability-v8/05-storage-retention-and-delivery.md) | SQLite, projections, reaper, queues, health |
| [Migration and implementation](docs/design/observability-v8/06-migration-and-implementation.md) | Current mapping, phases, compatibility, removal scope |
| [Verification and acceptance](docs/design/observability-v8/07-verification-and-acceptance.md) | Test matrices, E2E scenarios, commands, release gates |
| [Decisions and exclusions](docs/design/observability-v8/08-decisions-and-exclusions.md) | D-/S-/P- log, exclusions, change control |
| [Configuration UX and bucket evolution](docs/design/observability-v8/09-configuration-ux-and-bucket-evolution.md) | Concise source, effective/reference views, catalog evolution |
| [Automatic upgrade](docs/design/observability-v8/10-automatic-upgrade-and-migration.md) | One-command migration, backup, failure, rollback |
| [Trace and span contract](docs/design/observability-v8/11-trace-and-span-contract.md) | Rich topology, families, events, links, Galileo, sampling |
| [Telemetry schema architecture](docs/design/observability-v8/12-telemetry-schema-architecture.md) | Canonical registry and generated artifacts |
| [Decision traceability](docs/design/observability-v8/13-decision-traceability.md) | Mechanical D-/S-/P- contract/test index |
| [Agent lifecycle and dashboard compatibility](docs/design/observability-v8/14-agent-lifecycle-and-dashboard-compatibility.md) | PR #403/#412 compatibility floor and local bundle |
| [Current-state inventory](docs/design/observability-v8/current-state-inventory.yaml) | Drift-checked v7 config, producer, schema, metric, dashboard, datasource, and pinned PR #403/#412 compatibility-baseline migration inputs |
| [Minimal configuration](docs/design/observability-v8/config-v8-observability-minimal.yaml) | Compact authoring example |
| [Reference configuration](schemas/config/v8/reference/observability.yaml) | All-knobs generated/reference target |

Pinned compatibility baselines:

- PR #403 merge: `9e417889c4c456bc3c7e6c160ee98c1add1094ee`.
- PR #412 merge: `94dd46c689fefbcf85b8f478249e74f3925eca49`.

## Non-Negotiable Invariants

- Every canonical record has exactly one primary bucket.
- Collection is evaluated before construction, sampling, routing, and redaction.
- Exactly one implicit mandatory SQLite store receives every collected log and the
  local-only mandatory floor.
- Fresh v8 defaults collect every defined signal and preserve schema-eligible
  content unredacted; no remote export starts without a configured destination.
- An enabled destination with omitted policy receives every supported signal and
  catalog bucket unredacted. Explicit `send` or `routes` narrows that behavior.
- A record fans out independently to every matching destination. Advanced routes
  are first-match-wins within each destination and signal.
- The canonical record is immutable. Redaction and vendor adaptation operate on
  destination-owned projections.
- Automatic migration preserves narrower or redacted v7 behavior rather than
  broadening it to fresh-v8 defaults.
- Labels, span names, events, links, queues, caches, bodies, and cardinality are
  bounded; content and credentials never become metric labels.
- Valid reload swaps one immutable graph atomically; invalid reload leaves the old
  graph active.
- One semantic action produces one canonical record and expected projections; old
  and new direct paths must not duplicate it.
- Missing tokens, cost, content, timing, and parentage remain truthfully absent or
  `not_reported`; no producer or dashboard fabricates zero/empty values.
- PR #403 root/subagent, lifecycle/execution, phase/sequence/operation,
  hook-decision, and completed-operation-before-Stop behavior remains intact.
- PR #412 metric names, labels, histogram buckets, 60-second delta cadence,
  fourteen dashboard UIDs, three datasource UIDs, and source/packaged parity remain
  intact.
- The authenticated target-release POSIX resolver performs the required `0.8.4`
  bridge handoff, atomic migration, and compatible local-bundle refresh while
  preserving custom files and history volumes; platforms without a published
  bridge refuse before mutation.

## Status Vocabulary

Use only these values in work-package and gate tables:

| Status | Meaning |
|---|---|
| `TODO` | Defined but dependencies or capacity are not ready |
| `READY` | Dependencies are `DONE`; safe to assign |
| `IN_PROGRESS` | One named owner is actively working it |
| `BLOCKED` | Cannot progress; row records reason and unblock action |
| `DONE` | Root verified deliverables and commit-specific evidence |
| `SUPERSEDED` | Replaced by another named work package |

Only the root coordinator changes phase/work-package status. A subagent proposes
completion; the root verifies and records it. Relevant changes invalidate previous
evidence and move the affected gate out of `DONE` until reverified.

## Definition of Ready

A subagent work package is `READY` only when it has:

- One bounded objective and explicit non-goals.
- Normative document/decision references.
- Satisfied dependencies.
- Exclusive write scope and named shared hotspots.
- Expected artifacts and exact verification commands.
- Compatibility, privacy, security, migration, and performance constraints.

## Definition of Done

A work package is `DONE` only when:

- Implementation, tests, generated artifacts, docs, and fixtures for its scope are
  complete.
- No generated file was hand-edited when a generator owns it.
- Exact commands and observed results are recorded at the current commit.
- The diff contains no unrelated user changes or broad formatting churn.
- Compatibility/security implications were reviewed against D-/S-/P- decisions.
- Root integration verification passes and the work is committed.

## Dependency Graph

```text
P0 specification import/freeze and baseline inventory
  └─> P1 contracts, config compiler, strict validation, converter engine/API
       └─> P2 canonical model, router, redaction, SQLite, retention
            ├─> P3 destinations, OTel signals, Galileo, local profile
            │    └─> P4 producer migration and duplicate-path removal
            └─> P5 telemetry registry and rich trace/schema generation
                  └─> P5-WP06 generated v7 selection + converter goldens
P4 + P5-WP06 ─────────────────────────> P6 operator UX, dashboards, docs
P1 + P3 + P5-WP06 + P6 ───────────────> P7 one-command upgrade integration
P0..P7 ────────────────────────────> P8 final security/perf/release gates
```

P5 may begin after P2 while P3/P4 continue, but its destination projections depend
on stable P3 contracts. No phase is complete merely because code exists; its phase
gate must pass.

`P1-GATE` approves the side-effect-free converter engine and the strict typed API
that consumes generated compatibility data; it does not certify final v7 family
eligibility, local-dashboard coverage, or the complete migration golden matrix.
`P5-WP06` supplies those generated inputs from the sole registry compiler, proves
every current exporter/family mapping, and completes the goldens. It is a hard
dependency of automatic activation in P7 and final release, and neither phase may
substitute a converter-local family list, `*`, or all-catalog-buckets fallback.

## Phase and Work-Package Ledger

### P0 — Specification Import, Freeze, and Baseline

| ID | Status | Owner | Depends on | Deliverable | Verification/evidence |
|---|---|---|---|---|---|
| `P0-WP01` | `DONE` | root | — | Track normative package, root manifest, docs index; remove temporary-path claims | Commit `b5167c2d9`; link/YAML/decision lint passed |
| `P0-WP02` | `DONE` | subagent, root verified | `P0-WP01` | Machine-readable inventory of current config fields, producers, schemas, metrics, dashboards, and migration disposition | 37 anchors, 14 events, 188 actions, 131 metrics, 22 schemas, 14 dashboards, 3 datasources, 2 compatibility commits; checker and 6 tests passed at `963b1bc9f` |
| `P0-WP03` | `DONE` | subagents + root | `P0-WP01` | Review D-/S- locks and P-* proposed defaults; resolve contradictions in-package | 81 decisions mechanically traceable; two CodeRabbit review passes raised 23 total findings, all valid findings resolved; commit `963b1bc9f` |
| `P0-GATE` | `DONE` | root | `P0-WP01..03` | Specification approved for behavior-changing implementation | `make check`, 42 focused tests, Ruff, and diff validation passed at `963b1bc9f` |

### P1 — Contracts, Configuration, and Converter

| ID | Status | Owner | Depends on | Deliverable | Verification/evidence |
|---|---|---|---|---|---|
| `P1-WP01` | `DONE` | subagent + root | `P0-GATE` | Bucket/signal/event/severity/source/selector types and classification contract | 14 gateway events and 188 audit actions exhaustively classified; focused Go tests/vet at `d15292434` |
| `P1-WP02` | `DONE` | root + subagents | `P1-WP01` | Go v8 schema, defaults, compiler, capabilities, routes, profiles, strict legacy rejection | Closed schema and embedded registry; strict parser; immutable/masked plan; capability/preset/route/profile/secret/path/endpoint/provenance diagnostics; normal/race/vet/schema suites and `make check` passed at `780adcf72` |
| `P1-WP03` | `DONE` | root + subagents | `P1-WP02` | Python source/schema parity with registry-owned selectors delegated to the canonical Go helper; comment-preserving writer; source/effective/reference/plan generation | Commits `3c9739c0c..c7234b5de`; 82 focused Python tests, normal/race Go tests, schema/reference drift gates, Ruff, and real Go/Python bridge passed |
| `P1-WP04` | `DONE` | root + subagents + Claude review | `P1-WP02..03` | Side-effect-free deterministic v7-to-v8 converter engine plus a strict typed contract for generated v7 eligibility, exact filter predicates, adapter routes, and local-profile coverage; no generated registry data is hand-authored here | Commits `bf4fed78b..c29cdddc4`; 237 focused converter/contract tests, Ruff/format/diff checks, exhaustive v7 precedence/secret/filter/parser/idempotence cases, and two canonical Go candidate validations passed. Synthetic fixtures prove missing, malformed, duplicate, incomplete, or inexact contract data fail closed; semantic family completeness/ambiguity/staleness remains `P5-WP06`/P7. |
| `P1-GATE` | `DONE` | root | `P1-WP01..04` | Immutable validated runtime plan plus converter engine/artifact API from YAML; runtime producers not switched and final generated selection/goldens explicitly remain in `P5-WP06` | At `c29cdddc4`: 334 broad Python tests; Go normal/race/vet; schema/reference/spec and full `make check`; local and multi-destination candidates compiled valid by the real Go helper. This gate does not certify final v7 family eligibility, PR #403/#412 dashboard coverage, Galileo equivalence, or complete migration goldens. |

### P2 — Canonical Router, Redaction, SQLite, Retention

| ID | Status | Owner | Depends on | Deliverable | Verification/evidence |
|---|---|---|---|---|---|
| `P2-WP01` | `DONE` | root + subagents | `P1-GATE` | Immutable generic canonical record substrate, deterministic current classified-log builder, and strict registered-identity validation; generated trace/metric family builders remain `P5-WP02` | Commits `13fddfe7c..b328351a6`; lossless lexical JSON, exact union/classes/bounds, immutable values/records, registry-resolved builders, authenticated minimal floor, adversarial normal/race/vet gates |
| `P2-WP02` | `DONE` | root + subagent | `P2-WP01` | Collection/floor gates and per-destination route compiler/evaluator | Commit `b8a2a9823`; immutable metadata, zero-allocation collection admission, floor over/understatement prevention, ordered selectors/drop/fan-out/capability tests, normal/race/vet gates |
| `P2-WP03` | `DONE` | root + subagents + Claude review | `P2-WP01` | Central profiles/detectors/field transforms and cross-language `hash-v1` | Commits `ba0f56636..b5890a92c`; exact profiles, 14-detector conformance, Unicode/hash parity, pure legacy-v7, key custody, immutable projection, P-060/P-061 privacy hardening, config adapter, normal/race/vet/schema/spec gates |
| `P2-WP04` | `DONE` | root + SQLite/judge/storage/graph subagents + Claude + CodeRabbit | `P2-WP01..03` | Implicit SQLite store, projections, integrity, judge separation | Commits `4f2a80166`, `1e0b9ea42`, `39fe480d1`: exact graph-bound local projections, mandatory all-bucket SQLite pipeline, hardened main/sidecar paths and readiness, correlation-key integrity, immutable finding plus mutable alert CAS/receipts/replay, retention-safe judge separation, post-commit reentrant health, and rollback-readable additive schemas. Normal/race/vet/Windows/make/spec/inventory gates passed; 12 of 16 CodeRabbit issues fixed and 4 contract/query-invalid suggestions rejected with evidence. |
| `P2-WP05` | `DONE` | root + runtime/storage subagents + adversarial/Claude-assisted review | `P2-WP04` | Global retention reaper and immutable atomic runtime graph/reload/health | Commits `9a982e816..8d26ebd4f`: exact global age/cutover retention, process-stable readiness-gated scheduler, atomic graph swap/leases/rollback, lossless ordered reentrant reporting, real SQLite path identity, graph-bound local runtime, synchronous retention-policy activation, bounded retryable shutdown, and normal/race/vet/Windows/failure/stress evidence |
| `P2-GATE` | `DONE` | root + runtime/security subagents | `P2-WP01..05` | Representative producers route once through safe canonical path | Commit `1e8463ad6`: bound Sidecar start/stop persist exactly once through the real runtime; unbound v7 behavior remains exact; disabled collection and all ambiguous/failure paths never fall back; focused normal/race/stress, full gateway, vet, spec, and PR #412 dashboard gates passed |

### P3 — Destinations, OTel, Galileo, Local Observability

| ID | Status | Owner | Depends on | Deliverable | Verification/evidence |
|---|---|---|---|---|---|
| `P3-WP01` | `DONE` | root + config/delivery/network/adapter/runtime subagents | `P2-GATE` | JSONL, console, Splunk HEC, HTTP JSONL adapters and isolated queues | Commits `d493f487c..a3c76dc3b`: bounded delivery, compiler defaults, guarded local/push adapters, generation-owned post-SQLite dispatch, composite factory, and real five-destination fan-out with reload/failure isolation passed normal/race/vet/Windows and PR #412 dashboard gates |
| `P3-WP02` | `DONE` | root + OTel/inbound subagents | `P2-GATE` | OTLP log/trace/metric routing, projection, sampling, inbound normalization | Commits `d068e0bee..7ba051348`, `8d6e0ae44`, `13c03a822`, `ff710467e`, `1bd90a44d`, `dec25eaa9`, and `ffab0c3ab` add exact terminal partial accounting, plan-aware signal-pipeline ownership, real HTTP/gRPC canonical OTLP log/trace/metric fan-out, provider/runtime generation binding, typed trace and generated metric projection, strict generated-pair acknowledgement, and fail-closed production bootstrap/reload before serving. Production inbound v8 strictly normalizes JSON/protobuf; emits generated accepted/rejected/authentication logs plus all five ingest and source-backed GenAI token/duration metrics; preserves Claude cache categories and PR #412 labels; hardens loop suppression; and never dual-writes legacy telemetry. Generated receive SERVER roots and normalize INTERNAL children now share one generation lease, retain only measured bounded metadata, remain independent of durable logs/metrics, and abort self-exports before canonical handoff. Commit `b67961e23` closes the missing contract with D-023/P-071..075: compiler-expanded per-target bindings, import-only non-floor construction, exact GenAI/Codex/Claude derivations, original-versus-forward instance provenance, four-hop termination, per-leaf accounting, and OTLP-A01..A24. Commit `6e10950d9` retains the official typed request through v8 admission, rejects duplicate JSON object members recursively, preserves repeated OTLP attribute-list entries for generated target-specific ambiguity checks, and counts metric data-point leaves exactly; generic-map import remains forbidden. Commit `c6c72752a` separates match and target IDs plus native/external shape authority; `47b7eb4f3` pins all imported log/metric targets in one request generation and proves disabled/floor safety across reload. Commits `9a350ef76` and `cd68fcf06` make nested import-provenance normalization executable and add its copy-safe canonical Record boundary without exposing it through ordinary family inputs. Commit `d6e4d568d` generates the closed 237-match/245-target inbound catalog, 24 native markers, 249 echo recognizers, 93 non-floor log contexts, concrete private family descriptors, and 711 JSON/protobuf fixture cases. Commit `287b78786` loads those tables once behind immutable typed handles with exact cross-reference/concrete-descriptor validation and no construction capability. Commit `1369de338` publishes the closed 13-field nested `provenance.import` structure, executable mode/derivation/count/hop rules, runtime UTF-8-byte ownership, public schema/catalog, examples, and package resources. Commit `c64dcbec4` adds the pure typed OTLP-I02 classifier and a collision-checked wire-echo index: all 237 match descriptors pass 711 exact positive/negative/single-fault cases, malformed native shapes remain terminal, and classification grants no construction or floor capability. Commit `f7df935c9` adds the private ordinary-only imported-log builder: all 93 contexts and 96 log targets bind exact generated descriptors/source/provenance/conditions, reuse the four sealed generated GenAI structured grammars, stop before construction when collection is disabled, persist SQLite before remote fan-out, and export nowhere after local persistence failure. Commit `6d1641203` seals exact source-unit rules and constructs every 31 trace, 104 native metric, and 14 derived metric target with generated field/topology/provenance authority; imported spans preserve sender topology through direct canonical fan-out, collection precedes mapping and occurrence allocation, and trace/metric paths create no SQLite rows. Commit `3ddb79bf8` adds the imported-only origin and terminal-hop seam: exact local origins suppress only their destination, foreign provenance has no routing authority, and hop-four logs remain durable while trace/metric construction reports suppressed export. Commit `7b55e880b` activates the official typed receiver per leaf under one pinned generation, makes generated PR #412 plans the sole v8 token/duration authority, preserves strict structured GenAI/topology/resource/time/import provenance, and enforces collection-before-construction plus per-destination echo/redaction isolation. Commit `457ef5bf8` makes that typed receiver the only live handler: unbound traffic fails closed and the v7 audit/provider/raw-envelope/HEC/heuristic path is deleted. OTLP-A01..A21/A23, full scoped normal/race/vet, and Windows compile gates passed; live destination evidence remains at P3-GATE/P6-WP03 rather than in this implementation package. |
| `P3-WP03` | `DONE` | root + telemetry subagent | `P3-WP02` | Metric catalog/gates/bounded attributes and native Prometheus option | Commits `6d4a20f64`, `b6c4ea312`, `7ba051348`, and `eed11d322` preserve the exact 131-instrument/60-second-delta catalog with collection gates, bounded attributes/cardinality, independent registered readers, retry-truthful lifecycle, and real runtime fan-out/reload/rollback isolation. Native Prometheus now consumes generated local projections on its existing generation-owned endpoint while retaining all 131 active legacy producer fields during cutover; separate private registries merge exact overlap without scrape failures, reapply the 2,048-series ceiling, and fail closed on incompatible HELP or histogram boundaries. |
| `P3-WP04` | `DONE` | root + Galileo subagents | `P3-WP02` | Galileo projection, delivery funnel, partial success, exact canary | Commits `9269b80c5`, `d068e0bee..69ccc62db`, `ab48aae80`, `8d6e0ae44`, `8d3d7c480`, `ebaed7dbb`, `7f47f46f3`, `b57703f30`, and `fa498f0a9` implement generated `galileo-rich-v2` projection, generation-owned canonical consumption, guarded delivery, partial-success accounting, strict canary acknowledgement, v8 setup/status/test UX, all local-judge call shapes, generated structural authority, and generic-versus-Galileo isolation. Commits `4a2e611d5` and `10d417c60` replace the two-span-only runtime gate with one six-family W3C graph covering workflow, agent, model, tool, retrieval, and judge; it asserts generated profile membership, resource/scope schema, OpenInference shape, topology, events, links, safe synthetic structured model messages, tool arguments/results, usage, delivery health, and zero canonical failures. Focused normal/race/vet passed, and the exact rich graph with a fresh trace ID was accepted without partial rejection by the configured live Galileo HTTPS collector in 2.32 seconds. |
| `P3-WP05` | `DONE` | root + local-observability subagent | `P3-WP02..03` | `local-observability-v1`, Collector pipeline, PR #412 query inventory | Commits `d052c4113`, `3197fdf74`, and `ecf70595c` freeze 441 consumer expressions, 14 dashboards/313 panels, 131 metrics, datasource/Collector/spanmetrics/volume/package parity, correct truthful first-phase lifecycle emission, bounded live queries, a green 48-hour inventory, and rendered root/direct+nested-subagent Agent360 evidence across Loki/Prometheus/Tempo. |
| `P3-GATE` | `DONE` | root | `P3-WP01..05` | All destinations isolated; Galileo and local stack preserve baseline | The five-service local stack retained 14 dashboards, 313 panels, 441 queries, four history volumes, a complete root/direct/nested-subagent Agent360 trace/log/metric run, and zero Collector refusal/export errors at P6-WP03. General OTLP and Galileo use destination-private generated projections with exact sibling isolation. The final six-family runtime graph passed normal/race conformance and live Galileo collector acceptance with no partial-success rejection. |

### P4 — Producer Migration and Legacy Removal

| ID | Status | Owner | Depends on | Deliverable | Verification/evidence |
|---|---|---|---|---|---|
| `P4-WP01` | `DONE` | root + audit/schema subagents | `P3-GATE`, `P5-WP02` | Control-plane/platform/health producers migrated | Commits `eb507a119`, `7eb3d28eb`, `9392a48dd`, `35132e2bb`, `2000bfb93`, and `bbff8ee1c` route control-plane, authentication, approval, audit-sink, and subsystem health through generated families with exact floors and independent generated metrics. Commits `ceff8cdb2` and `efe77d19d` add the remaining judge-body close-skipped health and sidecar/guardrail/watcher start-stop lifecycle transitions without fabricating state. Commits `00d589f71` and `95ad8f412` hard-cut ordinary sidecar, scoped OTLP, and guardrail-proxy authentication failures to generated logs and metrics with bounded route/reason labels; none can resurrect a gateway event or Provider metric. Commits `d94f4bfcd` through `4d4df7b6e` subsequently removed the obsolete live v7 provider/bridge branches; the repository hard-cut audit proves no active control-plane/platform fallback remains. |
| `P4-WP02` | `DONE` | root + audit/security-schema subagents | `P3-GATE`, `P5-WP02..05` | Guardrail/model/tool/approval/enforcement producers migrated | Commits `2cc88f1d0`, `1f6b26404`, `7eb3d28eb`, `bd3e7df60`, `b57703f30`, and `88c92860d` activate truthful root-agent/model/judge/approval/enforcement and hook lifecycle/model signals with generated logs, traces, and metrics. Commit `c0fcbb764` completes successful quarantine as two linked mandatory occurrences—`enforcement.quarantine.applied` and `asset.quarantined`—plus the generated quarantine metric on one pinned generation; a failed move cannot invent an asset transition. Commit `11f0f002e` removes the dedicated judge/quarantine APIs' unbound persistence and fanout escape hatches: invalid facts, detach, or a missing batch fail closed before any alternate owner can write. Commit `1d6debca9` cuts terminal EventRouter approval decisions to the generated request-bounded approval trace, preserves default-unredacted command/argv/cwd facts for central per-destination redaction, leaves pending requests outcome-free, and makes v8 authority sticky so detach cannot resume direct legacy OTel spans or alerts. Commit `657211ff6` cuts EventRouter tool calls/results to generated requested/terminal logs, tool traces, and metrics; joins only exact call IDs; bounds retained source facts by count/age/bytes; makes missing, conflicting, and expired joins result-only; and removes the name-keyed span queue plus v8-authoritative legacy call/result rows and direct SDK spans. Commit `ea713545a` deletes the local LLM judge's direct SDK span and metric owner plus its v7-only tests and APIs; every judge path now emits the generated rich span and generation-pinned gateway/guardrail/GenAI metrics, with standards-defined `chat` operation semantics and metrics surviving trace sampling. Commit `31c97a685` cuts the blocking chat HILT manager to generated request/resolution compliance logs, a rich approval span, and an independently collected typed lifecycle metric; terminal resolutions retain the SQLite floor, technical failures remain distinguishable, and no direct audit logger or SDK provider can resume after v8 binding. Commit `ff086a82a` removes EventRouter's retained SDK provider and cuts completed WebSocket model messages plus guardrail evaluation/runtime-alert metrics to generated v8 families. Commit `a5d6a960f` cuts regular, synthetic, rejected, and unified connector-hook telemetry to the generated `hook_decision`, hook, inspect, and token families while retaining active-span forensic identity and exact local-dashboard dimensions. Commit `a44397b58` cuts every HTTP inspect, policy-rule, and CodeGuard evaluation/latency metric to generated v8 families with exact dashboard projections, canonical severity, request correlation, and per-connector scanner attribution without duplicate inspect counts. Source token, finish-reason, tool-count, response-content, decision, evaluation, and security dimensions remain independently collection-gated. Subsequent guardrail event/evaluate, policy, proxy phase, finding, alert, and sidecar hard-cut commits removed the remaining live v7 owners while preserving source-backed facts and dashboard dimensions. |
| `P4-WP03` | `DONE` | root + domain-schema subagents | `P3-GATE`, `P5-WP02` | Scan/asset/network/discovery/ingest producers migrated | Commits `5cb4ac73a`, `dec25eaa9`, `f4d09dfa8`, and `7b55e880b` bind asset discovery, OTLP ingest, agent/AI discovery, and accepted records to generated v8 authority with PR #412 metric continuity. Commit `efe77d19d` cuts correlated audit/gateway network egress logs and its metric to generated families without exporting full URLs. Commit `e831701b5` persists forensic scan tables and then emits every immutable `finding.observed` before its generated `scan.completed`/`scan.failed` summary plus all source-backed scan metrics on one generation. Commit `c0fcbb764` adds the required post-success asset-quarantine companion. Commit `11f0f002e` makes asset discovery and network egress v8-only at their production APIs, rejects invalid or unavailable runtime ownership before fallback persistence/fanout, and retains only canonical local event-history and forensic projections. Commit `457ef5bf8` removes OTLP ingest's unbound v7 producer, raw-body retention, and direct audit/provider fan-out while retaining typed accepted-record and PR #412 derivation behavior. The Go/Python hard-cut commits subsequently deleted residual discovery/config-v7 runtime branches and fallback-only tests; the generated domain producers are the sole live owners. |
| `P4-WP04` | `DONE` | root + producer-authority subagents | `P4-WP01..03` | Duplicate bridges/direct fan-out/global toggles/legacy emit gates removed | Generated authority owns every current family through compact generated producer identity sets; duplicate watcher/receiver paths and handwritten identity vocabularies are gone. Commit `0cec33a8f` makes target startup and reload strict v8-only before compatibility decoding, deletes legacy config callbacks plus OTel/audit-sink reload branches, and removes legacy runtime environment switches. Commit `11f0f002e` removes the asset/judge/quarantine/egress producer escape hatches and their direct sink/structured/OTel fanout. Commit `31c97a685` removes the HILT manager's direct audit/provider ownership, makes generated-only metric additions registry-owned without duplicating the legacy migration inventory, and deletes remaining exact-total assertions exposed by the additive family. Commit `457ef5bf8` deletes the receiver's v7 audit/provider, generic session/token/duration, Splunk HEC shadow, raw-envelope dedupe, and hook-usage bridge paths plus their runtime-only tests; an unbound handler now returns 503 without a SQLite write. Commit `ff086a82a` removes EventRouter's provider field, constructor argument, accessors, direct model span, inspect metric, and runtime-alert calls; generated v8 families are now its only telemetry owner. Commit `a5d6a960f` removes the connector hook's direct invocation/outcome/token/inspect/unified-dispatch Provider calls and free-form OTLP log bridge; the canonical durable log and generated metrics are now authoritative for regular, synthetic, and rejected requests. Commits `00d589f71` and `95ad8f412` remove API, scoped-OTLP, and proxy authentication gateway-log/direct-Provider fallbacks while retaining generated dashboard visibility. Commit `a44397b58` removes the remaining HTTP inspect and guardrail metric Provider calls; absent v8 metric capability cannot revive them, while the local-observability profile preserves every established series and bounded label. Commits `d94f4bfcd`, `18aaa7d9e`, `4d4df7b6e`, and `2777ae256` completed D-024. The semantic audit scans 1,107 source files and permits v7 only at converter/preview, historical fixture, rollback/recovery, and additive consumer-projection boundaries. |
| `P4-GATE` | `DONE` | root | `P4-WP01..04` | Every current producer classified; no active duplicate/legacy path | The semantic hard-cut audit passes across 1,107 source files; its four adversarial tests reject removed writers, ambient direct OTel, legacy span arms, and disallowed runtime compatibility. |

Checkpoint `e1d1507ac` advances `P4-WP02`, `P4-WP04`, `P5-WP04`, and
`P6-WP03`: connector lifecycle logs, transitions, model spans, tool spans, and
their metrics no longer have an unbound or failed-capability
`telemetry.Provider`/gateway-event fallback. Independently delivered session,
subagent, turn, and compact facts use one generated request-bounded
`span.agent.transition` root (or an inbound-W3C child) instead of fabricating a
completed `span.agent.invoke` or retaining a span/runtime lease across hook
requests. The content-free `hookSessionState` cache retains only bounded
identity/lineage needed to join later model and tool occurrences. Generated
tests preserve first-phase absence, multiple inferred children, lifecycle-only
start/end visibility, exact W3C IDs, model/tool timing and tokens, per-route
redaction, dedupe, and all local-observability consumer dimensions; the removed
tests asserted only the deleted v7 SDK topology.

Checkpoint `a44397b58` advances `P4-WP02`, `P4-WP04`, and `P6-WP03`:
HTTP prompt/response/tool inspection and policy-rule/CodeGuard evaluation
metrics now use the generated inspect and guardrail families exclusively.
Canonical `NONE` becomes `INFO`, fractional-millisecond latency is preserved,
request correlation reaches the generated envelope, and scanner metrics carry
their real connector. CodeGuard emits an additional scanner observation but no
duplicate inspect occurrence. The local-observability projection retains the
existing Prometheus names and `action`/`connector`/`severity`/`tool`/scanner
labels, and dashboard provenance names the generated producers.

Checkpoint `e7a30608b` advances `P4-WP02`, `P4-WP04`, `P5-WP04`, and
`P6-WP03`: EventRouter exec approvals now own generated rich request/resolution
logs, a terminal rich span, the typed exec lifecycle metric, and the
generated `approval.count` dashboard compatibility metric. Pending requests
remain outcome-free; disabled compliance logs retain only the mandatory
content-free resolution floor. The direct approval SDK span, provider
metric/alert, and legacy requested/pending/resolution audit writes are gone.
Because the current approval wire has no session/run anchor, the span remains a
truthful root instead of inheriting an unrelated most-recent model context.

Checkpoint `fe1433640` advances `P4-WP02`, `P4-WP04`, `P5-WP04..05`, and
`P6-WP03`: proxy prompt, completion, and proposed-tool evaluations now have
one generated owner for the durable evaluation log, native guardrail span,
evaluation/latency metrics, and actual-enforcement companion. Exact W3C,
request, agent/model, evaluation, scan, policy, and enforcement identities join
those signals; observe-mode blocks remain `would_block` allows and never invent
enforcement. The supported agent/model spans receive a bounded
`guardrail.decision` event for Galileo while the native guardrail span retains
the complete evidence. Trace sampling cannot suppress logs or metrics,
disabled enforcement logging retains only the authenticated mandatory floor,
and raw proxy bodies are no longer copied into free-form audit details outside
central destination redaction. The overview and traffic dashboards now combine
the generated proxy series with existing inspect series without renaming either
producer contract.

Checkpoint `6d1a3cb72` and `3c56fe21d` complete the proxy inspector's
stage/phase trace ownership under `P4-WP02`, `P4-WP04`, and `P5-WP04`.
Generated `span.guardrail.phase` children for regex, AI Defense, judge, policy,
and mid-stream inspection share the exact evaluation lease and retain
fractional timing beneath the prompt/output `span.guardrail.apply`. The
inspector no longer installs Provider stage/phase/panic callbacks. Evaluation,
caller-minted scan, rule, enforcement, W3C, durable-log, metric, and SQLite
identities remain joined; observe mode and already-delivered streams cannot
claim enforcement. Sampling or collection decline cannot revive a legacy span,
and scanner persistence validates but never serializes its caller-minted scan
UUID. Galileo continues to receive the bounded guardrail decision on supported
agent/model shapes while native phase detail remains destination-neutral.

### P5 — Rich Telemetry Registry and Trace Contracts

| ID | Status | Owner | Depends on | Deliverable | Verification/evidence |
|---|---|---|---|---|---|
| `P5-WP01` | `DONE` | root + registry/compiler/domain subagents + Claude review | `P2-GATE` | Registry authoring model, dependency lock, sole compiler | Commits `73a54b548`, `d95c2f23f`, and `d1ed5278d`: pinned lossless upstream snapshots; 277 local attributes plus 45 privacy overlays; 25 bounded span and 131 exact metric families; structured normalizers; sole offline compiler; projection-aware current metric inventory; atomic drift/provenance gates. Seventy-seven focused tests, schema/inventory/generator/Go gates, and two Claude Opus xhigh reviews passed with no unresolved finding. |
| `P5-WP02` | `DONE` | root + generation/public-view/structural subagents + Claude review | `P5-WP01` | Generated bundle/catalog/docs/constants/builders/field classes/selector registries/fixtures | Commits `852b19195` through `793473271` establish the lossless immutable registry IR, structural/privacy/outcome/mandatory contracts, complete generated Go and portable candidates, transactional publication, first/repeat public-schema adoption, and the typed 21-view/756-field/94-dynamic-scope/39-reference compiler boundary. Commits `76669620f`, `e25666bea`, `385949ebe`, and `8d064149a` add copy-safe Go/Python distribution, stage all 26 marker-only views without claiming live authority, and execute the exhaustive legacy parity corpus. Commit `f980f102b` performs the one manifest-last cutover: all 21 public schemas, four gateway mirrors, and one CLI embed are generated at their live paths; the staged tree is retired; exact prior-schema validation remains digest-addressed; and direct live parity plus installed-wheel bytes pass. The final gate covered all 519 generator, 235 renderer, 145 transaction, and 12 parity tests; generator/schema/v7 gates; Go normal/race/vet/gofmt; and clean Python 3.12 wheel installation. |
| `P5-WP03` | `DONE` | root + trace-contract/lifecycle subagents | `P5-WP01`, `P3-WP02` | Rich bounded spans/events/links/status/content/retry/timing | Commits through `e39a7f844` and `8d6e0ae44` close the 1,785-declaration rich-trace substrate: generated trace state/full flags, exact resource identity, clone-only count/byte-bounded canonical handoff, per-destination canonical/legacy XOR, collection/sampling/generation/parity gates, panic-safe lifecycle, and the fail-closed agent-root/model-child release canary. One runtime lease now covers generated construction, handoff, flush, acknowledgement, concurrent reload publication, and old-generation retirement; bare v8 providers cannot bypass ownership. Generic OTLP and Galileo acknowledge only the exact canonical pair and preserve family/bucket, hierarchy, resource, scope, flags, and trace state. The independent diagnostic family remains separate, and marked pairs remain in Tempo while the Agent360 spanmetrics branch excludes them. Normal/race/vet/generator/spec/dashboard gates passed. Canonical destination activation remains gated on P5-WP04/WP05, P3-GATE, and P4. |
| `P5-WP04` | `DONE` | root + bounded lifecycle subagents | `P5-WP02..03` | PR #403 lifecycle fixture migration and missing-data fidelity | Commits `2cc88f1d0` and `1f6b26404` activate real root-agent/model traces with honest missing identity and conversation data. Commit `c5a0a9938` adds generation-pinned agent-transition and approval handles plus exact root/direct/nested-subagent and tool/approval topology, reload, sampling, and local-profile tests. Commit `b4c6207ce` publishes one request-bounded root agent/model/tool/approval lifecycle interface atomically to API, EventRouter, and proxy consumers; root approvals require reported identity and never fabricate an agent, while construction, replacement, and shutdown cannot retain a retired generation. Commit `4976f0739` moves Codex, Claude Code, generic connector, and inferred-child durable lifecycle occurrences to exact generated session/subagent/turn/tool/compact/event families with conservative lineage provenance, source-backed terminal outcomes, collection/sampling independence, and canonical/legacy XOR while preserving existing PR #403/#412 metrics, spans, and non-lifecycle fanout. Commit `b30b8b103` makes transition, last-seen, current-phase, phase-transition, and reported-cost metric updates survive config-v8 startup without publishing or retaining an SDK provider; one runtime lease covers the entire occurrence and each family remains independently collection-gated. Commit `88c92860d` adds request-bounded hook agent/model hierarchy and pins its Agent360/token/duration metric siblings to the same live generation without retaining spans across hook deliveries. Strict redaction and raw routes are applied only after generated construction, missing identities remain absent, and unreported finish reason no longer drops reported content. Commit `157423063` pins EventRouter to the authoritative OpenClaw run-observation contract: current callbacks lack `sessionId`, validated agent/root/parent/depth/type and terminal facts, payload sequence differs from connection sequence, and no cross-delivery trace handle is allowed. P-076 defines the only truthful partial cutover as outcome-free `log.agent.run.observed`, outside PR #403 lifecycle metrics and Agent360, with ER-RUN-01..12. Commit `435552bd5` implements that family and cuts EventRouter over while preserving literal start/end/error observations, checked source time, bounded exact dedupe, mandatory content-free rejection accounting, and no cross-delivery trace context or inferred correlation. Commit `1d6debca9` emits each actually observed automatic approval or policy denial as one generated `span.approval.resolve` with requested/resolved events, exact result/actor/dangerous/content facts, process/policy correlation, and no invented agent or session topology; pending manual approvals remain durable audit occurrences until a terminal result exists. Commit `657211ff6` completes the retrospective call-ID-safe tool adapter: two same-name calls cannot cross-pair, no span/generation handle crosses deliveries, missing topology stays absent, disabled trace collection cannot suppress enabled metrics, raw content reaches only central redaction, and generated SQLite projections retain the PR #412 tool dimensions without a legacy row. Commit `31c97a685` gives blocking chat approvals one correlated W3C operation: request/resolution logs, trace events, lifecycle metrics, and the persisted projected record share exact trace/span/run/request/session/turn/agent/evaluation/policy identity; missing topology remains absent, sampling cannot suppress durable logs or metrics, and the legacy compatibility reader now round-trips `turn_id`. Commit `ff086a82a` adds the truthful EventRouter message boundary: a completed assistant message with no start time becomes a zero-duration generated model span, source token/tool/finish facts remain present, and an ended W3C context may parent only a matching session/run tool. The bounded cache retains neither a live span nor a generation lease, rejects cross-session/ambiguous matches, and never parents an unanchored approval. P-076 closes the current OpenClaw/EventRouter boundary truthfully: its wire lacks the stable topology needed for a full invocation span, so `log.agent.run.observed` is the completed adapter rather than fabricating PR #403 lineage. Full root/direct/nested-subagent topology remains covered by the richer hook wire and its goldens. |
| `P5-WP05` | `DONE` | root + bounded projection subagents | `P3-WP05`, `P5-WP02` | Galileo/OpenInference/local-observability generated projections | Commits `8d3d7c480`, `13c03a822`, and `3de157cee` activate mutually exclusive Galileo/generic/local canonical trace consumers, preserve local Agent360 aliases, and materialize digest-bound generated manifests for all three compatibility profiles. Commit `dc4797439` makes that generated authority executable for the original 131 metric contracts and adds generation-owned canonical-versus-local projection, stable per-destination sink ownership, and one runtime lease across pre-construction collection gating through delivery. Commit `1bd90a44d` activates destination-private generic and local OTLP metric sinks with exact generated selection, resources, label dialect, instrument/value types, units, boundaries, delta temporality, and cardinality, plus real HTTP/gRPC, reload, sibling-failure, rollback, and shutdown evidence. Commit `eed11d322` activates native Prometheus generated records through a no-loss legacy/generated bridge with exact overlap and cardinality semantics. Commit `fa498f0a9` binds Galileo's exact six shapes to generated attribute, event, event-field, link, link-field, resource, and scope contracts while retaining only five explicit compatibility-only inputs; a seven-span root/subagent/workflow/model/tool/retrieval/judge graph proves rich projection and exact topology. Commit `31c97a685` proves an additive generated-only metric automatically reaches the local compatibility manifest and native Prometheus label dialect without a legacy inventory entry. Commit `ff00a5e1a` activates the generated OpenInference projection with exact profile-manifest authority and OTLP destination tests. Local observability covers every declared family, and Galileo retains its exact six rich shapes. Credentialed Galileo cloud delivery remains the separate P3/P8 live gate. |
| `P5-WP06` | `DONE` | root + compatibility-generation subagent | `P1-WP04`, `P5-WP02..05` | Generate `compatibility/v7-exporter-selection.json`, integrate it and the generated local-observability/Galileo profiles with the converter engine, and complete the reviewed mapping-by-mapping v7-to-v8 golden matrix without wildcard broadening | Commits `4ea96120a`, `8bcc396ed`, and `160933297`: the sole compiler derives all 14 always-local log buckets, all conditional signal buckets, generic/local 14/25/14 selection, Galileo's exact three spans, 188 audit actions, 172 gateway identities, and the exact 19 four-path audit-sink gateway identities. The four additive identities are the source-backed AI/agent discovery families introduced by the enriched registry epoch. The packaged loader is epoch-bound to `catalog.json`, has no repository fallback, and passed migration/compiler/renderer/wheel gates. |
| `P5-WP07` | `DONE` | root | `P5-WP02`, `P5-WP04..05` | Maintainable compiler and compact generated representation | Commits `d7b3b85a5`, `435552bd5`, and `2fea68c6d` remove hand-maintained global family/symbol/declaration/descriptor/producer-row counts and current-output digest pins; inventories derive from authored registry/ownership data; `make telemetry-generate` and `make telemetry-check` are the public workflow. Producer compilation validates every expanded source tuple but generated Go stores each exact identity once, each of the 19 contextual sets once, and 203 producer default/set references instead of the 8,096-row Cartesian product, reducing the producer authority by about 71% without changing routing. The static API gate derives every input/descriptor/builder relationship from generated family descriptors; no duplicate method JSON inventory or dead compile-only test functions remain. Curated fixture output falls from 287 KB/4,470 lines to 93 KB/356 lines, and the repository manifest owns exactly the 40 materialized outputs while still constructing and validating reproducible reports/corpora in memory. Commit `31c97a685` removes the last routine-addition coupling exposed by the HILT metric: generated-only families need no legacy inventory row, native inbound/metric tests derive their inventories, and migration callsite evidence uses stable producer files plus exact generated builders rather than source line numbers. |
| `P5-WP08` | `DONE` | root + compiler/generated-asset subagents | `P5-WP07` | Reviewable generated footprint with identical wire/runtime contracts | Commits `a6fe4e2f6` and `ae6ad7530` removed public-view/baseline/adoption duplication, durable output journals, tracked staging, unused semconv payload, plaintext runtime JSON copies, and expanded native OTLP tables while preserving published schema bytes and raw-byte APIs. The final branch is about 338k added/54k removed (+284k net); 270 compiler/renderer tests, registry drift, full observability destinations/integration, telemetry, hard-cut, and 14-dashboard/326-panel checks pass. |
| `P5-GATE` | `DONE` | root | `P5-WP01..08` | One canonical registry, complete generated migration selection, and no independently maintained telemetry schemas or converter family lists | Generated semantic diff, converter goldens, public-schema byte parity, executable Galileo/OpenInference/local profiles, deterministic drift checks, and compact runtime artifacts are verified. |

### P6 — Operator Experience, Dashboards, and Documentation

| ID | Status | Owner | Depends on | Deliverable | Verification/evidence |
|---|---|---|---|---|---|
| `P6-WP01` | `DONE` | root + inspection/destination subagents | `P4-GATE`, `P5-GATE` | Validate/effective/reference/plan/destination-test UX | Commits `ff4d566f5` and `94e48e12f` complete truthful effective/provenance/plan inspection plus the safe single-destination handshake/write-probe command. Attempt and terminal activity use generated typed families, authenticate through an internal loopback helper without exposing the bearer to Python/argv, persist exactly once through the local-only SQLite path, reduce to the mandatory floor when collection is disabled, and never fan out to optional destinations. Clean detached-worktree generator, Go, CLI, race, and failure-path gates passed. |
| `P6-WP02` | `DONE` | root + operator-control/health subagents | `P6-WP01` | Setup/TUI/doctor destination, privacy, retention, and health UX | Commit `f0d41ba6d` adds canonical masked status, atomic validated source mutation, capability-default setup for every preset, destination list/enable/disable/remove/test controls, and doctor output for SQLite, retention, judge storage, signals, buckets, redaction, and catalog warnings. Commit `c2fa173d8` loads that masked plan asynchronously in the TUI and merges it with only content-free observed health. Commit `40c66613f` publishes the complete generation-pinned destination/signal inventory with queue utilization, monotonic counters, real success/failure times, stale-generation suppression, and separate retention/event-history state. TUI and doctor render the resulting content-free state while sanitizing endpoints/errors and showing `unavailable` rather than inferring missing runtime facts; normal/race/vet, full gateway, and CLI grammar gates passed. |
| `P6-WP03` | `DONE` | root + live-destination subagent | `P3-WP05`, `P5-GATE` | Coordinated dashboard/rule/query migration and local-stack validation | The preserved five-service stack passed all 14 dashboards, 313 panels, and 441 queries with zero errors. Fresh run `a22-20260706-a37c625f` produced one eight-span root/direct/nested/model/tool/workflow trace, 16 correlated Loki records across three agents, and 17 Prometheus families; 25 Agent360 panels were populated, all 50 focused dashboard tests passed, and Collector refused/export error counters remained zero. Commit `31c97a685` replaces the chat HILT panels' overloaded guardrail query with the typed `defenseclaw_approval_lifecycle_total` result/surface/connector contract while leaving exec approval queries isolated. Commit `ff086a82a` moves inspect filters from encoded tool prefixes to the generated first-class connector label, moves runtime-alert panels to `defenseclaw_alert_count_total`, and removes source-level `NONE` assumptions after canonical normalization to `INFO`; the packaged-source parity and complete 14-dashboard/313-panel semantic audit remain green. |
| `P6-WP04` | `DONE` | root + operator-documentation subagent | `P6-WP01..03` | User/admin/developer/migration/release documentation | Commit `700248376` publishes the concise config-v8 authoring/effective-plan workflow, all 14 buckets, collect/export/unredacted defaults, independent multi-destination fan-out, SQLite floor, field-aware redaction, Galileo/local/Splunk behavior, and one-command upgrade/rollback/Agent360 continuity. The 143-page docs build, link validator, 500-snippet validator, 1,031-command grammar check, and diff checks passed. |
| `P6-GATE` | `DONE` | root | `P6-WP01..04` | Operators can author, inspect, test, and diagnose the full graph | Generated source/effective/reference/plan/test UX, masked health/TUI/doctor, complete documentation, and the real preserved-volume dashboard/query/trace/log/metric run all passed. Galileo runtime delivery remains the separate P3-GATE obligation. |

### P7 — Automatic Upgrade Integration

| ID | Status | Owner | Depends on | Deliverable | Verification/evidence |
|---|---|---|---|---|---|
| `P7-WP01` | `DONE` | root + upgrade-controller subagents | `P1-GATE`, `P5-WP06`, `P6-GATE` | Publish a reachable `0.8.4` controller bridge, then require protocol 2 and the v8 migration exactly once at `0.8.5` | Release `0.8.4` is published with verified Linux/macOS bridge artifacts, config v7, controller protocol 2, and no v8 runtime. The authenticated target-release shell resolver selects and verifies that bridge, terminates the old path, and re-execs under a fresh bridge controller before the `0.8.5` migration. The immutable bridge parser limitation is documented; no Windows bridge artifact is claimed. |
| `P7-WP02` | `DONE` | root + activation/rollback subagents | `P7-WP01` | Exact activation plus cross-phase rollback to a healthy verified bridge | Live Linux and macOS fault injection retained authenticated bridge artifacts and exact config/database/cursor/bundle custody, restored the `0.8.4` CLI and gateway after post-migration target failure, passed fresh-process health and SQLite checks, left no lease/journal residue, and completed an immediate same-command retry. |
| `P7-WP03` | `DONE` | root + bundle-upgrade subagent | `P3-GATE`, `P6-GATE` | Local bundle backup/refresh/restart with custom files/volumes preserved | Commit `30315ba14` runs the refresh from the installed target wheel, validates every target asset plus the exact five-service/four-volume/fourteen-dashboard contract, creates a private exact managed-file backup, preserves custom files and all named volumes, activates and verifies manifest-last with exact rollback, and conditionally restarts only a previously running stack without `-v`. Unsafe or ambiguous pre-activation state fails closed; post-activation readiness failures degrade without rolling back a healthy gateway migration. The focused and broad upgrade/dashboard matrix passed 324 tests and 12 subtests; the P3/P6 live-stack gate remains authoritative for real Docker evidence. |
| `P7-WP04` | `DONE` | root + release-gate subagents | `P7-WP01..03` | Manifest-derived historical refusal, staged success, rollback, and live continuity matrix | The manifest-derived published-baseline matrix, protocol/refusal fixtures, exact bridge selection, full v8 conversion, rollback fault injection, SQLite/history/secret behavior, fresh-process health, and local-volume continuity gates pass. Real public-bridge Linux/macOS runs additionally prove staged success, failure recovery, and retry outside fixtures. |
| `P7-WP05` | `DONE` | root + Windows release subagents | `P7-WP01..04` | Truthful Windows preflight refusal for a hard cut with no published Windows bridge | Release `0.8.4` intentionally published no Windows binary, gateway, refusal binary, or Windows SBOM. The signed PowerShell resolver and target manifest fail closed before service stop or installed-state mutation when no bridge exists; static installer/refusal tests and Windows daemon/CLI compile gates pass without claiming an upgrade path. |
| `P7-WP06` | `DONE` | root + release-gate subagents | `P7-WP01..05` | Sealed-candidate release workflow | The release workflow builds once, binds manifest/provenance to immutable release source, conditionally signs macOS or publishes the explicit unsigned `-unverified` fallback, omits all Windows binaries for the bridge, and runs protocol, rollback, manifest, version, schema, and parity gates against sealed bytes. The published POSIX-only `0.8.4` run passed; the v8 candidate retains squash-safe dynamic provenance. |
| `P7-GATE` | `DONE` | root | `P7-WP01..06` | One user request stages every supported POSIX old source through a healthy bridge and either reaches healthy v8 or restores healthy `0.8.4`; unsupported platforms refuse before mutation | Published-bridge Linux/macOS upgrade, forced rollback, immediate retry, fresh install, SQLite continuity, and health gates pass. Windows truthfully refuses before mutation because no Windows bridge was published. |

### P8 — Final Integration, Security, Performance, CI/CD, and PR

| ID | Status | Owner | Depends on | Deliverable | Verification/evidence |
|---|---|---|---|---|---|
| `P8-WP01` | `DONE` | root | `P0..P7` | Security/fuzz/network/redaction/canary audit | V-0199: all six required fuzz targets completed their ten-second runs; network, redaction, delivery, and destination suites passed normally and under race; the deterministic security corpus passed; the strict Galileo and generic-OTLP canaries passed without partial rejection or cross-export; every branch secret candidate was inspected and proved synthetic. |
| `P8-WP02` | `DONE` | root | `P0..P7` | Race/performance/cardinality/queue/reaper benchmarks | V-0199: cardinality and queue isolation passed normally and under race, count/byte pressure proved newest-drop plus FIFO recovery, the reaper sustained exact 1,000-row batches, and the recorded three-sample runtime/redaction/projection medians establish the first approved v8 baseline. No comparable canonical v8 runtime exists on `main`, so the baseline is recorded without inventing a before/after percentage. |
| `P8-WP03` | `DONE` | root | `P0..P7` | E2E-1 through E2E-9 and final command suite | V-0205 records the complete local/live suite and all 7,096 collected CLI tests. V-0206 proves the exact committed code head: all 27 CI jobs, the 27-cell connector contract matrix plus selection/report gates, the docs build, and both macOS application gates passed. |
| `P8-WP04` | `DONE` | root | `P8-WP01..03` | Final legacy/duplicate-path/generated-drift/secret review | V-0205 records the final hard-cut, generated-drift, secret, governance, release-contract, and documentation audits. V-0206 supersedes its pre-commit scope count with the exact code head: 1,211 files, +415,040/-57,685 (net +357,355) against base `7544f6157a9fea37ef7f1230495f35582b4ed833`; exact-head CI and targeted Claude reviews found no remaining actionable code defect. |
| `P8-WP05` | `DONE` | root | `P7-GATE`, `P8-WP01..04` | Detailed PR against current `main`, targeted review/fixes, exact-head CI/CD green | PR [#490](https://github.com/cisco-ai-defense/defenseclaw/pull/490) is the single v8 hard-cut PR. V-0206 pins exact code head `a8dabc35f864d65e31f1c02e1cced6073cf4a2f3` and its green CI, connector, docs, and macOS workflows. The sole actionable review issue is code-addressed and verified; no implementation follow-up remains. GitHub discussion resolution and required human approval remain reviewer-owned. |
| `P8-GATE` | `DONE` | root | `P7-GATE`, `P8-WP01..05` | Every requirement proven; active goal may be completed | V-0204 closes the published staged-release gate, V-0205 closes local/live final-candidate verification, and V-0206 closes exact-code-head CI/CD. No required implementation or release work remains. |

## Agent Coordination Protocol

- The root coordinator owns this file, integration ordering, task assignment,
  shared hotspots, commits, branch/PR operations, and final status.
- A subagent receives one concrete work package with normative references,
  dependencies, exclusive write scope, deliverables, and exact tests.
- Read-only inspection is unrestricted. Concurrent writes must not overlap.
- Shared hotspots require root ownership or an explicit handoff: `internal/config/`,
  migration registry/manifest, canonical telemetry registry, generated outputs,
  Makefile, local dashboard bundle, and this manifest.
- Exactly one agent at a time runs generators that write telemetry/dashboard
  artifacts. Generated outputs are changed through their generator only.
- A subagent handoff reports files changed, decisions made, exact commands/results,
  remaining risks, and current diff/commit state.
- Subagents propose `DONE`; root reviews the diff and reruns proportional tests
  before updating status and committing.
- Preserve unrelated user changes. Do not broadly format, destructively reset, or
  expand authority beyond the assigned package.
- A decision blocker pauses only the affected work package. Record the impacted
  D-/S-/P- IDs and continue independent `READY` work.

## Verification Ledger

Results are commit-specific. Append one row per meaningful command run; do not write
only “passed.” A relevant change invalidates old evidence.

| Run ID | Date | Commit | Work package/phase | Command | Observed result | Agent |
|---|---|---|---|---|---|---|
| `V-0001` | 2026-07-02 | `b5167c2d95d72ae2b1c71813aa35fd065c8a2d63` | P0 | `.venv/bin/python scripts/check_grafana_dashboards.py --require-packaged` | 14 dashboards, 313 panels; passed | root |
| `V-0002` | 2026-07-02 | `b5167c2d95d72ae2b1c71813aa35fd065c8a2d63` | P0 | `uv run python -m pytest cli/tests/test_agent360_dashboard.py cli/tests/test_grafana_dashboards.py -q` | 33 passed in 0.61s | root |
| `V-0003` | 2026-07-02 | `963b1bc9fb5a564c53809a70b452e61e888f768c` | P0 | `make check` | v7 parity, 81-decision spec, current-state inventory, 14 dashboards/313 panels, Go/TS provider coverage, catalog, and upgrade manifest passed | root |
| `V-0004` | 2026-07-02 | `963b1bc9fb5a564c53809a70b452e61e888f768c` | P0 | `.venv/bin/python -m pytest cli/tests/test_observability_v8_spec.py cli/tests/test_observability_v8_inventory.py cli/tests/test_agent360_dashboard.py cli/tests/test_grafana_dashboards.py -q` | 42 passed in 3.38s | root |
| `V-0005` | 2026-07-02 | `44b0b059c1f7441279f8f2318ed61c80a4cd25be` | P0 | `make check-observability-v8-spec` and focused spec tests/Ruff | 81 decisions valid; 5 fence-aware spec tests passed; all 5 post-approval CodeRabbit findings resolved | root |
| `V-0006` | 2026-07-02 | `d152924345426208223a07c75be6d8853b86a41c` | P1 | `go test ./internal/audit ./internal/gatewaylog ./internal/observability -count=1 && go vet ./internal/observability` | 14 gateway events and 188 audit actions classified; tests and vet passed | root |
| `V-0007` | 2026-07-02 | `705b185c982e9e6a83c292443689bf6abac06bf1` | P1-WP02 | `go test ./internal/observability -count=1` | Canonical event registry matched 14 gateway/188 audit classifications, 25 trace families, 131 metric instruments, and lifecycle aliases; passed | root + subagent |
| `V-0008` | 2026-07-02 | `780adcf72bf3db0cd1041abf2855197fe95b39f9` | P1-WP02 | `go test ./internal/observability ./internal/config ./schemas -count=1`; focused config race; vet; schema/spec checks; `make check` | Normal/race/vet passed; 23 JSON schemas plus v8 semantic lock passed; 83 decisions valid; v7 parity, 14 dashboards/313 panels, Go/TS provider coverage, LLM catalog, and upgrade manifest passed | root + subagents |
| `V-0009` | 2026-07-02 | `c7234b5de4baa4092761ad886a24ebcba8478af8` | P1-WP03 | `go test ./internal/observability ./internal/config ./internal/cli ./schemas -count=1`; `go test -race ./internal/config ./internal/cli -count=1`; focused v8 Python suite | Normal and race suites passed; 82 Python validator/writer/reference/parity/bridge/CLI tests passed | root + subagents |
| `V-0010` | 2026-07-02 | `c7234b5de4baa4092761ad886a24ebcba8478af8` | P1-WP03 | `uv run python scripts/generate_observability_v8_reference.py --check`; `uv run python scripts/check_schemas.py`; focused Ruff | Six generated/reference/wheel mirrors, 23 JSON schemas, 131-metric catalog, schema mirrors, and all Python lint checks passed | root + subagent |
| `V-0011` | 2026-07-02 | `c7234b5de4baa4092761ad886a24ebcba8478af8` | P1-WP03 | Build real gateway; run `defenseclaw config validate/show/reference` and `defenseclaw observability plan` against a temporary v8 install | Versioned Go/Python bridge, full-capability OTLP expansion, generated reference, and filtered canonical-plan rendering passed | root |
| `V-0012` | 2026-07-02 | `c29cdddc4e785463d8f26da43df199897051c163` | P1-WP04 | `uv run --project cli pytest -q cli/tests/test_observability_v8_migration.py cli/tests/test_observability_v8_compatibility.py`; targeted Ruff/format and `git diff --check` | 237 focused converter/typed-contract tests passed; lint, format, and diff checks passed | root + subagents + Claude review |
| `V-0013` | 2026-07-02 | `c29cdddc4e785463d8f26da43df199897051c163` | P1-GATE | Broad v8 Python suite covering migration, compatibility, config, YAML, parity, reference, spec, config-v8 commands, plan, inspect, and environment-registry coverage | 334 passed in 13.92s | root |
| `V-0014` | 2026-07-02 | `c29cdddc4e785463d8f26da43df199897051c163` | P1-GATE | `go test` normal and race plus `go vet` for `./internal/observability ./internal/config ./internal/cli ./schemas`; `make check` | Go normal/race/vet passed; v7 parity, 91-decision spec, inventory, 23 schemas/131 metrics, 14 dashboards/313 panels, Go/TS provider coverage, catalog, and upgrade manifest passed | root |
| `V-0015` | 2026-07-02 | `c29cdddc4e785463d8f26da43df199897051c163` | P1-GATE | Build `/tmp/defenseclaw-v8`; convert and run `config-v8 validate` for full local-observability and multi-destination/audit/filter candidates | Both real Go compiler results returned `valid:true`; this proves candidate validity, not generated family mapping completeness | root |
| `V-0016` | 2026-07-02 | `b328351a64554dcf47a046d5ed24fd1f0290d833` | P2-WP01 | `go test ./internal/observability -count=1`; `go test -race ./internal/observability -count=1`; `go vet ./internal/observability`; `git diff --check` | Canonical value/record/builder normal and race suites passed after two adversarial subagent passes; all nine original trust-boundary/serialization findings closed or explicitly assigned to generated P5 family validation | root + subagents |
| `V-0017` | 2026-07-02 | `b8a2a982366fc6e6040d39f0b726bc75286dcfa9` | P2-WP02 | `make check-observability-v8-spec`; focused spec tests; `go test ./internal/observability ./internal/observability/router ./internal/config -count=1`; router/record race; vet; diff check | 92-decision spec and 5 tests passed; router/record/config normal suites, record/router race, and vet passed; disabled collection is lazy, current log floor eligibility is catalog-derived and immutable, floor is minimal/local-only, and destination routing is ordered independent fan-out | root + subagents |
| `V-0018` | 2026-07-02 | `ba0f56636` | P2-WP03 | Isolated Go hash normal/race/vet; shared Go/Python golden tests; `scripts/generate_unicode13_repertoire.py --check`; `scripts/check_schemas.py` | Exact 32-byte keyed hash-v1, Unicode-13 repertoire, Windows/UNC/path/URI normalization, shared success/error parity, and generated drift checks passed | root + subagent + Claude review |
| `V-0019` | 2026-07-02 | `bb245eb03` | P2-WP03 | `go test ./internal/redaction -count=1`; `go test -race ./internal/redaction -count=1`; `go vet ./internal/redaction` | Pure legacy-v7 string/entity/content/reason/evidence goldens, global/environment invariance, idempotence, spoof resistance, coordinates, and compatible sink wrappers passed | root + subagent |
| `V-0020` | 2026-07-02 | `bf0def9af` | P2-WP03 | Detector Go normal/race/vet; generated catalog check; `scripts/check_schemas.py`; focused Python config/catalog tests and Ruff | Machine catalog/schema/generated Go/Python parity passed; 14 bounded recognizers, work limits, original-byte intervals, overlap union, token domains, and synthetic corpus passed; exhaustive §6.1 expansion remains active | root + subagent + Claude review |
| `V-0021` | 2026-07-02 | `d44a58f5f` | P2-WP03 | Redaction package normal/race/vet; compile-only tests for Linux, FreeBSD, OpenBSD, NetBSD, DragonFly BSD, and unsupported Windows | Fixed-path key create/load, concurrent convergence, no-follow/type/owner/mode/length validation, entropy/interruption cleanup, immutable access, safe errors, and portability passed | root + subagent + Claude review |
| `V-0022` | 2026-07-02 | `b5890a92c202dd18a1b8c8742187d784fb9b424d` | P2-WP03 gate | `go test` and `go test -race` for `./internal/observability/... ./internal/redaction ./internal/config`; `go vet`; 69 focused Python hash/catalog/config/spec tests; Unicode/catalog generators; schema/spec checks; diff check | All Go normal/race/vet passed; 69 Python tests passed; six generated config mirrors, detector/Unicode artifacts, 23 schemas, 131 metrics, and 95 decision-traceability entries were current and valid | root + subagents + Claude review |
| `V-0023` | 2026-07-02 | `4f2a80166d3dbf477897b2fbd67587e485ae8382` | P2-WP04 immutable event history | `go test ./internal/audit -count=1`; focused `-race`; `go vet ./internal/audit`; actual `origin/main` reopen/read probe | Additive event-history migration preserved v7 provenance meanings, stored exact post-redaction envelopes with projection hash/HMAC metadata, verified bounded ID/ranges, enforced mandatory readiness, and remained readable by the previous code | root + subagents + Claude review |
| `V-0024` | 2026-07-02 | `1e0b9ea426f7acdcaa34e184592dfd4eb4d386ee` | P2-WP04 judge-body cutover | `go test ./internal/audit ./internal/gateway -count=1`; `go test -race ./internal/audit ./internal/gateway -count=1`; focused post-review race; `go vet ./internal/audit ./internal/gateway ./internal/telemetry`; Linux/Windows test compilation | Broad normal passed (`audit` 6.539s, `gateway` 46.699s); broad race passed (`audit` 154.450s, `gateway` 506.823s); focused race, vet, and both cross-platform compile gates passed after all review fixes | root + SQLite/judge subagents + direct/Claude-assisted review |
| `V-0025` | 2026-07-02 | `1e0b9ea426f7acdcaa34e184592dfd4eb4d386ee` | P2-WP04 judge compatibility/security | `make check-observability-v8-spec`; focused TUI pytest/Ruff; create v8 audit/judge DBs, reopen/write with actual `origin/main`, reopen/repair with v8 | 95-decision spec passed; 8 TUI history tests and Ruff passed; actual previous release opened both additive schemas, wrote legacy-shaped rows, and v8 reopened/read them with non-NULL normalized timestamps in all four rows | root + subagents |
| `V-0026` | 2026-07-03 | `39fe480d1266164aef2e0f2b5c70c7a02daec53e` | P2-WP04 implicit SQLite/local pipeline | `go test ./internal/audit ./internal/observability/pipeline ./internal/observability/router ./internal/config ./internal/telemetry -count=1`; focused post-review race; `go vet` for the same packages; Windows amd64 test compilation for audit/telemetry/pipeline | Final normal suite passed (`audit` 9.307s, pipeline 0.764s, router 1.724s, config 1.065s, telemetry 1.274s); final focused race passed (`audit` 64.667s, pipeline 2.967s); vet and all Windows compile gates passed | root + storage/graph/alert subagents |
| `V-0027` | 2026-07-03 | `39fe480d1266164aef2e0f2b5c70c7a02daec53e` | P2-WP04 repository/review gate | `make check`; `.venv/bin/python -m pytest cli/tests/test_observability_v8_inventory.py -q`; `coderabbit review --agent -t uncommitted`; `git diff --check` | Repository check passed: v7 parity, 23 baseline schemas plus separate v8 redaction schemas, 95 decisions, 14 dashboards/313 panels, Go/TS provider coverage, catalog, and upgrade manifest. Inventory tests 8/8 passed. CodeRabbit raised 16 issues: 12 valid issues fixed and reverified; 4 suggestions rejected because they contradicted locked unbounded receipt/ACK initialization decisions, requested unsafe error disclosure, or indexed an unqueried field. | root + Claude + CodeRabbit |
| `V-0028` | 2026-07-03 | `211b4d8c7` | P2-WP05 exact retention lifecycle | `go test ./internal/audit -count=1`; focused/full retention race; `go vet ./internal/audit`; repeated contention/failure tests | Global UTC cutoff, strict older-than semantics, 1,000-row transactions, child/parent age independence, protected-state exclusions/capacity, crash-resumable judge cleanup, exact timestamp repair, passive checkpoint, and active reader/writer contention passed. | root + storage subagent |
| `V-0029` | 2026-07-03 | `8d26ebd4f` | P2-WP05 runtime graph and production assembly | `go test ./internal/config ./internal/audit ./internal/observability/... -count=1`; `go vet` for the same packages; focused runtimegraph/runtime stress | Normal suites passed (`config` 1.347s, `audit` 13.697s, runtime 2.891s, runtimegraph 1.592s); vet and diff checks passed. Deterministic tests cover atomic swap, old/new lease coherence, candidate rollback, global cleanup ownership, 300 queued/reentrant report batches, exact SQLite persistence, synchronous retention activation, rejected-policy preservation, and retryable shutdown. | root + runtime/storage subagents + adversarial review |
| `V-0030` | 2026-07-03 | `8d26ebd4f` | P2-WP05 race/platform/spec gate | `go test -race ./internal/audit ./internal/observability/runtime ./internal/observability/runtimegraph -count=1`; Windows amd64 compile via `go test -exec=/usr/bin/true`; `make check-observability-v8-spec` | Race passed (`audit` 315.683s, runtime 33.133s, runtimegraph 1.539s); all three packages compiled for Windows amd64; 95 D-/S-/P- decisions validated. Multiple bounded Claude Opus xhigh attempts returned no usable output; independent adversarial reviews found and closed reporter-spool deadlock, opened-path identity, exclusive-controller ownership, and unbounded constructor-cleanup defects. | root + subagents |
| `V-0031` | 2026-07-03 | `1e8463ad6` | P2 representative-producer gate | Focused Sidecar v8 normal, race, and 10-run stress tests; `go test ./internal/gateway -count=1`; `go vet ./internal/gateway`; static dual-emission coverage; `make check-observability-v8-spec`; packaged dashboard audit and focused Agent360/Grafana pytest | Focused normal passed in 4.084s, race in 16.475s, stress in 7.819s, and full gateway in 53.369s. Exactly one bound canonical start/stop row preserves action, actor, details, INFO severity, event/bucket ownership, correlation IDs, graph digest, and generation. Failure, ambiguous outcome, canceled shutdown, disabled collection, and unbound compatibility cases passed. Dashboard audit remained 14 dashboards/313 panels and 33 focused dashboard tests passed. A bounded Claude Opus xhigh pass emitted no usable review output and was not treated as evidence. | root + runtime/security subagents |
| `V-0032` | 2026-07-03 | `4c63c7c52` | P3 v8 push-network guard | `go test ./internal/netguard -count=1`; `go test -race ./internal/netguard -count=1`; `go vet ./internal/netguard`; Windows amd64 and Linux arm64 compile-only tests; `git diff --check` | Additive destination-scoped private/loopback and CGNAT policy passed exhaustive IPv4/IPv6 classification, mixed-answer DNS, per-dial rebinding, redirect, cancellation, bounded-error, invalid-port, NAT64/translation, environment-invariance, and 800-call concurrency tests without changing legacy APIs. A Claude Opus xhigh review attempt returned empty output and was not treated as evidence; root added translation/port hardening during manual review. | root + network subagent |
| `V-0033` | 2026-07-03 | `d493f487c` | P3 bounded-delivery specification | `make check-observability-v8-spec`; `.venv/bin/python -m pytest cli/tests/test_observability_v8_spec.py -q`; `git diff --check` | Spec gate passed with 97 decisions and 7 focused tests. P-062 pins implicit count/byte queue and encoded-batch limits while keeping normal YAML concise; P-063 prevents Splunk aliases from recovering canonical, producer, raw, or another destination's data. Claude Opus xhigh produced a usable inventory review whose queue-generation and projected-byte boundaries were incorporated. | root + destination subagent + Claude |
| `V-0034` | 2026-07-03 | `d25cb6b8d` | P3 delivery configuration/compiler | Go config normal/race/vet; schema/CLI and 82 focused Python tests; shared Go/Python corpus; schema, generator, spec, and diff checks | Canonical schema, Go compiler, Python validator, CLI plan rendering, generated references, and shared corpus agree on implicit and explicit queue/batch limits. JSONL and console accept only queue controls, Prometheus rejects batch controls, push destinations accept the full grammar, and all invalid/overflow values fail closed without clamping. | root + config subagent |
| `V-0035` | 2026-07-03 | `c0a5339a0` | P3 graph-owned trace substrate | `go test ./internal/... -count=1`; telemetry/runtimegraph normal/race/vet; focused PR #403 gateway trace/lifecycle test; packaged PR #412 dashboard inventory and 33 focused dashboard tests | Inactive plan-bound providers remain generation-owned with no global mutation. Six samplers, parent coherence, collection-before-construction, targeted canary bypass, bounded attributes/events/links/errors, process-owned resource truth, and content-free backend failures passed. Existing Agent360 identifiers and 14-dashboard/313-panel inventory remained intact. | root + telemetry subagent |
| `V-0036` | 2026-07-03 | `d15c04520` | P3 isolated optional-delivery core | Delivery package normal/race/vet, focused 50-run stress, and all observability-package tests | Independent count/byte admission, drop-newest overload, encoded-batch ceilings, exact-byte retries, typed outcome classification, bounded backoff, seven health states, recursion-safe asynchronous health, and retryable activation/drain/close passed without producer-thread adapter I/O. A bounded Claude attempt emitted no usable output and was not treated as evidence. | root + delivery subagent |
| `V-0037` | 2026-07-03 | `c06c5b345` | P3 resource-attribute disclosure guard | Focused Go/Python shared-corpus tests, Go config normal/race, Ruff, and diff check | Both compilers reject path-bearing resource keys and absolute Unix, Windows, UNC, tilde, and file-URI values, preventing configured resource attributes from bypassing process-truth and path-disclosure protections. | root |
| `V-0038` | 2026-07-03 | `f9ebc2e43` | P3 local JSONL/console adapters | Local adapter normal/race/vet; Windows amd64 compile; full observability tests; post-review newline-injection regression | Generation-owned JSONL and console adapters passed exact FIFO projection delivery, single-line UTF-8 JSON enforcement, terminal-control escaping, owner-only/no-link file handling, no-clobber rotation, compression/pruning, replacement/reopen, bounded cleanup, and platform-specific tests without adapter goroutines. | root + local-adapter subagent |
| `V-0039` | 2026-07-03 | `3a1d14e16` | P3 HTTP JSONL/Splunk adapters | Netguard/delivery/push normal; push race/vet/repeated; Windows amd64 compile; post-review invalid-UTF-8 regressions | Both adapters use offline plus activation resolution and guarded per-dial rebinding protection, disable proxy and redirects, validate TLS/headers/secrets, close responses, classify pre-write transient versus post-write ambiguous outcomes, and emit bounded exact projection-only wire data. Splunk aliases cannot recover removed values or opaque producer HEC events. | root + push-adapter subagent |
| `V-0040` | 2026-07-03 | `d68870941` | P3 generation-owned log dispatch | Pipeline/runtime normal; focused runtime race; pipeline/runtime vet; Windows amd64 compile; queue-byte separation regression | Optional projections carry only bounded canonical identity, enqueue after successful SQLite persistence, and remain attached to their graph generation across reload/removal. Independent queues isolate overload, adapter, observer, and projection failures; rejected candidates and shutdown clean acquisitions in reverse order. Queue-only encoded writes no longer misuse the projected-byte ceiling. | root + runtime subagent |
| `V-0041` | 2026-07-03 | `f5704777c` | P3 ambiguous-delivery contract | `make check-observability-v8-spec`; focused spec pytest; diff check | The contract now consistently permits bounded exact-byte/exact-ID retry for transient and ambiguous acknowledgement loss while keeping authentication, permanent payload, and unsafe endpoint outcomes terminal. Spec lint passed with 98 traced decisions and 7 focused tests. | root |
| `V-0042` | 2026-07-03 | `706043bba` | P3 composite log-adapter assembly | Destination-factory normal/race/vet; Windows amd64 compile; full observability tests | One process-stable factory now prepares distinct generation-owned JSONL, console, HTTP JSONL, and Splunk adapters from detached compiled destinations, resolves bounded secrets/CA material once per candidate, returns retryable cleanup, and rejects unsupported or malformed destinations with content-free errors. | root + adapter subagents |
| `V-0043` | 2026-07-03 | `705c62a18` | P3 OTLP path/protocol parity | Go config normal/race/vet; 59 focused Python tests; Ruff; shared corpus and spec checks | Go and Python now reject signal-path overrides for gRPC OTLP while retaining per-signal endpoints; HTTP/protobuf remains the only protocol with configurable paths. The shared corpus prevents migration or CLI validation from accepting a graph the runtime cannot implement. | root |
| `V-0044` | 2026-07-03 | `a3c76dc3b` | P3 real multi-destination boundary | Integration normal x10, race, vet, Windows amd64 compile; packaged dashboard audit; 33 focused Agent360/Grafana tests | One canonical `security.finding` commits exactly once to SQLite before independent JSONL, console, HTTP JSONL, Splunk, and failing-slow delivery. Destination-specific unredacted/content/sensitive/strict/legacy projections preserve identity and correlation, Splunk aliases remain projection-only, optional failures cannot delay the producer, rejected reloads stay content-free, and destination removal drains cleanly. PR #412 remained at 14 dashboards/313 panels. | root + destination/runtime subagents |
| `V-0045` | 2026-07-03 | `62b19fd0f..2e35ff2e3` | P3 Claude destination hardening | Local and push normal x10/race/vet; config parity corpus; focused Python/Ruff; spec lint | Claude's HTTP-path mismatch claim was disproved by the existing schema and locked with a shared Go/Python absolute-path fixture. Its valid JSONL finding was fixed by rolling incomplete appends back before retry and failing the generation closed if rollback fails. Explicit plaintext HTTP remains compatible, but resolved authentication now emits exactly one content-free `plaintext_credentials` warning/audit under P-066. | root + Claude Opus 4.8 |
| `V-0046` | 2026-07-03 | `6d4a20f64` | P3 canonical metric substrate | Telemetry normal/race/vet/Windows; runtimegraph lifecycle; exact 131 schema; 2,048-series overflow; dashboard audit and 33 focused tests | Bucket collection precedes SDK instrument construction; every meter entrypoint is explicit, requested-bucket/catalog bound, and label/slice/cardinality bounded. Independent readers preserve 60-second delta policy without globals, candidate cleanup is bounded, and v8 shutdown retries observe the same terminal cleanup. PR #412 remained 14 dashboards/313 panels. | root + telemetry subagent + Claude Opus 4.8 |
| `V-0047` | 2026-07-03 | `9269b80c5` | P3 Galileo rich projection | Galileo normal x10/race/vet/Windows; existing filter/canary/fan-out regressions; diff check | Only an immutable route-redaction projection can enter `galileo-rich-v2`. Agent, LLM, tool, retriever, workflow, judge-chat, and diagnostic-canary shapes preserve bounded PR #403 lineage, truthful missing/content states, safe events/links/status/timing, and deterministic bytes; schema misses are destination-local closed reasons. | root + Galileo subagent |
| `V-0048` | 2026-07-03 | `1a0dd1f48` | P3 guarded OTLP transports | OTLP normal x10/race/vet/Windows; full observability packages; exact retry/CA/partial/rebinding boundary tests | HTTP/protobuf and gRPC logs/traces/metrics use detached headers/CA bytes, per-signal endpoints, fixed gRPC paths, activation and per-dial guards, TLS, count/byte bounds, charged trace queues, pre-allocation metric bounds, bounded exact-request retry, terminal unsafe/auth/permanent outcomes, and content-free counters. Empty protobuf acknowledgements and partial success are handled without retrying accepted subsets. | root + OTel subagent |
| `V-0049` | 2026-07-03 | `d068e0bee` | P3 exact partial delivery | Delivery normal x10; malformed disposition and exact-split tests; diff check | The common dispatcher now accepts one terminal acknowledged partial outcome only when positive delivered/rejected counts exactly partition the immutable batch. It never retries the accepted subset, reports record-exact counters and degraded health, and rejects malformed adapter counts closed without over-reporting delivery. | root |
| `V-0050` | 2026-07-03 | `7fdf074f0` | P3 plan-aware signal ownership | Telemetry normal x10/race/vet/Windows; runtimegraph lifecycle and cleanup tests | One production generation callback receives the exact immutable plan, generation, and metric policy and returns independent span processors/readers. Candidate failures clean every returned child, disabled trace/metric collection skips factory construction, no global provider is mutated, and existing lower-level factories remain test-only seams. | root |
| `V-0051` | 2026-07-03 | `69ccc62db` | P3 OTLP log runtime fan-out | Destination/OTLP/integration normal and race; vet; Windows; full observability packages | The composite factory now builds HTTP/protobuf and gRPC OTLP log adapters from detached compiled state with guarded network/TLS, one candidate-local secret/CA resolution, typed warnings, cleanup, logger metadata, exact projected identity, and SQLite-first runtime fan-out. Trace-only destinations allocate no log resources. Partial responses report exact accepted/rejected records; negative counts fail terminally and over-counts clamp to all rejected without replay. | root + OTel subagent |
| `V-0052` | 2026-07-03 | `d052c4113` | P3 local-observability-v1 compatibility | Ruff; 10 new plus 33 existing focused tests; 39 bundle/config tests; dashboard audit; Compose/Collector/Promtool validation | The checked P3 bridge freezes 441 dashboard/variable/rule expressions, 14 UIDs/313 panels, three datasource UIDs, current metric/label/Loki/Tempo dependencies, histogram policy, exact Collector 0.153.0 three-signal pipelines, Agent360 dimensions/buckets, persistent volumes, loopback binds, and complete source/package byte parity. Five-service readiness and one shared deadline prevent live validation hangs; the running stack compiled live queries, while slow 48-hour Loki inventory remains final P3-gate evidence. | root + local-observability subagent |
| `V-0053` | 2026-07-03 | `ef86cd586` | P3 unified runtime generation | Runtime repeated/race/vet/Windows and reload binding tests | The plan-bound OTel provider now activates and retires inside the exact runtime graph that owns retention, SQLite, and optional dispatch. Reload replaces the provider with the next digest/generation, retires the old provider, and cannot expose cross-generation processors or readers to producers. | root |
| `V-0054` | 2026-07-03 | `5545c20bf` | P3 signal-factory containment | Telemetry focused normal/race/vet/Windows; panic factories and cleanup regressions | Panics from generation-pipeline, static-span, and static-metric factories are contained as bounded candidate errors. A malformed extension cannot crash reload, disclose panic text, or leave partially prepared processors/readers alive. | root |
| `V-0055` | 2026-07-03 | `1fde55787` | P3 exact canary query bridge | Telemetry/provider normal/race/vet; generation retirement and panic-safe callback tests | The active provider exposes a generation-owned destination/trace acknowledgement query only when trace collection and processors exist. It returns false after retirement or shutdown and contains callback panics without fabricating delivery. | root |
| `V-0056` | 2026-07-03 | `b6c4ea312` | P3 native Prometheus destination | Prometheus/telemetry normal and broad observability tests; focused race/vet; Windows amd64 compile; schema/dashboard checks; 56 local-observability/Galileo/Agent360 Python tests | Each enabled Prometheus destination owns a private official OTel reader, registry, loopback listener, exact path, cumulative pull view, first-match bucket/event routing, and bounded HTTP lifecycle. Unknown metric families/labels fail closed, the 131-metric compatibility catalog remains authoritative, and stuck scrapes or observers cannot own graph shutdown. | root + telemetry subagent |
| `V-0057` | 2026-07-03 | `66475992a` | P3 inbound OTLP normalization seam | Full gateway normal; focused race; gateway vet; Windows amd64 compile; strict JSON/protobuf, floor, loop, mixed-batch, and v7 compatibility tests | A package-private v8 receiver seam strictly normalizes all three OTLP signals, rejects unknown nested fields, emits content-free accepted/rejected/authentication metadata through collection and mandatory-floor admission, and drops only all-item exact self-exports. It is deliberately not production-bound until P4/P5 provide per-record builders plus v8 token/duration derivation; unbound v7 raw/HEC and PR #412 metrics remain exact. | root + inbound subagent |
| `V-0058` | 2026-07-03 | `ec98ed742` | P3 generation-owned OTLP traces and metrics | OTLP/destination/telemetry normal and race; vet; Windows amd64 compile; multi-destination, routing, reload, rollback, and canary tests | General OTLP trace processors and metric readers resolve the unmasked runtime transport once per candidate, apply ordered bucket routes before trace queueing and exact metric family/event filtering before export, isolate generations, and roll partial candidates back. Galileo, transformed traces, and unsupported advanced selectors fail closed pending generated projections. A canary is acknowledged only after one successful exact two-span `invoke_agent`/`chat` export for its target destination and cannot outlive its generation. | root + OTel subagent |
| `V-0059` | 2026-07-03 | `7ba051348` | P3 composite signal-provider integration | Destination/telemetry focused x10 and package normal; focused race; vet; Windows amd64 compile; full runtimegraph fan-out/reload/rollback test | The single process-stable generation callback now composes general OTLP processors/readers with private native Prometheus readers from the exact plan and generation. A discovered SDK wiring defect was fixed so prepared metric readers are actually registered rather than merely retained for cleanup. One runtime test proves capability-default OTLP logs/traces/metrics, Prometheus metrics-only, independent delivery of the same metric to both transports, exact targeted canary acknowledgement, reload listener/reader/processor retirement, candidate rollback, and unchanged OTel globals. | root + signal-provider subagent |
| `V-0060` | 2026-07-03 | `ab48aae80` | P3 Galileo projected-trace delivery | Galileo/OTLP/compatibility focused normal and x10; focused race/vet; Windows amd64 compile; full observability packages; 293 Galileo/v8/local Python tests; 14-dashboard/313-panel audit | A guarded HTTP/protobuf adapter now accepts only immutable route-redacted `galileo-rich-v2` projections with exact current envelope/catalog versions, canonical ended-span identity, complete resource/scope/timing, and the pinned trace/semantic/Galileo profile tuple. Mixed/raw or incomplete inputs fail before network; rich root/subagent/lifecycle/tool/events/links/status fields survive; partial responses are terminal and exact; only a fully accepted two-span targeted canary is acknowledged. Production activation remains closed until P5 generated builders supply those projections. | root + Galileo subagent |
| `V-0061` | 2026-07-03 | `73a54b548` | P5 registry source boundaries | `make check-observability-v8-spec`; 7 focused spec tests; Ruff; diff check | The contract now defines offline SHA-256-verified upstream convention snapshots, separates 14 gateway/188 audit producer mappings from canonical families, generates local-observability ownership from parsed bundle assets, and preserves old public schema `$id`/`$ref` plus copy-safe embed compatibility through one generated cutover. Existing P-030/P-040/P-046/P-056/P-061 traceability covers the clarified behavior without a new decision. | root + registry subagent |
| `V-0062` | 2026-07-03 | `3197fdf74` | P3 PR #403 first-phase fidelity | Gateway focused normal x10 and parent rerun; focused race; gateway vet; gatewaylog/observability packages; corrected 18-event live replay | First root/subagent observations no longer fabricate `agent_previous_phase: unknown`, which the strict canonical schema correctly rejected. Logs/traces omit the unreported value while only the bounded metric label projects `unknown`. The replay produced no schema violations, exact paired tool IDs, 61/61 OTLP deliveries, three-agent root/parent/lifecycle/execution lineage, a Loki first-lifecycle record, and a two-span Tempo root-to-tool waterfall. | root + local-observability subagent |
| `V-0063` | 2026-07-03 | `ecf70595c` | P3 live local-observability gate | 21 focused checker tests; Ruff; 14-dashboard/313-panel static audit; five-service live 48-hour packaged inventory under 900s global/60s query budgets; root in-app Browser DOM/screenshot/interaction QA | Per-query timeouts are configurable and capped by the shared deadline, so valid heavy queries complete without hiding genuine hangs. The canonical inventory returned data=250, zero=47, expected-idle empty=11, interactive=2, static=3, errors=0, unexpected-empty=0. Agent360 rendered the live root plus direct/nested subagents, 2 turns, 4 model calls, 2 tool calls, 2 descendants, truthful unreported tokens/cost, and 100% terminal success; Activity navigation worked. Span throughput/p95 `No data` after inactivity was proven rate-window idleness: 33 count/495 bucket series existed and fresh activity made exact PromQL nonzero with finite p95. | root + local-observability subagent |
| `V-0064` | 2026-07-03 | `d95c2f23f` | P5 canonical telemetry source model | Generator write/check; exact source counts; upstream digest and ownership checks; schema gate; diff check | The canonical source now contains the exact pinned OTel core, dedicated GenAI, and OpenInference 0.1.30 normalized inputs plus 277 DefenseClaw attributes, 45 upstream privacy overlays, 322 structured normalizers, 25 bounded span families, 131 metric families, 112 local compatibility projections, and 11 exact field-class examples. PR #403 lifecycle identities and PR #412 labels remain explicit compatibility contracts rather than inferred aliases. | root + domain/registry subagents + Claude Opus xhigh |
| `V-0065` | 2026-07-03 | `d1ed5278d` | P5 sole compiler and provenance gate | 77 focused pytest cases; Ruff; Python compile; generator write/check; current inventory checker; `make check-schemas`; `go test ./schemas -count=1`; diff check; two Claude Opus xhigh reviews | The offline compiler now rejects provenance, ownership, overlap, type/unit, normalizer, portable constraint, array-bound, span-name, field-class, metric-label projection, and generated-output drift. A one-release fail-closed analyzer proves all 131 current metric label/callsite sets and ten legacy-gate drops; malformed helpers, dynamic keys, timeouts, upstream YAML, deleted outputs, and rollback paths have negative coverage. Both Claude passes ended with no unresolved P0-P3 finding. | root + registry/compiler subagent + Claude Opus xhigh |
| `V-0066` | 2026-07-03 | `852b19195` | P5 lossless compiler IR | 80 focused compiler tests; Ruff; Python compile; generator drift; exhaustive source-to-IR audit; Claude Opus xhigh review | Every validated attribute, direct use role/constraint, group/span/log/metric extension, producer identity/mapping, semantic/metric profile, example, snapshot/source-file provenance, canonical upstream owner, and absent-versus-null legacy binding survives in immutable equality-only IR. Inheritance and output authority remain deliberately unactivated. | root + registry/IR-audit subagents + Claude Opus xhigh |
| `V-0067` | 2026-07-03 | `f9de2f438` | P5 deterministic family contracts | 116 focused compiler tests; Ruff; Python compile; generator write/check; inventory; full schema/spec gates; aggregate Claude Opus xhigh review | One DAG resolver materializes all 298 groups and 243 signal families with body-role transposition, exact log parent ownership, origin provenance, non-weakening requiredness, conditional coupling, restrictive constraint intersection, and zero ambiguity. Exact locked contracts cover 77 required/10 forbidden logs, 25 required spans, and 131 metrics without envelope outcomes; all Claude findings were fixed. | root + registry/outcome/IR-audit subagents + Claude Opus xhigh |
| `V-0068` | 2026-07-03 | `e309dffc3` | P5 public-schema baseline tooling | 70 focused tests; actual twenty-one-schema in-memory probe; Ruff; Python compile; diff check; Claude Opus xhigh security review | The inert tool reads only full pinned Git objects through bounded hardened commands, preserves exact JSON numeric tokens, rejects malformed/deep/unsupported schema semantics, proves mixed-dialect offline closure and commit/tree/blob/source/document provenance, prevents per-file/manifest generated-authority recursion, and writes atomically with directory durability. No real baseline or runtime/compiler authority landed in this commit. | root + public-view subagent + Claude Opus xhigh |
| `V-0069` | 2026-07-03 | `b9b4fd335` | P5 immutable public-schema baseline | Baseline bootstrap from `e309dffc369d8f0c722d74ace848cec74ff40e3c`; baseline check; exact resource/dialect/number inventory; diff check | The inert `public-schemas-v7` epoch pins all 21 authoritative schemas, two exact dialects, 21 IDs, 162 numeric token lexemes, and complete local/three-resource closure with domain-separated digests. It remains pre-cutover and changes no public path, mirror, embed, wheel, compiler, or runtime authority. | root |
| `V-0070` | 2026-07-04 | `df0d16382` | P5 typed canonical structure | 177 generator tests; 8 spec tests; 27 final adversarial tests; generator write/check; Ruff/format/Python compile; full config/observability/schema normal plus focused race/vet; full `make check`; 14-dashboard/313-panel audit; independent adversarial re-audits and Claude Opus xhigh review | P-069 now closes the authored envelope/signal/trace/metric/condition/lifecycle/privacy/OTLP structure, recursively validates exact types and Go-compatible byte bounds, proves exact leaf classes and singleton invalid mutations, and filters removed catalog state. Source-derived semantic profiles must match shared compiled runtime capabilities; generated log construction accepts only an exact private family contract carrying catalog-derived mandatory state. PR #403 workflow/phase fidelity and PR #412 dashboards remain intact. Candidate rendering and consumer activation remain explicitly open. | root + compiler/public-view/structural subagents + Claude Opus xhigh |
| `V-0071` | 2026-07-04 | `99f8d9949` | P5 materialized generation foundations | 197 compiler tests; 105 transaction tests on Python 3.10-3.14; 16 focused derivation/type tests; generator write/check; Ruff/format/Python compile; Go family-builder normal/race/vet; 8 spec tests and 103 decisions; schema gates; 14-dashboard/313-panel audit; repeated independent race/portability audits and Claude Opus 4.8 xhigh reviews | Every renderer now has one recursively immutable typed materialized registry view with a domain-separated digest and exact ordered-versus-set semantics. All active spans prove six derivation bindings; family schema versions are positive uint32 through source, builder, and OTLP integer projection. The private builder kernel validates closed log/trace/metric descriptors before occurrence allocation. The standalone manifest-last transaction restricts ownership to exact generated paths and provides durable crash recovery, no-follow descriptor-relative I/O, exact journal and cleanup identity, linked-worktree/macOS alias support, and Python 3.10-3.14 cleanup portability. Candidate artifacts are not yet rendered or authoritative. | root + generation/public-view/structural subagents + Claude Opus 4.8 xhigh |
| `V-0072` | 2026-07-04 | `bab42f800957c8afa8228925e59dd3768e6e9e4a` | P5 generated-output transaction | Full telemetry generator and transaction pytest suites; root adversarial transaction/generator rerun | 322 combined tests passed; 17 root-targeted provenance, durability, recovery, and path-identity cases also passed after hardening. | root + generation/public-view subagents |
| `V-0073` | 2026-07-04 | `efa5b6e0e080f54d70333d93affc7c62aef84dcd` | P5 P-070 source contract | `make check-observability-v8-spec`; `.venv/bin/python -m pytest -q cli/tests/test_observability_v8_spec.py` | The specification gate validated 104 traced decisions; all 9 focused specification tests passed for closed structured identity/bounds and open GenAI values. | root + generation/public-view subagents |
| `V-0074` | 2026-07-04 | `cbd652a84e490514fd624334593205505600f40e` | P5 portable candidate renderer | `.venv/bin/python -m pytest -q cli/tests/test_telemetry_registry_candidate_renderer.py` | 37 candidate-renderer tests passed, including deterministic immutable rendering, complete value/event arms, full-set path preflight, mandatory catalog consumption, and explicit/exact-base example context publication. | root + public-view subagent |
| `V-0075` | 2026-07-04 | `cbd652a84e490514fd624334593205505600f40e` | P5 mandatory rules and builder facts | Full generator and candidate-renderer suites; focused mandatory/builder matrix; invalid-example mutation matrix; real generator `--write`/`--check`; Ruff and diff checks | 233 generator and 37 candidate tests passed; focused mandatory/builder coverage passed 18 cases and the final mutation-root matrix passed 8; real `--write` and `--check`, Ruff, and `git diff --check` passed. | root + generation/public-view subagent |
| `V-0076` | 2026-07-04 | `f99a400921dced393225e12b063a595bd0bb5e14` | P5 pinned structured GenAI compiler and refresh transaction | Full generator and candidate-renderer suites; updater-focused concurrency/CAS/rollback suite; spec decision/test gates; real generator `--write`/`--check`; Ruff; Python compile; diff check; independent state-machine and trust-boundary audits | 313 generator, 73 candidate-renderer, 45 updater-focused, and 9 spec tests passed; the spec gate validated all 104 decisions. The compiler preserves open provider values, exact privacy/normalization/null/encoding metadata, and four digest-pinned structural inputs while rejecting semantic loss and re-digested P-069/P-070 tampering. Concurrent disjoint refreshes retain both updates; stale locks, selected/unselected drift, inode or same-inode tampering, oversized references, and pre-/post-commit failures fail with coherent rollback or explicit committed state. | root + structural/updater/audit subagents |
| `V-0077` | 2026-07-04 | `1a023d4f18ae7d31efd2b7d7988280dfa964cd13`, `6eb29d997ef38e397522033ab172269cb6662145` | P5 candidate parity and Go declaration-form contract | 352 compiler tests; 178 candidate-renderer tests; real 29-artifact render; generator `--write`/`--check`; 9 spec tests and 104-decision gate; Ruff check/format; Python compile; diff check; independent adversarial audits; two Claude Opus 4.8 xhigh reviews | Candidate rendering now binds the exact v1 normalizer catalog, rejects forged effective maps and weakened use constraints, recomputes the complete direct/inherited origin DAG and resolution order, and preserves typed-enum, portable full-match, recursive item/property/depth, shape-aware UTF-8, scalar/polymorphic item, and portable-path semantics. The Go symbol contract emits each row once: 21 structured types and 17 arms are typed declarations, 49 members remain constants, and the reviewed 1,773-row count/digest is unchanged. | root + constraint/path/audit subagents + Claude Opus 4.8 xhigh |
| `V-0078` | 2026-07-04 | `bcb6a0faa2a72ad32b1b8c4aa4d2a58136c7cfa0`, `dc9c8340bcdf1fd6f32628c658bf0f0fac9e7df3`, `aa36ebe45f8a7350974cebd17e84e9adb6731fa1` | P5 deterministic Go symbol compiler and candidate boundary | 386 generator tests; 198 candidate-renderer tests; 34 focused compiler symbol cases; 14 focused candidate forgery cases; real 29-artifact render; exact golden and all-22-kind source audits; generator write/check; Ruff/format/Python compile/diff; two Claude Opus 4.8 xhigh reviews | The closed v1 policy compiles 1,773 unique declarations in 22 namespaces with exact 893/459/178/243 forms, d897 domain-separated table digest, and ee63 content-addressed acceptance golden. Candidate rendering consumes the immutable table without retokenizing or reading golden bytes, reconciles every source identity against materialized facts, rejects exact-type/digest/order/form/source forgeries, and preserves the existing artifact set. | root + compiler/candidate/audit subagents + Claude Opus 4.8 xhigh |
| `V-0079` | 2026-07-04 | `d9e301ef4caa2712fb103024d86ecd8174c4d823`, `4ecce4044055e3a2c711ff211989c72232b23871` | P5 exact-seven transaction and generated API authority | 110 transaction fault-injection tests; Ruff/diff; 104-decision spec gate; 9 focused spec tests; three independent repository audits; Claude Opus/Sonnet review with both findings resolved | Desired and prior internal generated outputs are accepted only as the complete seven-file inventory or none. The normative compiler boundary now owns all non-input trace values, selected-family mandatory programs, enriched descriptors, scoped Go selectors/types/signatures/conversions, structured sealed layouts, file assignment, and a candidate digest that explicitly hashes the materialized-view digest. Stale blocker prose was replaced with the executable remaining work. | root + constraint/API/traceability subagents + Claude review |
| `V-0080` | 2026-07-04 | `734ebde26385ab142bc1a5f7c90ff5565b096ab8`, `d11ea59fb8a9693e4bbbeb275ccf586637f2846e`, `60614f211c175e5dff34dd8d6bc35177e0c0865b`, `c57e2c955a4c7c63dae8e0e65b2c4b38a720cd1c` | P5 enriched candidate and Go semantic API plan | 24 coordinator tests; 18 fail-closed renderer tests; 205 candidate-renderer tests plus the focused structured-presence correction; 12 synthetic/real-view Go-plan tests; Ruff/format/Python compile/diff checks | The candidate boundary now materializes 2,728 scalar descriptors, 102 structural containers, 243 families, 25 traces, 131 metrics, 87 selected-family floor programs, 8,038 producer identities, and 893 exact constant values. The independent semantic Go plan covers all 1,773 declarations with the exact 893/282/212/386 partition, 421 typed inputs/callables, structured and kernel contracts, and catalog-only descriptor ownership. Seven-file preflight and rendering reject partial or syntax-incomplete candidates. Callable bodies, producer projections, executable fixture ASTs, and cryptographic plan embedding remain explicit blockers. A bounded Claude Opus xhigh attempt produced no output and is not counted as evidence. | root + enrichment/API/renderer subagents |
| `V-0081` | 2026-07-05 | `2b9819ed4d3f12bc5de7b38b1f7518f596a92d80`, `23d18e819bf609fe5dd43b09aa219d6d2fbcafe3`, `771c552a8414c70e5768b53e81a180e90f2dc614`, `70a21ecb5a88af2705249d8f446a5fe5bbb929c2`, `4200ad0431a1193792a1c3ec1abcecc5e70a725f` | P5 complete in-memory generated Go candidate | 86 canonical/API/fixture/renderer tests; 42 focused registry/candidate tests; full observability Go test/vet; 15-test exact-seven renderer suite with gofmt and real Go overlay execution; generator check; Ruff/format/Python compile/diff; independent span and kernel adversarial reviews | The frozen candidate now owns syntax-complete callable bodies, 8,038 producer projections, 426 generated fixture tests, all 19 required span-name substitutions, 36 family-local phase/code relations, derived condition closure, metric-private outcome policy, and one shared canonical payload-number encoder. All seven outputs render deterministically with the exact 893/0/0/282/212/386/0 declaration partition, 741 private declarations, 243 builders, 25 trace contracts, and 131 metric contracts. Type-owned API/candidate digest verification and bounded relation rendering reject stale or forged plans before any output object exists. Publication and authority cutover remain intentionally separate. | root + body/renderer/span/review subagents; Claude Opus xhigh review pending |
| `V-0082` | 2026-07-05 | `a8361ad02`, `753580e8b`, `c86068d03`, `4fdadf297` | P5 generated publication and shared public-baseline authority | 447 partitioned generator cases; 212 candidate-renderer cases; 33 fixture/render cases; 120 transaction/spec cases; full schema gate; generated Go normal/race/vet; Ruff/format/Python compile/gofmt/diff; 80 public-view baseline cases; Claude Opus xhigh exact-commit review | The generator now publishes all seven Go files and thirty portable artifacts through one manifest-last transaction, enforces the exact generated API surface, and preserves canonical JSON parity. A strict shared immutable baseline reader replaces duplicated public-schema parsing authority. Claude found no reachable correctness or security defect in the publication commit; stale prior-owned retirement, repeat public adoption, and public-view generation remained explicit follow-on work. | root + generation/public-view/renderer subagents + Claude Opus xhigh |
| `V-0083` | 2026-07-05 | `4fadc4a6c`, `8630abb16` | P5 public-schema adoption transaction | 145 transaction tests; Ruff/format/Python compile/diff; independent adversarial transaction reviews | The manifest-last writer can adopt exactly the 21 public schemas plus four gateway mirrors and one CLI embed as one authenticated 26-path set. First use requires exact predecessor bytes, SHA-256, mode, absent marker, and regular-file identity; repeat publication uses typed zero-payload `ContinuedAdoptionAuthority` only when all 26 paths are already manifest-owned. Mixed modes, partial/disappearing authority, all fault stages, rollback, committed completion, and strict v1/v2 prepared/committed recovery fail closed. | root + public-view/transaction subagents |
| `V-0084` | 2026-07-05 | `d651c03a4`, `793473271` | P5 typed public-view compiler and candidate trust boundary | 498 partitioned generator tests; 213 candidate-renderer tests; 80 public-baseline tests; real generator check; full schema/reference/mirror gate; generated Go normal/race/vet/gofmt; Ruff/format/Python compile/diff; independent adversarial review; Claude Opus xhigh exact-commit review and fix rerun | One immutable `public_views` source now covers all 21 public schemas, 756 properties, 94 dynamic scopes, 39 references, 40 definitions, 131 discriminators, 16 explicit additional-property overrides, 174 canonical sources, 582 transport-only fields, and the exact five mirror/embed targets. Compiler and renderer independently bind the complete IR to a pinned domain-separated authority digest; package/direct loader identities fail closed. Claude found no high/medium issue; its three low/nit findings were all fixed and reverified. The 37 generated outputs changed only authority digests, and no public schema, mirror, embed, wheel, manifest ownership, or runtime authority cut over. | root + public-view/renderer subagents + Claude Opus xhigh |
| `V-0085` | 2026-07-05 | `76669620f`, `e25666bea`, `385949ebe`, `8d064149a` | P5 generated-schema distribution, staging, and executable parity | Go schema copy-safety normal/race/vet; Python wheel archive and clean-install resource checks; 506 generator and 232 candidate-renderer tests at staging; 80 public-baseline tests; 10 focused legacy-parity tests; full schema gate; Claude Opus xhigh staged-commit review | The generated bundle and catalog are available through copy-safe Go and Python APIs, and only their exact two resources ship in the wheel. All 21 primary plus five mirror/embed public views were rendered marker-only under a temporary generated staging root while live authority remained unchanged. The parity corpus then proved exact 21/26/756/94/39/162 view/output/field/dynamic/reference/numeric coverage with 1,983 valid, 1,323 invalid, 176 omission, and 11 closed-object witnesses before cutover. | root + generation/public-view/renderer subagents + Claude Opus xhigh |
| `V-0086` | 2026-07-05 | `f980f102b` | P5 atomic generated public-schema cutover | 519 generator tests in three exact shards; 235 candidate-renderer tests; 145 transaction tests; 12 direct-live parity tests; real generator check; `make check-schemas`; `make check-v7`; Go schema/observability normal/race/vet/gofmt; Python 3.12 wheel archive and clean-install byte parity; exact 63-output/62-record/26-live/zero-staged audit; Claude Opus xhigh pre-cutover review and focused fix rerun | Generator v3 atomically adopted the exact 21 public schemas, four gateway mirrors, and one CLI embed at their live paths while retiring all 26 staged files and committing the manifest last. First use authenticated every unmarked predecessor against the pinned baseline; repeat use is payload-free and manifest-owned; partial/tampered/link/mode/mirror states fail closed; rollback and interrupted recovery preserve predecessor bytes. The manifest schema retains digest-addressed prior versions and admits only the generated root, exact seven Go outputs, and exact 26 live paths. Claude found no blocking logic defect; its recovery-diagnostic finding was fixed with a read-only transaction-state preflight before adoption inspection. | root + generation/public-view/renderer subagents + Claude Opus xhigh |
| `V-0087` | 2026-07-05 | post-`c0a39006526b` P5-WP03 worktree checkpoint | P5 generation-owned ended-span handoff substrate | `go test ./internal/... -count=1`; scoped telemetry/destination normal and `-race`; scoped `go vet`; runtime generation-retirement normal and `-race`; concurrent-end/resource-derivation focused normal x100 and race x10; `git diff --check`; 57 focused Agent360/setup/Grafana/local-observability Python tests; Claude Opus 4.8 xhigh read-only review | The complete internal Go suite plus scoped normal, race, vet, runtime-retirement, stress, and diff checks passed. The substrate proved clone-only immutable handoff; exact canonical/legacy per-destination XOR and fanout; sampled/recording/collection/plan/generation/identity/parity rejection; 2,048-record/64-MiB exact-byte ownership; duplicate/concurrent-end/retirement/panic cleanup; in-flight shutdown ordering; and terminal OTLP worker/exporter cleanup after timeout or panic. Claude's service-version concern was disproved by the generated `familyValueBinaryVersion` derivation and an explicit handoff assertion; its valid low-severity concurrent external-End race was fixed by accepting the honest unconsumed terminal result while retaining the zero-leak invariant. The 57 PR #412 compatibility tests passed, and provider resources retain `service.namespace=defenseclaw` plus canonical and legacy deployment/environment/device aliases. Canonical consumers remain unactivated pending record-authoritative trace state/full flags, typed safe custom resources, the generated two-span canary, runtime-graph lease E2E, PR #403/Galileo/PR #412 migration, and Agent360 spanmetrics canary exclusion. | root + handoff implementation/review subagents + Claude Opus xhigh |
| `V-0088` | 2026-07-05 | post-`5132fff38` P5-WP03 local-observability checkpoint | PR #412 canary isolation | Real `otelcol-contrib` 0.153.0 `validate` against source and packaged collector configs; 59 focused Agent360/local/setup/Grafana tests; source/package byte parity; Ruff; spec and diff checks | A dedicated `forward/agent360` trace branch now applies the exact OTTL Boolean marker filter only before `spanmetrics/agent360`. Marked synthetic canaries remain queryable in Tempo/debug; absent, false, and string-valued markers continue to Agent360 metrics. The source and packaged stack remain byte-identical, and the 14-dashboard/313-panel inventory is unchanged. | root + local-observability subagent |
| `V-0089` | 2026-07-05 | post-`662eaabd6` P5-WP03 resource-config checkpoint | Typed safe resource compilation and automatic migration | Go config normal/race/vet; focused provider resource normal/race; 70 Python v8-config tests; 219 migration tests; Ruff; schema/reference/spec/diff checks | The v8 source keeps one resource-attribute map but compiles configurable registered core separately from a bytewise-sorted custom-only projection bounded at 64 entries, 128-byte ASCII keys, 1,024-byte values, and 16 KiB aggregate data. Runtime options retain trusted precedence except explicit canonical environment configuration. Upgrade preserves effective service name, canonicalizes equal environment aliases, consumes preset markers with content-free summary codes, preserves tenant/workspace and safe custom entries, and rejects unsupported reserved keys without exposing values. | root + resource-config/migration subagents |
| `V-0090` | 2026-07-05 | post-`2fdd8d7a0` P5-WP03 canonical trace-context checkpoint | Record-authoritative W3C trace state, complete OTLP flags, and generated canary contract | Generator write/check; 534 generator and 235 candidate-renderer tests; full `./internal/...` Go suite; observability and telemetry normal/race/vet; handoff stress x20; 9 spec tests and 104-decision gate; Ruff/diff; Claude Opus 4.8 xhigh read-only review and focused fixes | Every generated span builder now accepts optional canonical W3C tracestate and a required complete OTLP `uint32` flags word with direct projection. The handoff consumes only record values, compares exact SDK parity, rejects reserved runtime flag bits, and has direct adversarial parser coverage. A generated guarded-optional canary contract applies only to agent-invoke/model-chat spans and forbids canary-only data unless the marker is explicitly true. The additive ABI contains 1,781 declarations with the exact 901/282/212/386 partition, 331 attributes, nine conditions, and seven independent caller facts. Claude found no high/medium defect; both low hardening gaps were fixed and the full internal Go suite passed. | root + trace-contract subagent + Claude Opus 4.8 xhigh |
| `V-0091` | 2026-07-05 | post-`53a525337` P5-WP03 local-resource checkpoint | PR #412 canonical-to-legacy environment alias preservation | Real Collector 0.153.0 validation for source and packaged configs; 29 focused local-observability/Agent360 tests; Ruff; source/package byte parity; 14-dashboard/313-panel audit; 441-query compatibility inventory; diff check | The Collector now preserves an explicit legacy environment, otherwise derives it from `deployment.environment.name`, and uses `local-dev` only when neither spelling exists. A canonical production resource can no longer acquire a contradictory legacy `local-dev` alias. Custom resource keys survive enrichment but remain absent from Agent360 dimensions and dashboard-required Tempo/Prometheus labels; exact Boolean canary behavior is unchanged. | root + local-observability subagent |
| `V-0092` | 2026-07-05 | `e39a7f84485691a506316947c220d0284b293170` | P3-WP02/P3-WP04/P5-WP03 exact resource epoch | Generator write/check; seven-suite regression reached 1,000 passes before exposing one stale ownership assertion, then the corrected assertion plus canonical planner/render checks passed 3/3; broad observability/config/telemetry normal and selected race suites; scoped vet; schema/reference/spec/diff gates; 14-dashboard/313-panel audit and 35 focused dashboard tests | The 1,785-declaration additive ABI seals bytewise-sorted custom resources, validates complete fixed/alias/custom maps with content-free errors, and preserves every prior baseline. One process-stable snapshot now supplies generated canonical spans, SDK traces/metrics, OTLP logs/traces/metrics, and Galileo with exact-set parity, explicit zero local resource-drop state, true/false aliases, platform-canonical host identity, immutable reload retention, and forged credential/path rejection. Normal/race/vet/schema/spec/dashboard gates passed; external review was intentionally deferred to the final consolidated branch at user direction. | root + bounded resource/codegen/provider/OTLP/Galileo subagents |
| `V-0093` | 2026-07-05 | `8d6e0ae448865270261c975e167c8b8c82f5b297` | P3-WP02/P3-WP04/P5-WP03 generated canary and lease closure | Full normal tests for telemetry, runtime, OTLP, Galileo compatibility/destination, destination assembly, and gateway; focused race across the same packages; three-run race regression for the generation pipeline; scoped vet; generator check; 104-decision spec validation; 35 Agent360/Grafana tests; diff check | The release canary is now a content-free generated `span.agent.invoke` root plus `span.model.chat` child whose construction, canonical handoff, flush, acknowledgement, and reload retirement remain under one graph lease. Bare v8 providers fail closed. Generic OTLP and Galileo require exact destination, family/bucket, operation, status, generation, hierarchy, resource, scope, sampled flags, and trace state; split, malformed, partial, mistargeted, or rewritten pairs do not acknowledge. Concurrent reload publishes generation two while a blocked generation-one canary keeps its old pipeline alive, then retires it only after acknowledgement and release; failed acknowledgement also releases cleanly. The gateway prefers this runtime-owned path and reports the exact generation. Tempo retention and Agent360 canary exclusion remain intact. | root + two bounded canary/runtime/export subagents |
| `V-0094` | 2026-07-05 | `8d3d7c480`, `2cc88f1d0`, `13c03a822`, `abb5ceca1`, `1f6b26404` | P3/P5 canonical consumer and real proxy producer activation | Full gateway normal suite; focused generated-trace stress; OTLP/Galileo/destination normal/race/vet; 29 focused dashboard tests; diff checks | Generation-owned Galileo, generic OTLP, and local-observability trace consumers now accept the generated canonical record rather than independently rebuilding it. The real proxy emits root-agent/model traces with truthful reported correlation and no fabricated process-global agent identity. Typed OTLP fields, events, links, resources, scope, status, flags, tracestate, dropped counts, local Agent360 aliases, exact destination isolation, and runtime reload audit families passed. | root + bounded Galileo/OTLP/proxy subagents |
| `V-0095` | 2026-07-05 | `4ea96120a` | P5-WP06 exact converter consumption checkpoint | 246 focused compatibility/migration tests; 358 broader observability/config/migration tests with one skip and eleven subtests; Ruff; compileall; registry check; diff check | Split local protocols retain the exact reserved `local-observability` trace destination name, Galileo's authoritative v7 filter resolves only the generated agent/model/tool selection, and exact goldens cover generic OTLP, `emit_otel:false`, individual-finding suppression, `legacy-v7` versus `none`, metrics, and wildcard prevention. The generated artifact itself remained explicitly incomplete at this checkpoint. | root + compatibility subagent |
| `V-0096` | 2026-07-05 | `ff710467e` | P3 production v8 activation and transactional reload | Full config/CLI/gateway normal tests; focused config/gateway race tests; scoped vet; diff check; live local-observability HTTP/protobuf canary | Schema v8 now compiles an immutable exact source snapshot before serving, skips competing legacy exporter graphs, assembles one process-owned runtime, and closes it before SQLite on every normal or early return. Hot reload decodes and compiles identical captured bytes, rejects ABA/source-version/resource-identity hazards, publishes provenance only after acceptance, installs fsnotify before synchronous reconciliation, and rolls back graph/config together. A real bootstrapped local destination acknowledged the exact two-span canonical pair with Agent360 aliases. | root + lifecycle/activation subagent |
| `V-0097` | 2026-07-05 | `eb507a119` | P4-WP01 first production control-plane cutover | Full audit/runtime/pipeline/gateway normal tests; focused audit/gateway race; scoped vet; diff check; live HTTP JSONL exact-delivery E2E | Six config/policy/authentication actions now build generated mandatory compliance families lazily through the pinned runtime generation. Ordinary emissions populate one canonical `audit_events` row while preserving meaningful v7 compatibility columns; runtime failure never falls back to a duplicate. Disabled collection persists only a minimal floor and an adversarial secret canary appears in no SQLite column. The default `none` route delivered the same occurrence exactly once to HTTP JSONL. Remaining P4 mappings and canonical admin target/reason fields are not claimed complete. | root + audit producer subagent |
| `V-0098` | 2026-07-05 | `7eb3d28eb` | P4-WP01/P4-WP02 runtime-exclusive typed audit cutover | Full audit/gateway/watcher/runtime/pipeline tests; focused audit/gateway/watcher race; scoped vet; gofmt and diff checks | The cycle-free audit seam now returns explicit admission plus local-persistence ownership. Every v8-handled ordinary, mandatory-floor, or dropped occurrence suppresses all legacy SQLite/activity/sink/structured/gateway/OTel resurrection. Approval grant/deny, judge allow/block, and successful watcher quarantine use generated families with truthful correlation and a distinct enforcement ID. Production judge `error` retains exactly one sanitized v7 row until a failed canonical outcome exists, preventing the data-loss regression found during root review. | root + audit producer subagent |
| `V-0099` | 2026-07-05 | `8bcc396ed` | P5-WP06 generated v7 exporter-selection closure | 263 migration/compatibility/package tests; 9 compiler/renderer authority tests; generator `--check`; Ruff; JSON/count and diff checks; isolated built-wheel resource parity | The sole registry compiler/renderer owns the versioned compatibility artifact. Exact coverage is 14 always-local log buckets; conditional OTel collection across all 14 buckets; generic/local 14 log buckets, 25 spans, and 14 metric buckets; Galileo's exact three spans; 188 audit actions; 168 gateway identities; and 19 identities reachable through the four v7 audit-sink gateway paths. The converter loads only the packaged artifact, rejects duplicate/malformed/mixed-epoch resources, and binds it to `catalog.json` by registry version plus materialized-view digest. | root + compatibility-generation subagent |
| `V-0100` | 2026-07-05 | `8bcc396ed` plus live working-tree acceptance | P3-WP05/P5-WP04 PR #403/#412 compatibility revalidation | Collector 0.153.0 validation; 50 focused dashboard tests; 14-dashboard/313-panel audit; independent 441-query inventory; full live dashboard compilation/inventory; real Tempo and Prometheus root/direct+nested-subagent/model/tool traffic | Source/package parity remained exact. A real v8 runtime emitted correct root, direct and nested subagent, turn, model, and tool ancestry into Tempo; Agent360 produced nine aligned series with root/parent/lifecycle/execution dimensions. Two generated canary pairs reached Tempo while the exact Boolean branch excluded only four canary spans from Agent360 spanmetrics. All live queries compiled with zero errors; no production fix was required. | root + local-observability subagent |
| `V-0101` | 2026-07-05 | `160933297` | P4-WP01..03/P5-WP02/P5-WP06 source-fact enrichment epoch | 538 registry compiler tests; 238 candidate renderer tests; 78 Go API/fixture/producer/renderer tests; focused security registry test; generator write/check; Ruff/gofmt/diff checks; all `internal/observability/...` packages; isolated-commit generator and Go verification | The sole authored registry now carries source-backed administrative, scan, finding, network, asset-lifecycle, AI/agent-discovery, and OTLP-ingest facts plus four dedicated discovery logs. Judge error is a generated `failed` outcome without inventing state; finding workflow status remains forbidden on observation families through a negative fixture while its deprecated global attribute stays additive-ABI compatible. The generated epoch contains 91 log families, 25 span families, 1,897 Go declarations, 172 v7 gateway identities, 13 normalized examples/OTLP cases, exact profile bindings, and a new content-addressed symbol-table baseline. PR #403 lineage/correlation fields and PR #412 metric/profile identities remained explicit. The commit was rechecked in a detached worktree, proving it does not depend on concurrent producer or P6 changes. | root + security/operations/audit subagents |
| `V-0102` | 2026-07-05 | `bd3e7df60` | P4-WP02 complete judge-result cutover | Audit/gatewaylog/telemetry/gateway normal tests; focused audit/telemetry/gateway race; scoped vet; gofmt/diff checks; amended-commit detached-worktree full gateway rerun | Provider, empty-response, and output-parse judge failures now produce exactly one generated `guardrail.judge.completed` record with outcome `failed`; allow/block remain exact. A closed internal failure class keeps bounded metric labels truthful, every failure has a centrally redacted summary, and only actual output parsing populates `parse_error`. The bounded queue emits canonical completions even when forensic body retention is off; body transaction begin/insert/commit failures record bounded health/drop telemetry but cannot suppress the ordinary completion. Raw prompt/response bodies remain excluded, malformed classifications fail closed, and the v7 error-action fallback is gone. A detached-worktree run exposed and removed an accidentally co-staged P6 test before the final isolated gateway suite passed. | root + judge-lifecycle subagent |
| `V-0103` | 2026-07-05 | `ff4d566f5` | P6-WP01 effective-plan inspection and reload truth | Config/CLI/runtimegraph/local-observability normal and race; scoped vet; 24 Python CLI tests; Ruff/format/diff checks; exact catalog parity; exhaustive leaf provenance; secret-bearing Go wire; real bound-Prometheus reload; detached-worktree rerun | `config show --provenance` now carries one exact Go-owned `value_path` annotation for every effective leaf while preserving truthful file/default/catalog/preset/generated/compiler origins and source locations without secret values. `observability plan` distinguishes profile membership, shape availability, end-to-end routed eligibility, and collection/routing/transport reload behavior. Generated profile and family-binding `pending` state is never presented as eligible. Local-observability identities have one cycle-free runtime authority. Native Prometheus policy/transport correctly reports restart-required until its listener becomes process-stable; same-binding reload rejects before candidate construction and preserves the serving listener, graph pointer/generation, digest, and config-manager generation. Destination handshake/write-probe UX remains open. | root + P6 inspection/review subagents |
| `V-0104` | 2026-07-05 | `5cb4ac73a` | P4-WP03 truthful asset-discovery producer cutover | Full audit/telemetry/watcher normal and race; focused ownership/no-loss race; scoped vet; gofmt/diff checks; exact-commit detached-worktree normal/race/vet | The watcher now emits one generated `asset.discovered` record for `install-detected` using the actual registry producer key and source-backed asset name, type, target path, reason, and initiator. Runtime ownership suppresses legacy log/sink fanout while retaining the compatibility audit metric; collection disablement creates no ordinary record; canonical persistence failure never resurrects a duplicate; and legacy identifiers outside the v8 grammar fall back without loss. The complete 23-action map proves only discovery is canonical: admission, health, enforcement, quarantine/restore, and API enable/disable actions are not relabeled. Existing `enforcement.quarantine.applied` remains unchanged, and its required post-state-change `asset.quarantined` companion is deliberately not fabricated before a registry-owned companion occurrence exists. | root + asset-lifecycle subagent |
| `V-0105` | 2026-07-05 | `3de157cee` | P5-WP05 generated compatibility-profile authority | `go test` normal/race and `go vet` for the focused profile/Galileo/local projection packages; broad `./internal/observability/... ./internal/config ./schemas` normal suite; registry generator `--check`; Ruff check/format; package-resource and built-wheel digest parity; `pytest -q cli/tests/test_telemetry_registry_candidate_renderer.py cli/tests/test_telemetry_registry_generator.py` | All focused and broad Go gates passed; generated artifacts were current; Python package and wheel bytes matched repository manifests; the exhaustive renderer/generator suite passed 777 tests in 1,893.85 seconds. The generated epoch proves Galileo's six-family shape/operation contract and local observability's complete 91-log/25-trace/131-metric contract. OpenInference carries a content-addressed manifest but remains non-executable and fails closed rather than claiming an absent runtime. | root + bounded projection subagent |
| `V-0106` | 2026-07-05 | `70e78287b` | P6-WP01 process-stable Prometheus reload contract | Three-run focused generation-pipeline regression; full destinations package | An enabled native Prometheus listener is restart-only even when its binding is unchanged. Reload rejects before constructing a second listener or provider, leaves the original graph/provider/listener/canary serving, sends no traffic to the replacement OTLP endpoint, and retires the original generation only on close without mutating OTel process globals. | root + destination-test subagent |
| `V-0107` | 2026-07-06 | `dc4797439` | P5-WP05 generated metric-contract substrate | Full normal tests for `./internal/observability/... ./internal/telemetry ./internal/config ./schemas`; focused three-run normal and race regressions; full telemetry/runtime race; scoped vet; registry generator `--check`; diff check | The digest-bound generated local profile plus canonical catalog now owns all 131 metric descriptors, including exact bucket, instrument/value types, unit, temporality, authored `null` versus empty histogram boundaries, 2,048-cardinality policy, canonical labels, and local aliases. Generic and local projections are independent; metrics-only local identity is preserved; duplicate destinations/families/sinks and unstable sink identities fail closed; failed flushes do not skip later destinations; rollback and retirement shut each stable sink exactly once. One graph lease spans collection-before-builder, generated construction, projection, and synchronous handoff, so reload cannot retire an in-flight generation. No destination factory or producer is presented as activated before its real sink exists. | root + generated-metric subagent |
| `V-0108` | 2026-07-06 | `1bd90a44d` | P3-WP02/P5-WP05 generated OTLP metric activation | `go test ./internal/telemetry ./internal/observability/destinations/otlp ./internal/observability/destinations -count=1`; the same three packages under `-race`; scoped `go vet`; gofmt/diff checks; ten-run lifecycle/transport stress by the bounded implementation agent | Each selected OTLP destination now owns a private generated-record SDK provider, periodic reader, exporter, guarded HTTP/gRPC transport, exact immutable generation resource, and instrument cache. Real HTTP protobuf evidence proves canonical versus local label dialect and exact resource/unit/delta/boundary behavior; real gRPC evidence covers every instrument/value combination present in the 131-family catalog. Reload keeps generations and endpoints disjoint, old records cannot enter a new generation, one failing destination cannot suppress its sibling, partial construction rolls back exactly once, and shutdown timeout/retry/idempotence shares one terminal operation. Native Prometheus generated-record activation and producer-wide cutover remain open and are not claimed here. | root + OTLP metric subagent |
| `V-0109` | 2026-07-06 | `94e48e12f1515086d4120e77defe43c4acc3bd20` | P6-WP01 local-only destination-test closure and registry successor | Registry generator write/check; 538 generator tests; partitioned complete 239 candidate-renderer tests; 78 Go API/fixture/producer/renderer tests; full `./internal/observability/... ./internal/cli` and gateway suites; focused race/vet; 56 CLI/spec/inventory tests; Ruff/gofmt/diff checks; clean detached-worktree generator, Go, gateway-handler, and Python rerun | The registry successor contains 93 logs, 25 spans, 131 metrics, 1,911 public declarations, 203 producer mappings, and 8,077 expanded producer rows with a reviewed content-addressed symbol baseline. An explicit destination test records generated attempted/completed compliance families through authenticated loopback and a runtime local-only operation, with exactly-once SQLite evidence, collection-disabled minimal floor behavior, bounded content-free fields, and zero optional fan-out. The Go helper resolves the bearer internally; Python, argv, output, error text, and records never contain it. Verification tools now recognize the generated metric allowlist and the intentional v7 selection contract without weakening unknown-authority rejection. The exact commit was rechecked in a detached worktree, proving independence from the concurrent P7 activation foundation. | root + destination-test subagent |
| `V-0110` | 2026-07-06 | `730ca89eac06471b4798a3305f5472bbed18f1c4` | P7-WP02 cooperative activation transaction foundation | 302 activation/converter tests; 416 related config/setup/migration/writer tests plus 18 subtests and one existing skip; Ruff lint; focused Ruff format; `py_compile`; diff check; two adversarial subagent passes with live stale-writer, ACL, dependency-drift, owner, and deadlock repros | Conversion binds the exact effective data directory, consulted environment presence/value digests, durable secret edits, and destination-field provenance. Activation holds config then environment sibling locks, validates through the target compiler seam, creates private descriptor-relative recovery backups, preserves trusted metadata, rejects unsafe parent/leaf ownership, symlinks, inherited macOS read ACLs, file flags, unsafe permissions, and unsupported Windows DACL mutation, then publishes `.env` before the config commit marker with exact verification and rollback. Supported Python writers share the lock order without nested-flock deadlock. The code explicitly limits stale-source protection to lock participants: production P7 must stop the gateway/Go `.env` writers before calling it, and no production caller or migration-cursor claim exists yet. | root + activation/adversarial-review subagents |
| `V-0111` | 2026-07-06 | `f0d41ba6de58f56e78c296df7ced796c6c685a4c` | P6-WP02 canonical operator controls | 246 focused v8/legacy setup, doctor, plan, and destination tests passed with one existing skip and eleven subtests; Ruff/compile checks; real Go helper smoke against one capability-default generic OTLP destination and all eleven optional presets | Status is derived only from the masked canonical Go effective plan. Atomic source mutations preserve comments and file mode, compare the source digest before replacement, validate through Python and the canonical Go compiler, and never persist inline secret values. Capability-default generic OTLP selects logs/traces/metrics, all fourteen buckets, and `none`; explicit signals narrow it, Galileo remains trace-only, and mandatory local SQLite cannot be disabled or removed. TUI/live health remains open. | root + operator-control subagent |
| `V-0112` | 2026-07-06 | `c5a0a9938` | P5-WP04 generated lifecycle topology substrate | Focused generated lifecycle normal/race tests; scoped `go vet`; gofmt/diff checks; 50 Agent360/Grafana/local-observability compatibility tests | Generation-pinned transition and approval handles preserve exact agent/tool parentage, root/direct/nested-subagent hierarchy, phase/sequence/operation fidelity, child-before-parent completion, missing approval content, reload, sampling, and collection behavior. Proxy v8/legacy agent-model XOR and local-profile eligibility remain exact. Production hook/EventRouter cutover is not claimed. | root + lifecycle-topology subagent |
| `V-0113` | 2026-07-06 | `dec25eaa9` | P3-WP02/P4-WP03 inbound OTLP generated cutover | Registry generator `--check`; full gateway suite in 84.040s; focused gateway race; gateway vet; full observability package; 35 Agent360/Grafana tests; diff check | The production API binds one process-owned v8 runtime for inbound OTLP logs and generated metrics. Accepted, malformed, oversized, and authentication-failure batches use generated telemetry families or content-free mandatory floors; the five ingest metrics and source-backed GenAI token/duration metrics retain exact PR #412 labels and Claude `cacheRead`/`cacheCreation`. Collection, two-destination routing, reload, loop prevention, 413 overflow, and v8/legacy XOR are covered. Receive/normalize spans and accepted-record classification/re-export remain open. | root + inbound-OTLP subagent |
| `V-0114` | 2026-07-06 | `eed11d322` | P3-WP03/P5-WP05 native Prometheus generated activation | Normal and race tests for Prometheus/destinations/telemetry; scoped vet; Windows amd64 destinations compile; all observability destination packages; 14-dashboard/313-panel audit; 35 Agent360/Grafana tests; diff check | Production retains the legacy SDK reader for all 131 still-active producer fields and attaches a generated sibling exporter to the same destination endpoint. Separate private registries avoid duplicate-family failures; a generation-local gatherer sums counters/up-down counters, makes generated gauges authoritative, merges histograms only for identical ordered boundaries, preserves compatible HELP, fails closed on ambiguity, and reapplies the exact 2,048-series/one-overflow ceiling. Routes, listeners, reload, rollback, shutdown, and process globals remain isolated. | root + native-Prometheus subagent |
| `V-0115` | 2026-07-06 | `b4c6207ce2661748c50c9933d8724a55afd1f169` | P5-WP04 lifecycle runtime binding foundation | Full runtime and gateway normal suites; focused runtime/gateway race; scoped vet; Windows amd64 gateway compile; ten-run concurrent bind/replace/detach/close stress; gofmt/diff check | One process-owned lifecycle seam now forwards request-bounded root agent, model, tool, and approval operations. Root approvals require a reported approval ID, retain exact physical parentage when supplied, and otherwise remain true roots without synthetic agent fields. Sidecar atomically publishes all API/canary/local/lifecycle capabilities plus EventRouter/proxy bindings under one lock order, detaches every replaced consumer, and prevents concurrent construction from republishing a runtime while close waits on an existing graph lease. Success, build rejection, sampling drop, abort, reload, replacement, and close paths release ownership exactly once. Producer callsites remain explicitly unmigrated. | root + lifecycle-runtime subagent |
| `V-0116` | 2026-07-06 | `ebaed7dbb` | P3-WP04/P5-WP05 Galileo shape closure | Registry generator `--check`; focused compiler/renderer tests; observability/profile/Galileo normal tests; Galileo/profile race; scoped vet; Ruff; diff check | `galileo-rich-v2` remains an exact positive allowlist of agent, judge-LLM, model, retrieval, tool, and workflow spans. Native agent-transition and approval-resolution spans are explicitly ineligible because Galileo has no truthful shape for them; generated manifests exclude them and the runtime projector rejects them even with forged agent attributes. The complete generated bundle was regenerated from the authored contract. | root + Galileo-profile subagent |
| `V-0117` | 2026-07-06 | `993bdbe99` | P7-WP02 required-migration restart gate | All 55 upgrade-command tests; required-failure end-to-end command regression; Ruff; Python compile; diff check | A release-manifest migration with failure policy `fail` now gates target service startup. A missing cursor entry leaves both gateways stopped, prints the existing recovery-backup location, exits nonzero, skips health polling and post-upgrade drift, and never prints upgrade success; successful and warn-policy paths retain the existing restart helper. | root |
| `V-0118` | 2026-07-06 | `4976f0739` | P4-WP02/P5-WP04 durable connector lifecycle cutover | Exact-commit detached-worktree focused and full gateway normal tests; exact focused race; scoped vet; Windows amd64 compile; 35 authoritative Agent360/Grafana tests; gofmt/diff check. The package-wide race run reached its existing 10-minute default timeout while unrelated parallel gateway tests were still executing and remains an aggregate P8 long-timeout gate, not positive evidence for this row. | Codex, Claude Code, generic connector, and inferred delegated-agent lifecycle occurrences now persist through exact generated compat families under one runtime-generation lease. Collection disablement cannot be bypassed, trace sampling cannot suppress durable logs, and runtime failures never resurrect legacy after ownership selection. Root/parent/depth lineage is conservatively marked reported only when all applicable source facts were reported; missing content/timing/usage stays absent. Existing lifecycle metrics/spans and prompt/response/tool fanout remain unchanged for PR #403/#412 compatibility. | root + lifecycle-topology subagent |
| `V-0119` | 2026-07-06 | `ffab0c3ab` | P3-WP02 inbound receive/normalize hierarchy | Full gateway normal in 84.273 seconds; full runtime/telemetry normal; focused gateway/runtime normal and race; gateway/runtime vet; Windows amd64 compile; diff check | Production JSON/protobuf OTLP ingest now emits generated `span.telemetry.receive` SERVER roots with generated `span.telemetry.normalize` INTERNAL children under one generation lease. Success, malformed input, invalid envelopes, unsupported content, oversize bodies, collection/sampling disablement, first-match route selection, reload, authentication, and exact self-export loop suppression retain only measured bounded metadata and never expose raw payloads. Durable accepted/rejected/authentication logs and all PR #412 metrics remain independent of trace sampling and preserve v8/legacy XOR. Arbitrary accepted-record re-export remains a tracked P3 contract gap because no canonical import mapping, upstream field classification, or origin/hop provenance exists yet. | root + inbound-span subagent |
| `V-0120` | 2026-07-06 | `196bf1943` | P7-WP02 production migration transaction | 165 focused migration, activation, cursor, and upgrade-command tests; 20 exact orchestration/path-swap tests; Ruff check/format; Python compile; diff check | The installed target wheel now converts the exact active v7 source, snapshots strict dotenv plus ambient precedence, validates an owner-only candidate with the installed target gateway and protected environment, and invokes the reviewed activation transaction only after fail-closed gateway quiescence. Ambiguous PID state, unsafe or swapped environment paths, validation/activation failure, and cleanup failure remain value-safe; already-v8 is idempotent and the cursor advances only after success. | root + activation subagent |
| `V-0121` | 2026-07-06 | `4344b90d0` | P7-WP01 required 0.8.4 registration | 100 release-invariant, migration, cursor, and upgrade-command tests; upgrade-manifest generator and Make target; Ruff check/format; diff check | The migration registry contains exactly one observability-v8 row at `0.8.4`, after published `0.8.3`. Source-pinned `0.8.0` manifests omit the forward row, while a release-stamped `0.8.4` manifest includes it under failure policy `fail`; production cursor failure/retry tests exercise the real row, so existing published cursor entries cannot silently skip the breaking conversion. | root + activation subagent |
| `V-0122` | 2026-07-06 | `b67961e23` | P3-WP02 accepted-record import contract | `uv run python scripts/check_observability_v8_spec.py`; all 9 focused spec tests; diff check | The normative package now validates 23 product, 12 semantic, and 75 ambiguity-removal decisions (110 total). Exact compiler-expanded inbound bindings, non-floor family reconstruction, typed mapping/field classes, SQLite-first logs, route redaction, original-versus-forward instance provenance, four-hop cycle termination, mixed-batch accounting, GenAI/Codex/Claude derivations, and PR #403/#412 fidelity are traced to OTLP-I01..I10 and OTLP-A01..A24. | root + inbound-contract subagent |
| `V-0123` | 2026-07-06 | `6e10950d9` | P3-WP02 typed inbound decode prerequisite | `go test ./internal/gateway -count=1`; focused gateway race for normalization/stats; `go vet ./internal/gateway`; `git diff --check` | The v8 receiver retains the official protobuf request model after strict JSON/protobuf decode instead of reconstructing imported leaves from generic maps. A streaming structural scan rejects duplicate JSON object members at every depth while preserving repeated OTLP attribute-list entries for generated target-specific ambiguity checks. Typed accounting counts logs, spans, and each metric data point as one leaf; v7 summary behavior remains isolated. | root |
| `V-0124` | 2026-07-06 | `c6c72752a` | P3-WP02/P5-WP01 inbound generator model correction | Spec validator; 9 focused specification tests; diff check | P-071 now distinguishes exact classifier matches from their one-family targets. Variant-bearing match IDs prevent five duration aliases from colliding on one canonical family, explicit import/derive augmentations cannot create false classifier ambiguity, and compiler-owned native-exact/native-malformed/external shapes prevent incomplete reserved markers from falling through to connector or GenAI bindings. | root + compiler-inventory subagent |
| `V-0125` | 2026-07-06 | `47b7eb4f3` | P3-WP02 inbound generation isolation | Full observability-runtime tests; focused inbound/metric race; scoped vet; diff check | One request-scoped import batch holds the runtime-graph lease across per-target collection, generated construction, SQLite-first log processing, destination enqueue, and synchronous generated metric delivery. Reload may publish a replacement while the request continues, but cannot retire the old generation or capture its remaining work. Disabled targets remain lazy and imported logs cannot acquire mandatory/floor state. Imported trace handoff will extend this same scope after generated provenance and reverse descriptors land. | root |
| `V-0126` | 2026-07-06 | `157423063` | P5-WP04 OpenClaw/EventRouter fact-fidelity contract | Spec validator (`D=23 S=12 P=76 total=111`); 9 focused spec tests; focused EventRouter/agent gateway tests; primary-source audit of pinned DefenseClaw, OpenClaw, and Bifrost revisions; diff check | P-076 and ER-RUN-01..12 distinguish OpenClaw's caller idempotency/run ID, per-run payload sequence, producer millisecond times, and literal start/end/error observations from the connection-local outer sequence and PR #403 session/root/subagent semantics. The current producer remains one legacy owner until the exact outcome-free `log.agent.run.observed` family is generated; no trace/generation lease or fabricated topology may bridge deliveries. | root + lifecycle-contract subagent |
| `V-0127` | 2026-07-06 | `3aa357c71` | P3-WP02 typed inbound attribute boundary | Full gateway tests; focused typed-attribute race; gateway vet; diff check | Generated discriminators can consume request-owned protobuf attributes through an exact-key/oneof-arm index with no JSON-map reconstruction, trimming, case folding, or scalar coercion. Repeated keys are ambiguous even when byte-equivalent, malformed presence remains distinguishable, and arrays/key-value lists/bytes cannot be flattened into strings. Target-specific mapping and unknown-field accounting remain pending the generated catalog. | root |
| `V-0128` | 2026-07-06 | `9680c9283` | P3-WP02 typed OTLP leaf traversal | Full gateway tests; focused typed-leaf/attribute race; gateway vet; diff check | One closed request-owned union now walks logs, spans, and every metric data point directly from the official protobuf model in protocol order, retaining exact resource/scope schema and typed attributes. Nil model elements fail structural processing, empty metric descriptors create no leaf, and v8 batch accounting uses the same walker the importer will consume, eliminating descriptor-versus-point drift. Generated matching/mapping and dispositions remain open. | root |
| `V-0129` | 2026-07-06 | `80859f23d` | P3-WP02 inbound disposition accounting | Full gateway tests; focused accounting race; gateway vet; diff check | The OTLP-I09 accumulator has a fixed closed primary/derivative vocabulary, accepts exactly one primary result per decoded leaf, keeps derivative targets outside the equation, derives completed versus partial only from the six permanent reasons, preserves the all-self terminal, and returns nonzero dropped reasons in normative fixed order. Generated leaf processing and health-family emission remain open. | root |
| `V-0130` | 2026-07-06 | `c2fa173d8` | P6-WP02 canonical operator status | 126 focused v8-status/TUI/doctor tests; 293-test expanded operator suite; Ruff; Python compile; diff check | The operator surfaces now load the canonical masked Go effective plan off the UI event loop, preserve the legacy rendering path for non-v8 sources, and merge policy with only positively observed bounded live facts. Destination signals, buckets, profiles, retention, judge capture, health, queue, and activity render without leaking headers, URL credentials/query/fragment values, response bodies, or raw `last_error`; unavailable runtime facts remain explicitly unavailable. | root + operator-control subagent |
| `V-0131` | 2026-07-06 | `30315ba14` | P7-WP03 transactional local-observability bundle upgrade | 324 focused/broad upgrade, dashboard, Agent360, setup, release, and migration tests plus 12 subtests; Ruff check/format; Python compile; source/package README parity; diff check | The ordinary upgrade now invokes target-wheel bundle logic after required config migration, inventories every shipped file and the exact PR #412 stack contract, backs up the exact managed set privately, preserves destination-only custom files and Docker volumes, admits reviewed tombstones only by historical digest, stages then atomically activates manifest-last, verifies exact bytes/modes, and restores the prior managed tree on partial failure. Running stacks use `down` without volume deletion and restart only after successful refresh; five-service readiness and all fourteen dashboard UIDs gate healthy restart. | root + bundle-upgrade subagent |
| `V-0132` | 2026-07-06 | `9a350ef76` | P3-WP02/P-072 import-provenance ambiguity closure | Spec validator (`D=23 S=12 P=76 total=111`); 9 focused specification tests; diff check | Section 15 now fixes every bounded import string at 512 valid UTF-8 bytes, keeps upstream redaction profiles on the 128-byte lower-case stable-token boundary, defines the canonical UUID-or-stable-token upstream record-ID union, and makes mode/derivation/positive aggregate-count presence plus the zero-through-four hop range executable before implementation. | root |
| `V-0133` | 2026-07-06 | `cd68fcf06` | P3-WP02/P-072 canonical Record boundary | Full observability package tests; focused import-provenance normal/race tests; scoped vet; gofmt/diff check | Canonical `Provenance` now owns an optional nested `provenance.import` with closed protocol, mode, derivation, aggregate-count, identifier, record-ID, redaction-profile, and hop validation. Construction, access, and clone deep-copy it; deterministic wire output appears only when present, ordinary records remain byte-compatible, and no import authority was added to ordinary generated family inputs. | root + import-provenance subagent |
| `V-0134` | 2026-07-06 | `9392a48dd` | P4-WP01 operator-API authentication cutover | Focused authentication/OTLP normal and race tests; full gateway normal suite; gateway vet; gofmt/diff check | Non-OTLP bearer, custom-token, origin, content-type, Sec-Fetch, and CSRF failures now persist exactly once through generated `log.authentication.failed` with the protected-boundary mandatory fact and local floor. Canonical ownership never falls back to a legacy gateway log after build/persistence failure, request secrets and network metadata are absent, the independent registered auth metric remains exactly once with fixed-cardinality labels, and scoped OTLP authentication retains its specialized telemetry-ingest owner. | root + control-plane subagent |
| `V-0135` | 2026-07-06 | `40c66613f` | P6-WP02 generation-pinned live health | Full delivery/runtime/telemetry/destination/gateway normal suites; focused delivery/runtime/provider/gateway race; scoped vet; 19 CLI status grammar tests; diff check | One graph lease now joins the complete effective destination inventory with generation-exact log, trace, metric, and Prometheus sources. The gateway exposes only bounded state/reason tokens, exact queue/capacity values, monotonic counters, and actual success/failure times; copy isolation, panic isolation, stale-generation suppression, later-success recovery, disabled destinations, retention, and event-history failures are all explicit without endpoints, headers, payloads, or raw errors. | root + health subagent |
| `V-0136` | 2026-07-06 | `d6e4d568d` | P3-WP02/P-071 generated inbound catalog | Focused compiler/candidate/Go-plan tests; generated manifest inventory; generator `--check`; repeated isolated portable and seven-file Go render equality; full observability package; Ruff/format; diff check | The sole registry compiler now expands ten logical inbound classes into 237 exact matches and 245 one-family targets, with 24 structural/native markers, nine typed alias sets, 249 self-echo recognizers, 93 ordinary non-floor log contexts, and 711 JSON/protobuf fixture cases. Every target and import context binds its exact private generated family descriptor and ordered field inventory; malformed native shapes remain terminal and no public runtime API or heuristic receiver activation was introduced. | root + inbound compiler subagent |
| `V-0137` | 2026-07-06 | `1ddb65d48` | P7-WP02 ordinary-upgrade backup compatibility | 57 focused upgrade-command tests; 78 activation tests; Python compile; diff check | The ordinary upgrade now creates a descriptor-pinned, unique, private backup directory, tightens legacy umask-created roots from `0755` to `0700`, and rejects symlink roots. This removes the real one-command v8 migration failure without weakening activation's private-root requirement or same-second retry isolation. | root |
| `V-0138` | 2026-07-06 | `35132e2bb` | P4-WP01/P4-WP04 control-plane evidence and duplicate removal | Full audit/watcher normal suites; focused audit/watcher race; scoped vet; gofmt/diff check | Generated configuration/policy activity now carries only source-backed safe actor/origin/target, revision, state-count, stable reason-code, and change-count facts. Free-form values/keys never enter the canonical body, subsystem actors are not fabricated as principals, independent registered metrics remain exactly once, and the watcher no longer emits a second gateway/activity/metric path. | root |
| `V-0139` | 2026-07-06 | `6b1d421b8` | P7-WP04 historical upgrade matrix | 10 harness-contract tests plus 121 activation/upgrade/bundle production tests; Ruff/format; shell syntax; Make dry run; diff check | The single release harness preserves v7 checks for pre-0.8.4 targets and exercises the complete representative v7-to-v8 migration for 0.8.4+: routes/signals/redaction, protected values, exact recovery layers, cursor, local bundle, 14 dashboards, four volumes, SQLite, and target gateway health. Docker isolation prevents the harness from stopping a developer stack, and failure tails redact every fixture secret. | root + historical-matrix subagent |
| `V-0140` | 2026-07-06 | `287b78786` | P3-WP02 immutable inbound runtime catalog | Full observability normal; focused inbound-catalog race; scoped vet; gofmt/diff check | One fail-closed loader validates and freezes the generated nine aliases, 237 matches, 245 targets, 24 native markers, 249 echo recognizers, and 93 ordinary import contexts. Opaque copy-isolated handles expose exact typed predicates/rules/cross-references and concrete descriptors but no builder, constructor, mandatory, or floor capability. | root + inbound-catalog subagent |
| `V-0141` | 2026-07-06 | `7f47f46f3` | P3-WP04 first-class Galileo v8 CLI | 95 expanded focused tests (40 direct Galileo/v8 tests re-run by root); Ruff check; diff check | Exact-v8 Galileo setup/status/enable/disable/remove/test reuse the canonical v8 writer, masked plan, sanitized health, and content-free destination handshake. V7 retains its original path; the destination remains trace-only/unredacted with a one-second preset, credentials/raw errors never print, unsupported direct OTLP writes fail clearly, and no delivery count is fabricated. | root + Galileo-CLI subagent |
| `V-0142` | 2026-07-06 | `1369de338` | P3-WP02/P-072 generated nested import provenance | 544 generator tests; 242 candidate-renderer tests; 16 packaged-resource tests; seven focused structural tests; generator `--check`; full observability normal/vet; Ruff/diff | The sole structural registry now owns optional `provenance.import` and its closed 13-field contract, exact mode/derivation/aggregate-count/hop/identifier rules, nested bounds, and runtime UTF-8-byte validation ownership. The public schema carries five direct cross-field clauses and exact annotations; generated catalog, examples, OTLP fixtures, compatibility manifests, Go outputs, and six packaged resources are synchronized without granting ordinary builders import/floor authority. | root + structural-generation subagent |
| `V-0143` | 2026-07-06 | `c64dcbec4` | P3-WP02 exact inbound OTLP-I02 classification | Full gateway and observability normal suites; focused gateway/catalog race; combined vet; diff check | A pure typed classifier evaluates the generated source/predicate vocabulary over official OTLP leaves, recognizes exact wire echoes including non-reversible metrics, and closes native/external/unsupported/ambiguous/malformed disposition without construction or activation authority. All 237 matches are synthesized into 711 positive, negative, and single-fault cases; strict projected-log structure and self-echo candidates are covered. | root + inbound-classifier subagent |
| `V-0144` | 2026-07-06 | `2ac1f05aa` | P4-WP04/P5-WP02 generated event identity authority | Generator write/check; all observability packages; focused race; scoped vet; 259 renderer/candidate tests; Ruff/diff | Runtime identity validation now derives the exact 93 log, 25 trace, and 131 metric bucket/signal/name tuples from the generated family descriptors and only compiler-marked compatibility identities. Handwritten vocabularies are deleted, wrong-bucket identities fail, producer references must agree with canonical families, and `span.guardrail.judge` requires a generated low-cardinality judge kind. | root + producer-authority subagent |
| `V-0145` | 2026-07-06 | `b57703f30` | P3-WP04/P4-WP02 rich local judge span cutover | Full runtime/gateway normal; focused runtime/gateway race; scoped vet; diff check | All seven actual local judge provider-call shapes emit generation-pinned `span.guardrail.judge` with bounded source-backed messages, content state, model/usage/timing, finish, action, severity, finding, and failure facts. Clean judge `NONE` normalizes to canonical `INFO`, deny/block terminate as blocked, existing metrics remain once, and v8 ownership never falls back to a legacy span after sampling, failure, reload, or detach. | root + judge-span subagent |
| `V-0146` | 2026-07-06 | `371df7af4` | P4-WP04/P5-WP02 compatibility identity integrity | Focused event-identity/producer tests; diff check | Generated compatibility identities may extend the canonical log vocabulary only when they are globally unambiguous: a compatibility name cannot collide with a canonical family or claim more than one bucket, and duplicate rows must agree exactly. Runtime identity membership therefore cannot depend on producer-row order. | root |
| `V-0147` | 2026-07-06 | `560f7694c` | P4-WP01/P4-WP04 destination authentication floor authority | Registry generator write/check; focused classification/producer/identity tests; diff check | The sink-failure producer mapping now carries `protected_boundary_auth_failure`, so generated `destination.authentication.failed` and `destination.authorization.denied` occurrences retain the mandatory SQLite floor required by their canonical families. The complete generated bundle, schemas, examples, fixtures, compatibility views, and digests were refreshed atomically. | root |
| `V-0148` | 2026-07-06 | `6e8dae9e8` | P4-WP01/P4-WP04 generated producer-rule runtime authority | Full observability package tree; exhaustive 8,077-row conformance; focused classification race; scoped vet; diff check | Runtime classified-log resolution validates the exact generated bucket/event selection and consumes its generated mandatory and companion rules. Handwritten compatibility metadata cannot silently weaken a floor or broaden an identity; policy drift and missing generated producer entries fail closed. | root |
| `V-0149` | 2026-07-06 | `f7df935c9` | P3-WP02 private imported-log construction | Focused inbound builder/router/runtime tests; full observability normal and race trees; scoped vet; diff check | One accepted-record-only builder constructs every one of the 93 generated log import contexts and 96 import-role log targets through exact target/context/source/descriptors. Imported logs receive new local identity and trusted local/import provenance, can use only the four existing sealed GenAI structured grammars, remain ordinary non-floor records even for mandatory-capable families, stop before construction when collection is disabled, persist SQLite before optional fan-out, and export nowhere after local persistence failure. | root + inbound-construction subagent |
| `V-0150` | 2026-07-06 | `f4d09dfa8` | P4-WP03/P4-WP04 generated discovery cutover | Broad gateway/inventory/runtime normal; focused final normal/race; scoped vet; 50 Agent360/Grafana tests; 14-dashboard/313-panel gate; registry fact test; diff check | On-demand agent discovery emits exact generated completed/rejected/signal logs and all five registered metrics without its duplicate generic lifecycle completion. Continuous AI discovery emits one generated scan root with real detector children, exact summary/signal/component logs, and every existing discovery/component metric; metric siblings remain independent. V8 authority survives disabled collection, sampling, failure, reload, and detach without direct-OTel fallback, while true v7 retains its root/detector topology, three log shapes, metrics, and single lifecycle event. | root + discovery-cutover subagent |
| `V-0151` | 2026-07-06 | `2000bfb93` | P4-WP01 generated audit-sink platform health | Focused sink and real 401/403 floor tests; full audit/gateway normal and race suites; latest-tree compile/vet; gofmt/diff | Audit-sink delivery and circuit observations use generated destination-authentication/authorization/export/queue/restored logs and the five metrics backed by real measurements. One sticky v8 binding prevents reload/detach/failure fallback; closed error classification and sink-name normalization retain no raw endpoint, credential, payload, or error text. Disabled platform-health log collection reduces 401/403 to the exact minimal SQLite-only mandatory floor with no optional leg while metrics remain independently enabled. True v7 facts and labels remain byte-for-byte compatible; queue depth remains intentionally unproduced because no source measures it. | root + platform-health subagent |
| `V-0152` | 2026-07-06 | `ae6d00510` | P4-WP04 generated-only producer classifications | Exhaustive 203-group/8,077-identity classification tests; full observability normal/race trees; full gateway normal; scoped vet; gofmt/diff and handwritten-authority absence checks | The public 15 gateway and 188 audit classifications now derive solely from generated producer groups. Generated defaults and context rows own identity and bucket semantics, deterministic public rule unions remain copy-isolated compatibility metadata, and runtime resolution still selects only the exact generated row's mandatory and companion rules. All handwritten action/event tables, severity overrides, and legacy-name synthesis are deleted; AI-discovery call sites now state their generated-required bucket explicitly. | root + generated-classification subagent |
| `V-0153` | 2026-07-06 | `6d1641203` | P3-WP02 private imported trace/metric construction and source-unit authority | 28 focused compiler/renderer tests; generator/schema/public-parity/spec gates; exhaustive catalog and constructor normal/race; full observability/telemetry normal; focused runtime/telemetry race; scoped vet; 393-unit/104-native artifact audit; diff check | The sole compiler now seals exact duration, token, and native target-unit rules into both match and target descriptors. Private accepted-record constructors cover all 31 trace, 104 native metric, and 14 derived metric targets without exposing identity, floor, class, or raw-map authority; exact unit/source/value validation determines import/field-value/elapsed/cumulative-delta/arithmetic-mean provenance. Imported topology, resource, events, links, status, counts, timestamps, and full flags pass directly to canonical consumers on one generation, while disabled collection stops before mapping/ID allocation and trace/metric paths never create SQLite rows. | root + source-unit and inbound-construction subagents |
| `V-0154` | 2026-07-06 | `3ddb79bf8` | P3-WP02 imported origin and terminal-hop routing seam | Pipeline/runtime/telemetry normal and race suites; focused policy cases repeated ten times; scoped vet; existing dispatcher loop-suppression test; gofmt/diff | Imported-only sealed controls carry a validated local origin solely into optional delivery identity: the exact origin destination is suppressed while siblings still receive, and foreign same-text provenance has no routing authority. The hop-four terminal policy invents no destination token; logs still commit SQLite before producing no optional work, while traces and metrics still map, validate, and construct before reporting suppressed delivery. Existing local producer APIs remain unchanged zero-policy wrappers, and origin/hop controls never enter canonical records, resources, projections, or metric labels. | root + inbound-runtime subagent |
| `V-0155` | 2026-07-06 | `bbff8ee1c` | P4-WP01 live subsystem-health cutover and lifecycle ordering | Exact-commit detached worktree: `go test ./internal/audit ./internal/cli -count=1` (`audit` 23.526s, `cli` 3.267s); focused gateway lifecycle-order tests 1.647s; `go vet ./internal/audit ./internal/gateway ./internal/cli`; agent audit race 477.772s and focused gateway race 8.398s; gofmt/diff checks | Seven live source-backed sidecar, guardrail, and judge-storage actions now build exact generated `subsystem.ready`/`subsystem.degraded` records with sticky v8 ownership, content-free bodies, correct floor eligibility, and no legacy resurrection. V8 judge-body readiness waits for canonical runtime binding; shutdown retains that runtime through judge drain and forensic-store close, then retires it before audit shutdown. True v7 timing/fan-out remains exact. A closed 21-action inventory records the remaining unrepresentable/non-live platform actions rather than fabricating state transitions. | root + platform-health subagent |
| `V-0156` | 2026-07-06 | `fa498f0a9` | P3-WP04/P5-WP05 generated Galileo projection and evidence closure | Exact-commit detached worktree: `go test ./internal/observability/... -count=1`; focused Galileo/profile-manifest/integration `-race`; `go vet ./internal/observability/...`; diff/gofmt checks. The live-cloud test was compiled and skipped by default because credentials were not supplied. | All 25 generated trace-family structures are available through a detached names-only contract, and Galileo's exact six eligible shapes consume that authority for attributes, events, event fields, link relations/fields, resources, and scopes. Structural dropped counts survive OTLP; structured tool values remain canonical; one seven-span PR #403 graph preserves root/subagent/workflow/model/tool/retrieval/judge richness. Exact content-free counters reconcile every observed handoff across route, projection, queue, and transport. Production assembly proves generic OTLP and Galileo are mutually exclusive canonical arms that each receive one sibling copy with identical topology. The opt-in live canary requires an absolute HTTPS endpoint and carries no user content. | root + Galileo completion subagent |
| `V-0157` | 2026-07-06 | `b30b8b103` | P4-WP02/P5-WP04 generated hook lifecycle metric continuity | Exact-commit detached worktree: focused runtime/gateway/telemetry normal tests; runtime/gateway focused `-race`; scoped `go vet`; gofmt/diff checks. | A bounded batch API holds one runtime-graph lease across up to sixteen related generated metric occurrences while applying collection before each builder and reporting sequential partial failure truthfully. Real hook lifecycle updates now emit transition, last-seen, current-phase, optional phase-transition, and optional reported-cost families through that batch. Canonical and `local-observability-v1` sinks receive exact PR #403/#412 identity, lineage, provider/model, phase, and compatibility labels on one generation; disabled collection or a missing v8 capability never invokes the legacy provider, while the unbound v7 path remains exact. | root |
| `V-0158` | 2026-07-06 | `88c92860d` | P4-WP02/P5-WP04 generated connector-hook model traces and metrics | Full `./internal/gateway ./internal/observability/... ./internal/telemetry` tests; focused runtimegraph/runtime/gateway race tests; scoped `go vet`; registry generator check; complete schema check; gofmt/diff checks. | Completed connector-hook model calls now emit source-backed generated agent/model hierarchy, structured prompt/response, token and duration metrics, exact PR #403/#412 identity, and destination-specific central redaction. A DefenseClaw extension makes output `finish_reason` optional while retaining strict validation of the pinned upstream OTel schema, so absent provider data cannot erase reported output. Trace-owned metric batches fork the exact generation lease before invoking reentrant builders outside session locks; reload cannot retire the graph underneath them and ended handles are rejected. Sampling, trace disablement, invalid optional identity, and missing generated capability do not resurrect v7 or suppress independent metrics. | root |
| `V-0159` | 2026-07-06 | `7b55e880b` | P3-WP02/P4-WP03/P4-WP04 typed accepted-record receiver activation | Full scoped gateway/observability/telemetry normal tests; exact receiver matrix under `-race`; scoped `go vet`; Windows amd64 compile; JSON/protobuf parity; diff checks. | Every accepted OTLP log, trace, and metric leaf is classified and rebuilt through generated match/target authority on one runtime generation. Logs persist SQLite before optional fan-out; traces and metrics never create SQLite rows; collection gates construction; local origin and terminal-hop rules suppress only allowed delivery legs; raw/legacy v8 bypasses are gone. Generated PR #412 plans are the sole v8 token/duration authority with exact unit, cumulative reset, Claude token-category, and histogram-mean semantics. OTLP-A01..A21/A23 cover exhaustive builders, live receiver samples, topology/resources/times/provenance, unknown fields, redaction isolation, reload, fuzz, and parity; A22 is retained as the live destination gate. | root + typed receiver subagent |
| `V-0160` | 2026-07-06 | `700248376` | P6-WP04 complete v8 operator and developer documentation | Docs-site production build (143 pages); link validation (zero errors); 500 snippet checks; 1,031 CLI command grammar checks; diff checks. | Repository and docs-site guidance now use the strict v8 graph rather than the legacy v7 exporter model. Operators get concise source examples plus generated all-knob references, exact collect/export/unredacted defaults, 14-bucket semantics, ordered routing, mandatory SQLite, central field-aware redaction, independent fan-out, Galileo/local/Splunk behavior, and the ordinary one-command upgrade with behavior preservation, secret promotion, backup, rollback, bundle refresh, and PR #403/#412 continuity. | root + operator-documentation subagent |
| `V-0161` | 2026-07-06 | `efe77d19d` | P4-WP01/P4-WP03 generated subsystem lifecycle and network egress | Full audit/gateway/observability normal tests; focused race; scoped vet; generator/public-schema/spec/inventory gates; diff checks. | Sidecar, guardrail, and watcher start/stop now use generated subsystem lifecycle logs with correct floor behavior. Audit/gateway outbound network activity uses generated correlated egress logs and metric without exporting a full URL; agent, interaction, and tool correlation required by PR #403 survives. The regenerated registry contains 250 families: 94 logs, 25 traces, and 131 metrics. | root |
| `V-0162` | 2026-07-06 | `e831701b5` | P4-WP03 immutable findings and scan summaries | Full audit/scanner/runtime tests; focused gateway/bootstrap; focused race; scoped vet; diff checks. | A bounded generation-pinned log/metric batch persists forensic scan tables first, then emits one immutable `finding.observed` per actual finding followed by one truthful `scan.completed` or `scan.failed` summary. Finding occurrence IDs are distinct from detector/rule IDs, no synthetic `status: open` exists, and all prior scan count/duration/severity/rule metrics remain independently collected through generated families. | root |
| `V-0163` | 2026-07-06 | `c0fcbb764` | P4-WP02/P4-WP03 successful quarantine companion closure | Full audit/observability/watcher tests; focused race; scoped vet; deterministic registry check; public-schema parity; spec and current-state inventory gates; diff checks. | A successful skill/plugin filesystem move emits linked mandatory `enforcement.quarantine.applied` and `asset.quarantined` rows plus one generated quarantine metric on one immutable runtime generation. Source-backed identity/type/source/destination facts are validated, collection disablement produces two content-free SQLite floor rows, and the watcher no longer double-records the successful legacy metric. The quarantine producer is narrowed to its two canonical families plus its migration-only legacy identity (8,086 rows, 1,846 canonical). | root |
| `V-0164` | 2026-07-06 | `c0fcbb764` + live run `a22-20260706-a37c625f` | P6-WP03/P6-GATE local-observability continuity | Five services healthy with volumes preserved; 14 dashboards/313 panels/441 queries with zero errors; 50 focused dashboard tests; 20 Galileo Python tests; Collector refused/export health checked. | The fresh run produced one eight-span Tempo trace with root/direct/nested subagents plus model/tool/workflow, 16 correlated Loki records across three agents, 17 Prometheus families, and 25 populated Agent360 panels. Inventory classified 39 data, 108 zero, 161 expected-empty, 2 interactive, 3 static, and 0 error panels. Direct authenticated Galileo HTTPS accepted content-free trace `68d8786f17e8675345ae552ef919b5f2`; runtime-path Galileo acknowledgement remains P3-GATE because no gateway process was available during that probe. | root + live-destination subagent |
| `V-0165` | 2026-07-06 | `0cec33a8f` | P4-WP04/D-024 target-runtime hard cutover | Full CLI and gateway tests; focused startup/bootstrap/config-manager tests; strict source, transactional reload, restart/rollback, gofmt, and diff checks. | The target binary strict-parses and compiles the exact v8 source before its general Config decoder, refuses v7 startup/reload with `defenseclaw upgrade` guidance, reconciles the watched source before serving, and has no legacy config apply callback, OTel/audit-sink reload reconstruction, privacy kill switch, or judge-retention environment override. Upgrade/preview and historical database migrations remain separate compatibility boundaries. | root |
| `V-0166` | 2026-07-06 | `11f0f002e` | P4-WP02..04 domain-producer hard cutover | Full audit suite (`17.466s`); full watcher suite (`5.504s`); focused gateway asset/network/quarantine tests (`1.384s`); exact API network-ingest test; gofmt and diff checks. | Asset discovery, judge completion, linked quarantine, and network egress now require their generated v8 capabilities and fail before alternate persistence or fanout when unbound, detached, malformed, or incomplete. Network forensic rows and canonical event-history compatibility projections remain; duplicate blocked-alert SQLite/sink/structured/direct-OTel paths are deleted. PR #403 lifecycle/execution/user/tool correlation and URL minimization remain exact. | root + domain producer subagent |
| `V-0167` | 2026-07-06 | `435552bd5` | P5-WP04/P5-WP07 truthful agent-run cutover and compact producer authority | `make telemetry-check`; 61 focused producer/API/renderer tests; generator write/retirement regressions; full observability package tree; full gateway plus post-binding focused lifecycle tests; scoped vet; spec and diff checks. | The compiler still validates all 8,096 expanded producer source tuples but emits 274 shared identities, 19 identity sets, and 203 group references; the generated producer file falls from 5.86 MB/24,466 lines to 1.71 MB/17,669 lines without broadening selection. EventRouter emits one generated, outcome-free `agent.run.observed` occurrence for each valid OpenClaw start/end/error fact, retains source time and run identity, rejects malformed content through a fixed mandatory diagnostic, bounds replay dedupe, and creates no fabricated PR #403 topology, terminal outcome, cross-delivery span, or inferred approval correlation. | root |
| `V-0168` | 2026-07-06 | `2fea68c6d` | P5-WP07 generated fixture and ownership-manifest closure | 17 renderer tests; complete fixture-plan suite; manifest and arbitrary-retirement regressions; `make telemetry-generate`; `make telemetry-check`; full observability package tree; scoped vet; Ruff; spec and diff checks. | The catalog-derived static gate validates every generated family descriptor, named input, builder method, compiled signature, and reachable public type without a second serialized allowlist. Compile-only cases remain validated in the fixture IR but are not emitted as dead Go tests; all curated success/failure canonical-byte and class assertions remain. The fixture file shrinks by 68% in bytes and 92% in lines. The manifest now owns only 40 present outputs instead of claiming 30 absent reproducible reports/corpora, while safe retirement of arbitrary prior owned files remains enforced. | root |
| `V-0169` | 2026-07-06 | `1d6debca9` | P4-WP02/P5-WP04 EventRouter approval trace cutover | Four focused approval/authority tests; focused race; scoped gateway vet; full gateway suite (`93.810s`); gofmt and diff checks. | Automatic approvals and policy denials now emit exactly one generated request-bounded approval span with requested/resolved events and default-unredacted source content for central route redaction. Invalid facts fail before runtime use, disabled trace collection creates no span, missing agent/session topology remains absent, pending manual requests do not invent a terminal result, and once v8 is bound a detach cannot revive the direct legacy OTel path. | root |
| `V-0170` | 2026-07-06 | `657211ff6` | P4-WP02/P5-WP04 EventRouter tool hard cutover | Seven EventRouter tool integration/contract tests plus the existing hook-tool matrix; focused race (`24.204s`); scoped gateway/audit vet; full audit (`17.349s`) and gateway (`97.643s`) suites; gofmt and diff checks. | Exact call-ID correlation preserves same-name concurrency while bounded TTL/capacity/content retention holds no runtime object. Missing IDs and conflicting/expired results remain result-only. Generated requested/completed/failed/blocked logs, spans, and independently collected metrics retain default-unredacted source content for route redaction, omit unavailable agent identity, preserve dashboard tool/destination dimensions from the redacted projection, and emit no direct legacy call/result row or SDK span after v8 authority. | root |
| `V-0171` | 2026-07-06 | `ea713545a` | P4-WP02/P4-WP04 local-judge provider hard cutover | Seven successful judge shapes plus provider/empty/parse failures; exact canonical metric family/dimension assertions; always-off sampling; runtime detach; full gateway (`89.017s`) and telemetry (`1.202s`) suites; focused race (`23.854s`); scoped vet; gofmt and diff checks. | The injection, PII, exfiltration, tool-injection, and three adjudication paths have no direct `telemetry.Provider`, legacy SDK span, global provider, or v7 runtime test. One process-owned generated runtime now emits the rich judge span plus gateway invocation/latency/error, guardrail latency, and GenAI input/output token metrics on a pinned generation. `gen_ai.operation.name=chat` follows the pinned convention while `defenseclaw.judge.kind` retains the DefenseClaw role; metrics remain present when trace sampling is always-off and inherit exact W3C IDs only when a real started span exists. | root |
| `V-0172` | 2026-07-06 | `31c97a685` | P4-WP02/P4-WP04/P5-WP04..07/P6-WP03 HILT hard cutover and dashboard correctness | `make telemetry-generate` and `make telemetry-check`; inventory/spec/public-schema gates; full observability (`2.052s`), telemetry (`1.452s`), audit (`18.609s`), and gateway (`97.843s`) suites; focused HILT race (`16.954s`); scoped vet; five focused generator regressions (`16.73s`); 55 dashboard/local/inventory tests (`19.03s`); complete 14-dashboard/313-panel audit; Ruff/gofmt/diff checks. | Blocking chat HILT now emits one generated request/resolution operation with exact W3C and PR #403 run/request/session/turn/agent/evaluation/policy correlation. The resolution remains a content-free SQLite floor when collection is disabled; default ordinary routes retain unredacted bounded command/reason/rules for central per-destination redaction; logs and the typed lifecycle metric survive trace sampling; unavailable and delivery failures stay distinct. The local HILT dashboard reads the new pending/approved/denied/expired/cancelled/unavailable/delivery-failed metric without contaminating exec approvals. Generated-only metric additions no longer require a legacy migration-inventory row, exact-total edits, or line-number callsite updates, while all 131 legacy metric type/unit/label contracts remain enforced. | root |
| `V-0173` | 2026-07-06 | `457ef5bf8` | P3-WP02/P4-WP03..04 inbound OTLP hard cutover | Full gateway suite (`97.998s`); focused inbound/handler race (`60.131s`); scoped gateway vet; v8 inventory/spec gates; focused typed receiver/raw/Codex tests; gofmt and diff checks. | The live handler now requires the process-owned v8 runtime and imports official typed JSON/protobuf requests only through generated per-leaf authority. An unbound receiver returns 503 without SQLite or direct-provider writes. Accepted logs preserve generated correlation and SQLite-first routing; W3C trace/span topology remains exact; accepted traces/metrics remain non-SQLite canonical signals; Codex/Claude token and duration derivations still feed every selected destination; malformed input emits content-free mandatory health evidence without retry storms; exact self-exports remain recursion-free. The v7 audit/provider fallback, raw request-body retention and dedupe, generic body-wide identity promotion, heuristic token/duration scanning, Splunk HEC shadow projection, and obsolete runtime tests were deleted (2,510 lines removed). | root |

| `V-0174` | 2026-07-06 | `e7a30608b` | P4-WP02/P4-WP04/P5-WP04/P6-WP03 EventRouter exec approval hard cutover | Full gateway suite (`102.222s`); focused approval race (`15.813s`); scoped gateway vet; v8 inventory/spec gates; Grafana 14-dashboard/313-panel audit; 35 dashboard tests (`6.41s`); gofmt and diff checks. | Exec approval requests and immediate policy/automatic resolutions now emit generated `approval.requested`/`approval.resolved` logs, one rich approval span for actual terminal decisions, the typed `approval.lifecycle{surface=exec}` metric, and the generated `approval.count` compatibility metric that preserves result/auto/dangerous dashboard dimensions. Command, argv, cwd, actor, rule, dangerous facts, and any source-backed correlation remain default-unredacted until central per-destination redaction. Pending manual requests emit no fabricated terminal span; disabled compliance logs drop request detail and persist exactly one content-free mandatory resolution floor while metrics remain independent. The current approval frame has no session/run anchor, so `ff086a82a` correctly leaves it a root instead of attaching the most recent unrelated model context. The handler has no direct approval SDK span, provider metric/alert, or legacy approval audit write, and dashboard provenance now names the v8 producer. | root |

| `V-0175` | 2026-07-06 | `ff086a82a` | P4-WP02/P4-WP04/P5-WP04/P6-WP03 EventRouter model/guardrail provider hard cutover | Full gateway suite (`106.557s`); focused model/tool/approval/agent-run race (`38.420s`); scoped gateway vet; v8 inventory/spec gates; Grafana 14-dashboard/313-panel audit; gofmt and diff checks. | Completed OpenClaw assistant messages now emit a generated default-unredacted zero-duration `span.model.chat` with response, provider/model, tokens, finish reasons, tool-call count, and source-backed correlation; token metrics remain available when traces are disabled, and no duration is fabricated. A bounded 4,096-entry/10-minute cache retains ended W3C contexts only, parents tools by exact or unique session match, clears terminal run entries, and refuses cross-session, ambiguous, or unanchored approval parenting. EventRouter guardrail decisions and concrete alerts use generated inspect/alert metrics, the router owns no `telemetry.Provider`, and dashboards consume the first-class connector, canonical severity, and dedicated alert series. | root |

| `V-0176` | 2026-07-06 | `a5d6a960f` | P4-WP02/P4-WP04/P5-WP04..05 connector-hook telemetry hard cutover | Full gateway suite (`107.958s`) and OTLP destination suite (`2.085s`); full observability package; generated registry check; focused hook/OTLP integration and sink-parity tests; focused hook and OTLP race tests; scoped gateway/OTLP vet; gofmt/diff checks. | Regular, synthetic, rejected, and unified connector-hook paths no longer call direct Provider hook, inspect, token, dispatch, or free-form log APIs. One generated default-unredacted `hook_decision` log retains final raw/effective decision, mode, would-block/enforced, bounded reason/rules, lifecycle/execution/operation identity, and exact W3C correlation for central per-destination redaction; seven generated metric families retain every PR #412 dashboard dimension with clean `NONE` normalized to `INFO`. The active hook span keeps canonical guardrail metadata plus compatibility aliases and forensic step/enforced/rule-pack identity without copying sensitive detail outside central redaction. Connector token `kind=prompt|completion|total` now uses its truthful bounded field instead of the incompatible GenAI input/output enum, and OTLP logs promote canonical trace/span IDs, timestamp, and severity into standard `LogRecord` fields for backend joins. | root |

| `V-0177` | 2026-07-06 | `00d589f71` | P4-WP01/P4-WP04 API authentication telemetry hard cutover | Full gateway suite (`107.785s`); focused authentication normal (`2.552s`) and race (`11.434s`) tests; generated registry check; scoped gateway vet; gofmt and diff checks. | Ordinary sidecar authentication failures emit only the generated mandatory `authentication.failed` compliance log and `defenseclaw.http.auth.failures` metric; scoped OTLP failures retain their generated signal-aware `telemetry.authentication.failed` log and share the generated metric family. Fixed `sidecar-api` and `otlp-<signal>` routes plus a closed reason vocabulary prevent caller routes, scoped tokens, credentials, network metadata, and user agents from reaching telemetry. Canonical W3C request correlation is preserved, disabled ordinary collection still retains the exact SQLite floor, and missing capability or failed emission cannot revive a legacy gateway event or direct Provider metric. | root |

| `V-0178` | 2026-07-06 | `95ad8f412` | P4-WP01/P4-WP04/P6-WP03 guardrail-proxy authentication hard cutover | Full gateway suite (`109.286s`); focused authentication race (`14.169s`); generated registry check; scoped gateway vet; Grafana 14-dashboard/313-panel audit; 29 local-observability/Agent360 contract tests; gofmt and diff checks. | Guardrail-proxy authentication rejections now share the generated protected-boundary builder and emit exactly one mandatory `authentication.failed` compliance log plus the generated `defenseclaw.http.auth.failures` dashboard metric. The fixed `guardrail-proxy` route and closed reason vocabulary exclude request paths, query strings, bearer tokens, IP addresses, and user agents. Standard OTLP log fields retain exact W3C trace/span IDs while request correlation survives in the canonical envelope without trusting unauthenticated agent/session identity. A real OTLP log/metric test proves generated-only delivery and runtime detach cannot revive a legacy gateway event or Provider counter. | root |

| `V-0179` | 2026-07-06 | `e1d1507ac` | P4-WP02/P4-WP04/P5-WP04/P6-WP03 connector lifecycle hard cutover | Full gateway suite (`110.392s`); focused lifecycle/model/tool race (`42.908s`); observability runtime suite (`7.746s`); generated registry check; scoped gateway/runtime vet; Grafana 14-dashboard/313-panel audit; 29 local-observability/Agent360 contract tests; gofmt and diff checks. | Hook lifecycle/model/tool telemetry has no direct Provider, SDK lifecycle/model/tool span, legacy gateway lifecycle writer, or unbound fallback. A generated root `span.agent.transition` represents independently delivered session/subagent/turn/compact observations without inventing a completed agent invocation; it retains exact lifecycle/execution/root/parent/session/phase/cost facts, zero measured duration, and inbound W3C topology. Content-free bounded session state preserves PR #403 identity joins without retaining spans or generation leases. Generated tests replace v7-only topology assertions and prove missing-data fidelity, two-child inferred delegation, lifecycle-only start/end visibility, model/tool content and timing, token presence/absence, central redaction, dedupe, sampling/collection independence, and dashboard compatibility. | root |

| `V-0180` | 2026-07-06 | `a44397b58` | P4-WP02/P4-WP04/P6-WP03 HTTP inspect/guardrail metric hard cutover | Full gateway suite (`110.085s`); focused generated metric normal (`2.276s`) and race (`5.719s`) tests; generated registry check; scoped gateway vet; Grafana 14-dashboard/313-panel audit; 29 local-observability/Agent360 contract tests; gofmt and diff checks. | Every HTTP prompt, response, tool-response, tool-policy, policy-rule, and CodeGuard evaluation/latency metric uses generated v8 families without a Provider fallback. Clean `NONE` normalizes to `INFO`, latency retains fractional milliseconds, request/W3C correlation enters the canonical envelope, and CodeGuard adds its scanner dimension without double-counting inspect traffic. Connector attribution is explicit for policy-rule and CodeGuard series. The local compatibility projection retains the exact PR #412 Prometheus names and bounded dashboard labels, and affected dashboard provenance now identifies both generated metric owners. | root |

| `V-0181` | 2026-07-06 | `6f9c61981`, `f58d1c054` | P4-WP02/P4-WP04/P5-WP03..04/P6-WP03 HTTP inspect trace, finding, and Agent360 hard cutover | Full gateway (`119.301s`), telemetry, and observability suites; focused generated guardrail normal and race tests; scoped gateway/telemetry/observability vet; generated registry check; v8 inventory/spec gates; Grafana 14-dashboard/313-panel audit; 29 local-observability/Agent360 contract tests; Ruff, gofmt, and diff checks. | Prompt, completion, tool-response, and tool-call inspections emit one generated `span.guardrail.apply` with exact inbound W3C parentage and source-backed run/request/session/turn/agent/root/parent/lifecycle/execution/operation/policy/tool/evaluation correlation, canonical severity and decision facts, measured fractional latency, and no fabricated topology. Sampling or collection decline cannot revive a Provider span. CodeGuard matches now enter the shared structured finding pipeline before the span/alert metric, while the dead Provider inspect-span/runtime-alert APIs are deleted. The registry adds tool identity to guardrail spans and removes duplicated magic inventory totals. Agent360 now selects canonical guardrail raw/effective/decision fields, its packaged copy remains byte-identical, and Tempo vocabulary validation derives from the generated trace catalog instead of literal SDK callsites. | root |

| `V-0182` | 2026-07-06 | `bbe5dcd12`, `f6ad81c06` | P4-WP02/P4-WP04/P5-WP04/P6-WP03 proxy model trace and metric hard cutover | Full gateway (`145.888s`); full telemetry and observability packages; focused generated proxy normal (`1.340s`) and race (`9.432s`) tests; scoped gateway/telemetry/observability vet; generated registry check; v8 inventory/spec gates; Grafana 14-dashboard/313-panel audit; 50 local-observability/Agent360/Grafana contract tests; focused compiler unit-rule tests; gofmt and diff checks. | Proxy chat requests no longer create Provider agent/model SDK spans or mutate raw span bodies. One generated agent-to-model hierarchy preserves PR #403 W3C parentage and structured input/output/tool-call facts. Generated GenAI token and measured upstream-duration metrics remain independent of trace sampling; when a model span exists, every metric carries its exact trace/span IDs. Local dashboards retain their established PR #412 Prometheus series and now identify the generated owners. Remaining inbound unit tests validate closed rule shapes, unique aliases/families, and positive scales instead of hand-maintained expanded-case/global-family totals. | root |

| `V-0183` | 2026-07-06 | `fe1433640` | P4-WP02/P4-WP04/P5-WP04..05/P6-WP03 proxy guardrail telemetry and dashboard hard cutover | Full gateway (`112.337s`); full telemetry and observability/Galileo destination/integration packages; focused generated proxy normal and race (`20.389s`) tests; scoped gateway/telemetry/observability vet; generated registry check; v8 inventory/spec gates; Grafana 14-dashboard/313-panel audit; 50 local-observability/Agent360/Grafana contract tests (`7.85s`); gofmt and diff checks. | Each proxy prompt, completion, and proposed-tool evaluation now emits one generated `guardrail.evaluation.completed` log, one `span.guardrail.apply`, and generated evaluation/latency metrics with exact W3C/request/agent/model/evaluation/scan/policy joins. Only an actually applied block emits the linked mandatory enforcement companion; observe mode remains an allowed `would_block`, and the local floor omits reason, rule, prompt, and response content. Generated guardrail metrics survive trace sampling. Galileo-supported agent/model spans receive only the bounded decision event while the native span retains complete evidence. Provider guardrail spans/direct audit/metric duplicates are removed from the proxy path, and raw model/tool bodies remain owned by generated model telemetry plus central destination redaction. Overview and traffic panels combine the established inspect series with the generated proxy series; all dashboard contracts remain green. | root |

| `V-0184` | 2026-07-06 | `6d1a3cb72`, `3c56fe21d` | P4-WP02/P4-WP04/P5-WP04 generated inspector phase and finding-join hard cutover | Full gateway (`111.762s`); full scanner and generated runtime packages; focused gateway race (`20.215s`) and scanner race (`1.346s`); scoped gateway/scanner/runtime vet; v8 inventory/spec gates; Grafana 14-dashboard/313-panel audit; 50 local-observability/Agent360/Grafana contracts (`6.75s`); gofmt and diff checks. | Proxy inspection opens its generated apply span before work begins and nests generated regex, AI Defense, judge, policy, and mid-stream phase children on the same generation lease with fractional timing. Prompt/output evaluations retain exact agent/model W3C parents; sampled or disabled traces do not suppress durable logs or metrics. The inspector has no Provider stage/phase/panic callback. Evaluation and caller-minted scan IDs exist before trace completion, scanner persistence validates and reuses the scan UUID without serializing it into evidence, and applied-block tests prove identical evaluation/scan/rule/enforcement/trace identities across the span, evaluation log, enforcement floor, findings, and SQLite. Observe and already-delivered results remain explicitly unenforced. Recovered-panic metrics use the generated platform-health family, and Galileo-safe decision overlays remain on supported agent/model spans. | root |

| `V-0185` | 2026-07-06 | `74da983d2` | P4-WP02/P4-WP04/P5-WP03..05/P6-WP03 Cisco AI Defense metric, context, and dashboard hard cutover | Full gateway (`113.075s`); full generated runtime, local-observability destination, and Galileo destination packages; focused Cisco/phase normal and race (`13.101s` gateway, `3.798s` runtime) tests; scoped gateway/runtime vet; generated registry check; v8 inventory/spec gates; Grafana 14-dashboard/319-panel audit; 85 local-observability/Agent360/Grafana/upgrade tests (`8.18s`); gofmt and diff checks. | Cisco inspection now uses the caller's cancellable context and the generated `guardrail.ai_defense` CLIENT phase instead of creating a background-rooted `cisco.inspect.chat` span. Generated `defenseclaw.cisco_inspect.latency` and `defenseclaw.cisco.errors` records carry exact W3C/request/session/turn/agent/connector correlation and use the live phase's own graph lease, so reload cannot split one phase and its metrics across generations. HTTP attempts use canonical completed/rejected/failed/timed_out outcomes; handled rule-override rejection remains distinct from terminal errors. No production Cisco callsite retains a Provider pointer or direct Cisco metric API. The security dashboard exposes attempt rate, error count, completion rate, p95, per-outcome quantiles, and errors by code. Local-observability validation now derives panel/query inventories semantically rather than pinning global totals, while datasource, label, query, mirror, histogram, and compatibility checks remain enforced. | root |

| `V-0186` | 2026-07-06 | `9bef113d6` | P4-WP02/P4-WP04/P5-WP04/P6-WP03 proxy operational metric and streaming-dashboard hard cutover | Full gateway (`114.633s`); full generated runtime, local-observability destination, and Galileo destination packages; focused operational normal (`1.772s`) and race (`20.343s`) tests; scoped gateway/runtime vet; generated registry check; v8 inventory/spec gates; Grafana 14-dashboard/322-panel audit; 86 local-observability/Agent360/Grafana/upgrade tests (`8.60s`); gofmt and diff checks. | Proxy rate-limit breaches, forwarded-header counts, and SSE lifecycle/duration/byte observations now emit only generated families; the direct Provider calls are gone. Rate-limit routes collapse caller-controlled paths to a bounded passthrough token, header path/result labels use their closed compatibility vocabulary, and streaming uses attempted plus completed/blocked/failed/cancelled/timed_out canonical outcomes with fractional milliseconds and measured bytes. Stream metrics are registered after generated model construction and complete before model end on the model's exact graph lease, preserving W3C/model correlation and generation coherence across long responses and reload. Real middleware, Azure raw-forward, and completed/failed/blocked stream tests prove the callsites and existing PR #412 Prometheus names. The traffic dashboard now exposes transition rates and duration/byte quantiles by outcome. | root |

| `V-0187` | 2026-07-06 | `958fb3458` | P4-WP01/P4-WP04/P5-WP04/P6-WP03 recovered-panic, watcher-health, and runtime-dashboard hard cutover | Full gateway (`113.150s`); full generated runtime (`5.860s`), local-observability (`1.576s`), and Galileo (`0.742s`) destination packages; focused producer normal (`1.545s`) and race (`10.133s`) tests; scoped gateway vet; generated registry check; Grafana 14-dashboard/325-panel live audit; 59 local-observability/Agent360/Grafana/upgrade tests (`3.36s`); gofmt and diff checks. | Recovered hook panics no longer call the Provider: generated `defenseclaw.panics.total` preserves exact W3C plus request/session/turn/agent/tool/policy joins, while a content-free mandatory `subsystem.degraded` record persists the bounded `PANIC_RECOVERED` code without recovered values or stacks. Hook self-heal watcher errors/events and post-boot gateway reconnects now use generated watcher families through the process-owned generation-safe metric seam; successful hook repair retains connector/event/target dimensions and the real self-heal test proves the callsite. The runtime dashboard adds watcher-error, reconnect, and per-connector hook-heal panels using the established local-observability projection labels. | root |

| `V-0188` | 2026-07-06 | `e60ce387a` | P4-WP02/P4-WP04/P6-WP03 REST scanner failure hard cutover | Full gateway (`143.204s`); focused scanner normal (`1.535s`) and race (`7.935s`) tests; scoped gateway vet; gofmt and diff checks; unchanged 14-dashboard/325-panel local-observability contract inherited from V-0187. | Skill, plugin, MCP, and CodeGuard REST scan failures no longer call direct Provider counters. The generated `defenseclaw.scan.errors` family retains the exact established scanner/target/error labels plus source-backed connector, request/session/turn/agent, and W3C trace/span correlation. A real missing-path CodeGuard request proves the production callsite and now classifies the standard filesystem `no such file` result truthfully as `not_found` rather than `crash`; the existing scanners dashboard continues to use the generated local-observability projection without query changes. | root |

| `V-0189` | 2026-07-06 | `913c9b18c` | P4-WP02/P4-WP04/P5-WP04/P6-WP03 sidecar HTTP request metric hard cutover | Full gateway (`114.505s`); focused middleware normal (`1.214s`) and race (`6.261s`) tests; scoped gateway vet; generated registry check; Grafana 14-dashboard/325-panel audit and 24 dashboard contracts (`3.04s`); gofmt and diff checks. | The sidecar API middleware no longer calls the Provider request counter/histogram. One generated batch records count plus fractional-millisecond duration on the same runtime generation with canonical HTTP method, matched ServeMux route, status, and exact W3C/request/session/turn/agent correlation. Unknown methods collapse to `_OTHER`; unmatched raw paths—including token-bearing OTLP paths—collapse to `unmatched`, preventing secret leakage and attacker-controlled cardinality. Existing traffic panels retain their count, status, route, and duration queries through the generated local-observability projection, and dashboard provenance now names the generated owner. | root |
| `V-0190` | 2026-07-06 | `37c793e89` | P4-WP02/P4-WP04/P5-WP04/P6-WP03 external guardrail-event hard cutover | Full gateway (`113.845s`); focused generated normal (`1.749s`) and race (`23.876s`) tests; full observability (`1.370s`); generated registry check; scoped gateway/observability vet; Galileo compatibility, destination, and integration suites; Grafana 14-dashboard/325-panel audit and 39 dashboard/local-observability contracts (`9.41s`); Python syntax, gofmt, and diff checks. | `POST /v1/guardrail/event` no longer makes three duplicate audit-store writes or calls direct Provider guardrail/token instruments. The terminal API now requires a real caller-supplied `evaluation_id`, rejects non-guardrail direction/action/severity vocabularies, emits one generated `guardrail.evaluation.completed` log plus independently collected guardrail/GenAI metrics, and retains source model/reason unredacted until central per-destination redaction while keeping stderr redacted. Exact W3C/request/session/turn/agent/policy/tool/connector/evaluation joins survive in SQLite and metrics; no enforcement fact is inferred. The schema now admits `gen_ai.request.model`, sandbox/e2e callers follow the breaking contract, and overview/traffic panels retain their generated local-label queries with provenance covering both inline proxy and external API producers. | root |
| `V-0191` | 2026-07-06 | `4a216aa3d` | P4-WP02/P4-WP04/P5-WP04/P6-WP03 external OPA guardrail-evaluate hard cutover | Full gateway (`117.822s`); focused generated normal (`1.667s`) and race (`20.989s`) tests; full observability package tree (including Galileo, OTLP, Prometheus, local-observability, redaction, router, and runtime); generated registry check; scoped gateway/observability vet; Grafana 14-dashboard/325-panel audit and 39 dashboard/local-observability contracts (`7.97s`); Python syntax, gofmt, and diff checks. | `POST /v1/guardrail/evaluate` no longer makes three duplicate verdict/inspection rows or calls the direct Provider. It now requires a real caller-supplied `evaluation_id`, validates request and policy-output vocabularies, and emits one canonical terminal log, one aggregate evaluation metric, one latency metric, and a real measured `span.guardrail.apply` policy operation. Logs/metrics remain when sampling is off, while an unbound runtime writes nothing through a legacy path. A new schema-bounded detector-source list preserves up to eight unique contributors without inflating evaluation counts; the existing rule-ID promise is now schema-enforced at eight. Model, reason, rules, finding count, mode, strategy, and full PR #403/W3C correlation survive for central route redaction, while enforcement and would-block remain absent because this endpoint returns a decision but does not apply it. Overview/traffic queries remain stable through the generated local label projection and now name all three guardrail producers. | root |
| `V-0192` | 2026-07-06 | `c75c019a1` | P4-WP02/P4-WP04/P5-WP04/P6-WP03 policy API and reload hard cutover | Full gateway (`114.473s`); focused generated normal (`1.797s`) and race (`18.569s`) tests; full observability package tree including Galileo, OTLP, Prometheus, local-observability, redaction, router, runtime, and integration; generated registry and public-schema parity checks; v8 spec (`112` IDs) and inventory gates; scoped gateway/observability vet; Grafana 14-dashboard/325-panel audit and 39 dashboard/local-observability contracts (`6.06s`); gofmt and diff checks. | Admission, firewall, audit-retention, skill-action, and policy-reload API paths no longer call direct Provider spans, policy/admission/reload metrics, SLO metrics, or policy-decision logs. Each policy evaluation now owns one generated evaluation ID, one canonical `guardrail.evaluation.completed/failed` log, one measured `span.guardrail.apply` child when sampled, and exact generated policy metrics; source W3C/request/session/turn/root-agent/subagent/tool/policy correlation is retained, absent security severity remains absent, and technical failure does not fabricate a policy decision. Successful reloads persist mandatory `policy.updated` evidence before nonmandatory metrics; failed reloads emit the new mandatory `policy.reload.rejected` family, including the SQLite-only floor when log collection is disabled. Local dashboards now query policy/admission metrics and canonical guardrail/reload Loki streams while retaining compatibility streams. The live direct Provider producer inventory falls from 20 call sites to 8. | root |
| `V-0193` | 2026-07-07 | `15a179787` | P7-WP01 target migration protocol compatibility | 530 upgrade, migration, cursor, local-bundle, release-invariant, receipt, remediation, and release-smoke tests passed; 6 skipped and 7 subtests passed; Ruff check/format; source-stamped upgrade-manifest check; v8 spec (`112` IDs); diff check. The actual published `0.8.3` wheel was statically inspected and rejected for an unversioned source, while a fresh isolated checkout stamped with `scripts/stamp-version.sh 0.8.4`, built as a real wheel, passed the same preflight with required migration `0.8.4`. | A development CLI can no longer install published 0.8.3 and fail after shutdown on the new local-bundle keyword. The verified target wheel is inspected without execution; incompatible stamps, runner shapes, registries, manifest rows, and config capabilities fail before confirmation, backup, service stop, or replacement. The installed target runner conditionally passes the keyword only when supported, runs with isolated imports, and missing config is an explicit no-op. | root + upgrade-protocol review subagent |
| `V-0194` | 2026-07-07 | `ae6ad7530`, `6e6352807` | P4-GATE/P5-WP08/P5-GATE hard-cut and generated-footprint closure | `pytest -q` over four compiler/renderer suites: 270 passed in 688.37s; registry generator `--check`; full `go test ./internal/observability/... -count=1`; `go test ./internal/telemetry -count=1`; hard-cut checker plus four adversarial tests; Grafana packaged audit; Ruff, gofmt, and diff checks. | The compiler proves every native OTLP predicate, mapping, field, unit, outcome, ID, and order before compacting repetitive literals into one descriptor-derived startup expansion. The branch is about 338k added/54k removed (+284k net), the hard-cut scan passes 1,107 files, and all 14 dashboards/326 panels remain valid. | root + generated-duplication subagent |
| `V-0195` | 2026-07-07 | `b982a8cb0` release-stamped as `0.8.4` | P7-GATE cross-platform ordinary upgrade | Real release artifacts; 15 seeded historical baselines from `0.8.3` through `0.4.0`; macOS/arm64 and Linux/arm64; fresh target gateway health; SQLite quick-check; TUI metric refresh; exact config/environment/recovery/bundle/dashboard/volume assertions. | All 30 ordinary upgrade executions converged on the required `0.8.4` cursor and generated config v8. Secret promotion remained private, source backups were byte exact, the installed bundle matched the target while preserving custom files and all four history volumes, all 14 dashboard UIDs remained installed, and the newly installed target gateway was healthy. This closes the release gate without relying on an unpublished network release: the same locally built/stamped artifacts were served through the production upgrade protocol. | root |
| `V-0196` | 2026-07-07 | `4a2e611d5`, `10d417c60`, `055bfb477` | P3-WP04/P3-GATE complete Galileo runtime conformance | Focused normal and race integration; scoped vet; generated-profile/resource/scope/OpenInference/topology/event/link assertions; configured live Galileo HTTPS collector acceptance in 2.32s with no partial rejection or canonical failure. | One generation-owned W3C graph now exercises every generated Galileo family: workflow, agent, model, tool, retrieval, and judge. The fixture carries visible safe synthetic structured model input/output, tool arguments/results, reported usage, lifecycle and retrieval metadata, judge context, events, links, and fresh trace, span, conversation, request, run, turn, lifecycle, and execution identities for every live run. The exact projected content is asserted before delivery, and the same graph is accepted by the live collector rather than merely by a fake endpoint. Galileo's logged-in UI confirmed rich content on an actual runtime session and that the screenshot's empty session was the older shape-only fixture. The new unique live run was collector-accepted; UI indexing of that specific run was not observed within the short polling window and is not claimed. | root + Galileo subagent |
| `V-0197` | 2026-07-07 | `5485591a5` | P8-WP03 E2E-9 root/subagent and local-dashboard upgrade continuity | Focused producer normal and race tests; scoped vet; Ruff; Bash syntax; whitespace/diff checks; real Linux/arm64 release-stamped `0.8.4` ordinary upgrade against a running five-service local-observability stack. | The production generated runtime emitted 24 exact canonical records before and after upgrade for a root agent, direct and nested subagents, two turns, reported and unreported model usage, model request/response logs, nested tool, approval request/resolution, raw-block/effective-allow observe decision, compaction, resume, and operation completion before session end. The verifier proved exact W3C topology, lifecycle/execution lineage, monotonic phase sequences, no duplicate/unexpected records, usage presence semantics, and distinct pre/post traces. The ordinary upgrade retained byte-identical Prometheus, Loki, Tempo, and Grafana volume identities and converged to config v8. Historical and new records remained queryable (`24` each); all 14 dashboard UIDs and 326 panel inventories evaluated without query errors. Target bundle configs/scripts were `0644`/`0755` and its private manifest remained `0600`. | root |
| `V-0198` | 2026-07-07 | `dfd6365e6` | P8-WP03 E2E-1 through E2E-8 acceptance closure | Focused E2E normal/race suites plus destination, runtime, CLI-limit, and canary tests. | Empty configuration collected all 14 buckets and all three signals locally exactly once without optional exporters; the applied high-finding case fanned independently through real SQLite, JSONL, Splunk, and OTLP adapters with destination-specific redaction; disabled model/tool logs remained absent while independent metrics survived; one rich six-family graph reached Galileo and generic OTLP without cross-export; no-policy destinations used capability defaults while explicit sibling policy stayed isolated; count/byte pressure proved newest-drop, FIFO recovery, and blocked-destination isolation; retention and runtime-graph continuity passed. The Galileo batch used the compiler-owned one-second preset so the strict same-request six-span canary remained coherent under race. |
| `V-0199` | 2026-07-07 | `dfd6365e6`, `9758e6d08` | P8-WP01/P8-WP02 security, fuzz, race, cardinality, and performance acceptance | Six ten-second fuzz runs; deterministic security corpus; normal/race network, redaction, delivery, destination, cardinality, queue, and strict-canary suites; three-sample `-benchmem` baselines on Apple M4 Pro/darwin-arm64. | Fuzzing completed config compilation (60,006 executions), selectors (11,952), canonical values (133,799), inbound OTLP no-raw-bypass (56,315), dynamic redaction (91,005), and malformed/Unicode redaction (111,369) without failures. The security corpus passed critical 13/13, high 12/12, benign 9/9, and deterministic adversarial 2/2; three model-gated cases skipped by design. Median disabled trace/metric/log costs were 104.5/46.5/1,013 ns; local-only and local-plus-three-sink logs were 638,489/776,479 ns; metric record was 82,231 ns; plain/nested sensitive redaction was 114,421/135,330 ns; rich six-span projection was 6.86/7.92/11.13 ms for one/two/four destinations; the reaper processed exactly 1,000 rows in 6.74 ms. These are the approved first canonical v8 baselines because `main` has no comparable runtime. |
| `V-0200` | 2026-07-07 | `9758e6d08` | P8-WP03 final integration, upgrade, and dashboard command suite | Exact section-15 normal and race Go commands; E2E observability/v8; full CLI; vet; schema, Grafana, Agent360, upgrade-manifest, repository, and telemetry checks; isolated release-stamped `0.8.4` upgrade matrix; live local-observability inventory. | All Go suites passed (including the sequential full gateway run at 123.421s and audit race at 525.291s); E2E passed; CLI completed 6,148 passed, 12 skipped, and 324 subtests in 1,933.04s; vet and every repository drift gate passed. Fifteen historical baselines from `0.8.3` through `0.4.0` upgraded on macOS/arm64 with exact config-v8 cursor, secret, backup, bundle, custom-file, volume, SQLite, gateway-health, and TUI assertions; V-0195 supplies the matching Linux/arm64 matrix. The live five-service stack recovered from a transient Loki readiness event without restart, then all 14 dashboards and 326 panels compiled with zero query errors and packaged inventory parity. |
| `V-0201` | 2026-07-07 | `9758e6d08` | P8-WP04 hard-cut, generated-drift, secret, and review-footprint audit | `make check`, `make telemetry-check`, schema/spec/registry/dashboard checks, hard-cut adversarial tests, branch secret scan with manual classification, and `origin/main...HEAD` diff inventory. | The hard-cut checker accepted 1,121 source files with no live v7/duplicate producer path; telemetry generation was deterministic and owned outputs were current; all 112 normative decision IDs and 14 dashboards/326 panels remained valid. Twenty-nine candidate secret patterns were confined to synthetic detector, redaction, scanner, proxy, hook, telemetry, test, documentation, and example fixtures; no live credential was present. The branch contains 922 changed files, +343,723/-50,091 (net +293,632), below the requested approximately 300k-net target without removing functionality. |
| `V-0202` | 2026-07-08 | `c53d2b075` | P8-WP05/P8-GATE final PR, review, live destination, cross-platform, and CI/CD acceptance | PR #451 review-thread audit; live logged-in Galileo trace `b2670d3a487c90b0509a30004fce68cc`; `make telemetry-check`, schema/spec/hard-cut/dashboard/upgrade-manifest gates; focused normal/race Galileo, OTLP, local-observability, CLI, Windows custody, and merged-main content-omission tests; local `make test` Python phase (6,153 passed, 12 skipped, 324 subtests) plus explicit 30-minute gateway race rerun; GitHub CI `28921755951`, docs `28921755988`, and connector `28921755993`. | Galileo rendered workflow request/response, tool arguments/results, and correlated W3C/tool/project/run/status parameters instead of the earlier shape-only empty UI. Generic/local OTLP fixtures obey the strict endpoint/TLS contract, split Galileo batches retain the complete seven-span graph assertion, and local release-canary pairing stayed exact across 40 focused plus 10 full-package race repetitions. Native Windows correlation-key custody and SQLite sidecar ACL tests passed, and all 27 Linux/macOS/Windows connector cells passed. Merging current `main` retained PR #452's unconditional content omission from stderr-backed `gateway.log` while canonical destination projections remain per-route configurable. All three PR threads are resolved; the PR is mergeable; all 26 core CI jobs, docs, and the connector report are green. The current-main diff is 931 files, +345,274/-50,236 (net +295,038), with no required follow-up. |
| `V-0203` | 2026-07-10 | working tree after `c841ccbf6` | P7/P8 release-safety audit and architecture correction | Live PR/head/check inspection; published `0.8.3` controller rehearsal; targeted Claude Opus 4.8 high architecture review; focused shell resolver tests; controller health review | The prior green matrix applies to remote head `ce29be4239474009f6c9ea61080ffd58a620dab7`, not the current local work, and PR #451 reports a conflicting/dirty merge state. A real published `0.8.3` controller can complete target migration yet time out its in-process health check and still report success; it also cannot safely preserve native Windows DACLs. Therefore V-0195/V-0200/V-0202 remain historical converter/runtime evidence but no longer close publication. C-0041 reopens P7/P8 until the published bridge, fresh-process staged handoff, exact rollback, Windows resolver/DACL transaction, sealed-artifact gates, and exact-head CI pass. |
| `V-0204` | 2026-07-15 | working tree above `28b4dc9c0` | P7-WP01..06/P7-GATE published staged-release closure | Successful POSIX-only `0.8.4` release run `29266150106` and immutable asset audit; public-release Linux and macOS staged upgrade, fault-injected rollback, immediate retry, and fresh-install runs; manifest/protocol/refusal/lease/SQLite/correlation gates; Windows installer/refusal static tests and cross-compilation; unsigned-macOS fallback audit. | The published config-v7 `0.8.4` bridge advertises protocol 2 and contains verified Linux/macOS artifacts with no Windows binary, gateway, refusal binary, or Windows SBOM. The authenticated target-release shell resolver selected the bridge and re-executed under a fresh controller before the `0.8.5` migration. Both live hosts reached healthy v8 with all ten correlation tables and preserved history; deliberate post-migration failure restored healthy `0.8.4` state with no journal or lease residue, and the same command then succeeded. Windows now truthfully refuses before service stop or installed-state mutation because no bridge was published. This supersedes V-0203's proposed native-Windows bridge obligation and closes P7; exact-head PR #490 CI remains the P8 completion gate. |
| `V-0205` | 2026-07-15 | final pre-commit candidate above `28b4dc9c0` | P8-WP01..04 local and live final-candidate closure | `go test ./...`; sequential `go test -race` over audit, gateway, observability, daemon, and CLI; `make check`; spec/schema/telemetry/version/manifest/dashboard/docs gates; `go vet ./...`; macOS packaging and unsigned application build; 7,096-test CLI corpus with exact reruns of the two corrected static failures; targeted Claude Opus 4.8 review; final scope/secret/governance audit; local, remote macOS, and Linux runtime canaries. | The complete v8 plus durable correlation tree is locally and live-host verified. Hook/native exact joins, replay/conflict/ambiguity semantics, restart persistence, correlation APIs, ten-table SQLite integrity, cold-row preservation, redaction, custody, fresh install, staged upgrade, rollback, and retry passed. Review findings for stale-process identity and restart preflight were fixed and live-tested with zero extra children or service interruption. The frozen pre-commit diff is 1,211 files, +414,671/-57,685 versus current `main`; no accidental artifact, credential, governance mutation, Windows bridge claim, or unsigned-macOS fallback regression remains. Exact-head PR #490 CI is the only remaining P8-WP05/P8-GATE condition. |
| `V-0206` | 2026-07-15 | `a8dabc35f864d65e31f1c02e1cced6073cf4a2f3` | P8-WP03..05/P8-GATE exact-code-head acceptance | GitHub CI `29402556121`; Connector Live E2E `29402556155`; Docs site `29402556169`; macOS App `29402555976`; native Linux/arm64 daemon identity stress; focused config recovery regressions; targeted Claude Opus 4.8 reviews. | CI completed 27/27 jobs successfully, including full Go coverage, unified `make test`, Python coverage, Windows refusal/installer, local-observability, upgrade, schema, and release gates. Connector Live E2E completed 27 OS/connector contract cells plus selector/report successfully, with only the expected template job skipped; the docs static build passed with PR deployment skipped as designed; both macOS build/test and license jobs passed. Native Linux/arm64 passed 1,000 normal and 1,000 race contaminated-identity repetitions plus full daemon cases. The exact code diff is 1,211 files, +415,040/-57,685 against base `7544f6157a9fea37ef7f1230495f35582b4ed833`. POSIX-only `0.8.4`, the signed PowerShell resolver, Windows pre-mutation refusal, and the unsigned macOS fallback remain intact. | root + CI + live-validation/review subagents |

Final integration requires, at minimum:

```bash
go test ./internal/config ./internal/redaction ./internal/gatewaylog ./internal/audit ./internal/telemetry ./internal/scanner ./internal/gateway -count=1
go test -race ./internal/config ./internal/redaction ./internal/gatewaylog ./internal/audit ./internal/telemetry -count=1
go test ./test/e2e -run 'Observability|V8' -count=1
uv run python -m pytest cli/tests -q
go vet ./...
make check-schemas
make check-grafana-dashboards
uv run python -m pytest cli/tests/test_agent360_dashboard.py cli/tests/test_grafana_dashboards.py -q
make check-upgrade-manifest
make upgrade-smoke-matrix ARGS="--target-version X.Y.Z --baseline-mode seed"
python scripts/check_grafana_dashboards.py --live --inventory --require-packaged
```

The last command runs against a started local-observability stack with generated
root/subagent traffic. Galileo validation additionally exercises eligible
agent/model/tool/retriever/workflow/judge spans, partial success, delivery counters,
and exact-trace canary acknowledgement against its conformance harness.

## Decision, Blocker, and Change Ledger

| ID | Date | Type | Affected work | D-/S-/P- IDs | Resolution or required spec change | Status |
|---|---|---|---|---|---|---|
| `C-0001` | 2026-07-02 | setup | `P0-WP01` | D-022, P-045, P-046 | Track package in repository and use this root execution ledger | complete |
| `C-0002` | 2026-07-02 | gate | `P0-WP02..03`, `P0-GATE` | D-001..022, S-001..012, P-001..047 | Approve the mechanically validated contract after current-state inventory, compatibility, and two CodeRabbit review passes | complete |
| `C-0003` | 2026-07-02 | architecture | `P1-WP03`, `P5-WP01..02` | D-019, P-010, P-030, P-046 | Python validates source/schema semantics and renders the masked source; canonical effective compilation and registered action/event selector validation always run in Go. P5's sole registry compiler will generate the shared Python selector constants instead of introducing a hand-maintained duplicate in P1. | complete |
| `C-0004` | 2026-07-02 | dependency | `P1-WP04`, `P1-GATE`, `P5-WP06`, `P7-WP01` | D-020, P-030, P-044, P-046, P-056, P-057 | Split converter-engine/API completion from generated family-selection integration: P1 may unblock canonical runtime work only after strict typed fail-closed engine tests; P5's sole compiler owns the complete compatibility artifact and mapping goldens, which remain mandatory before automatic migration or release. | complete |
| `C-0005` | 2026-07-02 | gate | `P1-WP04`, `P1-GATE`, `P2-WP01`, `P5-WP06`, `P7-WP01` | D-020, P-030, P-044, P-046, P-056, P-057 | Close P1 at `c29cdddc4` after independent/subagent/Claude review and exact-HEAD gates. The immutable plan and converter engine/API may unblock P2; generated v7 family eligibility, complete migration goldens, PR #403/#412 dashboard equivalence, Galileo conformance, and activation remain mandatory in their existing P5/P6/P7 work packages. | complete |
| `C-0006` | 2026-07-02 | security architecture | `P2-WP01..02`, `P5-WP02` | D-001, D-016, P-042, P-058 | Public generic construction requires complete exact leaf classes and cannot assert schema derivation or mandatory state. Current log metadata/builders resolve registered producer kind/key/typed facts internally; disabled-floor construction uses a separate content-free authenticated marker accepted only by local SQLite. Generated event-to-bucket ownership, family outcome subsets, schema-derived classes, and richer reviewed floor bodies remain solely P5 registry outputs. | complete |
| `C-0007` | 2026-07-02 | privacy architecture | `P2-WP03`, `P5-WP02` | D-011, P-059, P-060, P-061 | Custom redacting profiles cannot rewrite metadata/schema-approved identifiers. Delivery projections retain only surviving original field-class provenance, omit removed object pointers, and recursively prune containers emptied solely by descendant removal; array indices remain stable through `null` without synthesizing classes. Object member names are schema-owned vocabulary, while dynamic names are classified values. Canonical records retain their complete immutable maps. | complete |
| `C-0008` | 2026-07-02 | gate | `P2-WP03`, `P2-WP04` | D-009..012, P-026, P-038, P-051, P-059..061 | Close the central redaction work package after exhaustive detector grammar coverage, cross-language hash parity, secure fixed key custody, pure legacy-v7 compatibility, immutable per-route projection, structural-name protection, fail-closed plan adaptation, independent adversarial review, Claude review/fixes, and exact-HEAD normal/race/vet/schema/spec gates. This unblocks SQLite projection persistence but not producer cutover or remote adapters. | complete |
| `C-0009` | 2026-07-02 | storage architecture | `P2-WP04`, `P2-WP05` | P-021, P-045, P-047 | Preserve v7 audit column meanings while adding distinct exact-projection/hash/HMAC fields; cut retained raw judge bodies over once to an owner-only dedicated database with crash-resumable verification, normalized indexed instants, no audit.db writer fallback, and authoritative-first compatibility reads. Unix/Windows main and auxiliary SQLite paths fail closed across ownership, link/reparse, ACL, mutable-ancestor, and stale-sidecar hazards. The global age reaper and cutover-marker cleanup remain the next P2-WP05 scope. | complete |
| `C-0010` | 2026-07-03 | gate | `P2-WP04`, `P2-WP05`, `P2-GATE` | D-013..016, S-012, P-005..008, P-021, P-045, P-047 | Close P2-WP04 at `39fe480d1` after implicit all-bucket SQLite persistence, exact runtime-graph projection binding, hardened readiness/path/sidecar handling, correlation-key integrity, protected alert receipts and streaming replay, post-lifecycle health dispatch, adversarial Claude/storage review, one consolidated CodeRabbit pass, and final normal/race/vet/cross-platform/repository gates. P2-WP05 now owns the global age reaper, protected-state exclusions/capacity health, atomic graph reload/swap, and cleanup markers; representative producer cutover remains P2-GATE. | complete |
| `C-0011` | 2026-07-03 | gate | `P2-WP05`, `P2-GATE`, `P3-WP01` | D-017..019, S-012, P-021, P-045, P-047 | Close P2-WP05 at `8d26ebd4f` after exact global retention, readiness-gated six-hour scheduling, immutable atomic runtime generations, producer leases, rejection-safe reverse cleanup, lossless ordered/reentrant mandatory reporting, exact opened-SQLite path binding, graph-bound local persistence, and retention activation before producer readiness. Runtime exclusively owns controller lifecycle and shutdown is bounded/retryable without returning store ownership early. P2-GATE now owns only representative producer cutover/exactly-once failure injection; optional destinations remain rejected until P3. | complete |
| `C-0012` | 2026-07-03 | gate | `P2-GATE`, `P3-WP01..05`, `P7-WP01` | D-004..006, D-013..019, S-012, P-021, P-032, P-045, P-047 | Close P2 at `1e8463ad6` after a real-runtime Sidecar start/stop integration proves canonical local persistence precedes optional structured fanout, exactly-once writes, bounded cancellation-independent shutdown, no legacy fallback after a v8 decision, and exact unbound compatibility. This is a representative seam, not early global activation: the CLI still owns production runtime construction/close and config-version cutover in P7; remote destination delivery and canonical metrics remain P3; producer-wide migration remains P4. Existing PR #403 identifiers are not fabricated for process boot events, and PR #412 packaged dashboard contracts remain unchanged. | complete |
| `C-0013` | 2026-07-03 | architecture | `P3-WP01..02` | D-005, D-006, D-009, D-010, D-019, P-003, P-039, P-054, P-058..063 | Optional log/trace delivery is generation-owned and independently bounded by record count and immutable projected bytes; push encoding has a separate count/encoded-byte ceiling. Queue overload drops only the newest attempted item after local persistence. All adapters consume their destination projection only, with Splunk aliases as a projection-to-wrapper transform. The v8 network path has no ambient bypass: private/loopback and CGNAT opt-ins are destination-local, while link-local, metadata, translation, unspecified, multicast, and reserved targets remain blocked at validation, activation resolution, and every dial. | complete |
| `C-0014` | 2026-07-03 | architecture | `P3-WP01..02`, `P5-WP03` | D-005, D-006, D-019, S-005..011, P-003, P-010, P-039, P-054, P-062 | Optional destination workers and trace providers are immutable runtime-graph components created inactive, activated only for an accepted generation, drained during reverse teardown, and never installed as OpenTelemetry globals. Local SQLite commits precede optional enqueue; optional overload or delivery failure cannot change the producer result. Trace data is bounded at mutation time, canary bypass is exact and sampling-time only, and trusted process identity overrides configured resource values. | complete |
| `C-0015` | 2026-07-03 | delivery semantics | `P3-WP01..02` | P-062, P-064 | Distinguish pre-write transient failures from post-write acknowledgement ambiguity. Both may retry within the same bounded delivery sequence using identical projected bytes and record identity, so no retry can re-run redaction or recover canonical data; only the ambiguity window permits a downstream duplicate. Authentication, malformed payload, and unsafe endpoint outcomes remain terminal. | complete |
| `C-0016` | 2026-07-03 | security compatibility | `P3-WP01..02` | P-039, P-050, P-066 | Preserve intentional plaintext HTTP collectors rather than introducing a new breaking transport ban, but surface every resolved bearer, Splunk token, authentication-like header, or secret-backed header crossing that boundary through one content-free warning/audit. HTTPS and unauthenticated HTTP remain unchanged; no endpoint, header, reference, or secret enters the warning. | complete |
| `C-0017` | 2026-07-03 | OTLP retry and bounds | `P3-WP02..04` | P-032, P-039, P-048, P-062, P-064 | Keep trace/metric retry inside the protocol-specific OTel SDK layer where HTTP/gRPC outcomes remain classifiable and the exact encoded request is reusable; do not add a second processor retry that could replay authentication, permanent, or partial-success accepted subsets. DefenseClaw owns the count/byte trace queue and request preflight, while metric backpressure stays SDK-owned. Guarded-dial violations are terminal and externally retain `unsafe_endpoint`. | complete |
| `C-0018` | 2026-07-03 | generation architecture | `P5-WP01..02`, `P4-WP01..03` | D-019, P-010, P-030, P-040, P-046, P-056, P-061 | Close P5-WP01 at `d1ed5278d` with a manifest/provenance compiler only. Before P5-WP02 renders or activates runtime consumers, extend the compiler IR to preserve every already-validated requirement, constraint, privacy class, metric/span contract, producer mapping, example, and compatibility binding. Candidate bundle/catalog/builders land before one atomic generated-authority cutover; P4 producer migration depends on those generated builders/projections and cannot start solely from P3-GATE. | complete |
| `C-0019` | 2026-07-03 | registry inheritance and public-view authority | `P5-WP02..05` | P-042, P-046, P-058, P-061, P-067 | Resolve registry groups once through the normative non-weakening DAG/body-transposition contract and materialize the result for every renderer; no renderer reimplements inheritance. The global outcome vocabulary is not a family default, so exact family subsets must replace the current broad placeholder before builders activate. Candidate bundle/catalog outputs may land while the twenty-one old schema paths remain authoritative, but generated-path cutover additionally requires a digest-pinned typed `public_views` baseline with complete field dispositions, identity/dialect/reference closure, field classes, fixtures, mirrors, embeds, and installed-wheel parity. | complete |
| `C-0020` | 2026-07-03 | public-schema baseline security | `P5-WP02..05` | P-046, P-061, P-068 | Build the twenty-one-view migration baseline only from one full pre-cutover Git commit with lossless numeric-token-preserving canonicalization, complete mixed-dialect offline closure, bounded strict parsing, and domain-separated commit/tree/blob/source/document provenance. Public generated JSON and the generated manifest carry matching authority markers; baseline tooling rejects either marker, so generated views can never recursively become migration input. Current public files remain authoritative until typed field and dynamic-object dispositions, full-document patches/digests, fixtures, mirrors, embeds, wheel copies, and one atomic cutover all pass. | complete |
| `C-0021` | 2026-07-03 | structural registry authority | `P5-WP02..03` | P-058, P-061, P-069, P-070 | Make one closed typed registry contract authoritative for the canonical envelope, correlation, provenance, signal payload arms, trace resource/scope/status/events/links, metric samples, privacy classes, conditions, lifecycle phase/code values, and canonical-to-OTLP representations. The source/compiler/runtime foundation and immutable materialized view were verified through `99f8d9949`; commits through `f980f102b` now generate the schemas, builders, catalogs, fixtures, and projections from that same view and publish them through one manifest-owned authority. | complete |
| `C-0022` | 2026-07-04 | runtime capability and trust boundary | `P5-WP02..05` | P-058, P-069 | Registry and dependency-lock files remain source authority and snapshot provenance remains compiler-owned. The binary separately declares one shared implemented semantic-profile capability consumed by config and Galileo, so a same-ID tuple mutation fails closed and a new tuple requires a new ID plus runtime support. Schema-derived log builders cannot pass a raw mandatory Boolean; they must supply a private exact-family contract whose mandatory state was catalog-derived. | complete |
| `C-0023` | 2026-07-04 | generated-output transaction boundary | `P5-WP02..06` | P-030, P-046, P-061, P-069 | Candidate and cutover renderers publish exact schema and Go output allowlists through one manifest-last logical transaction. Ownership is content-addressed by manifest digest, marker, and mode; the seven internal Go paths are an exact all-or-none desired and prior inventory; journals and private staging are owner-only, bounded, canonical, crash-recoverable, and identity-bound. All-or-none means the validated candidate and final committed checked-in state, not a physical multi-file snapshot for concurrent readers. The advisory lock serializes writers only; `--write` requires a quiescent worktree, and an interruption must be recovered before Go, language-server, or other direct readers resume. All transaction-owned reads, mutations, cleanup, and linked-worktree resolution fail closed through descriptor-relative no-follow operations. Python 3.10-3.14 use the same security semantics, including a descriptor-relative recursive cleanup fallback. | complete |
| `C-0024` | 2026-07-04 | generated-builder source grammar | `P5-WP02..06` | P-069, P-070 | Keep P-069 as the sole canonical record authority and add only the missing generation facts through one closed P-070 grammar: dynamic mandatory rules, structured types/bindings, deterministic Go symbols, and explicit example builder contexts. Commits `dfba24401` through `793473271` close and materialize that grammar, the mandatory-rule catalog, candidate trust boundary, reproducible 1,773-row symbol table, enriched API/file plan, exact seven-file renderer, transactional publication, first/repeat adoption, and typed public-view compiler. Commits `385949ebe`, `8d064149a`, and `f980f102b` render marker-only public views, execute legacy parity, widen the manifest to the exact live allowlist, and complete the atomic public/consumer cutover. Compatibility-only and removed identities continue to require no builder. | complete |
| `C-0025` | 2026-07-05 | rich-trace successor contract | `P5-WP03..05`, `P4-WP02` | D-019, P-010, P-030, P-040, P-046, P-056, P-061, P-062, P-069, P-070 | Preserve the immutable 1,773- and 1,778-row generated-ABI baselines and add separately content-addressed successors for canonical trace context, exact resources, and the generated two-span canary contract. The final additive ABI has 1,785 declarations, 331 attributes, nine conditions but only seven independent caller condition facts, derives both reported-cost and canary enablement from their required/optional source Booleans, and forbids canary-only fields unless the marker is explicitly true. Every generated span input owns optional canonical W3C trace state and a required complete OTLP flags word; runtime handoff consumes those record values, rejects reserved runtime bits, and uses the SDK callback only for exact parity. The five core runtime span families retain the shared PR #403 context/cost contract; native `span.agent.transition` remains Galileo-ineligible unless its missing invoke-agent semantics genuinely exist. Generated construction, exact process-stable resource ownership, the generation-owned count/byte-bounded ended-span handoff substrate, the Tempo-preserving Agent360 exclusion branch, and the lease-bound exact agent/model canary are complete at `8d6e0ae448865270261c975e167c8b8c82f5b297`. Canonical destination activation separately remains gated on the PR #403 producer, Galileo, PR #412 projection, P3, and P4 work packages; this separation removes the prior circular dependency without weakening a release gate. | complete |
| `C-0026` | 2026-07-05 | exact resource ownership | `P3-WP02..05`, `P5-WP03..05`, `P4-WP02` | D-008, D-019, P-010, P-030, P-040, P-046, P-056, P-061, P-062, P-069, P-070 | `resource.core` owns one generated sealed, bytewise-sorted custom string-member value under the existing 64-entry/16-KiB configuration contract. It is the sole runtime custom-resource representation and is merged with fixed core plus exactly three policy-controlled derived aliases. One generation snapshot supplies canonical traces, SDK traces/metrics, OTLP logs/traces/metrics, and Galileo; trace handoff uses exact-set resource parity and rejects nonzero local dropped-resource claims. The generated complete-resource validator protects generic projection boundaries without becoming an alternate constructor. `discovery.source` is not a v8 process resource. Native Prometheus retains its generated label catalog, while local Collector promotion preserves process-stable resources without making them dashboard-required and rejects normalized-key collisions. The Collector derives legacy environment from canonical before the local fallback. Implemented and verified at `e39a7f84485691a506316947c220d0284b293170`. | complete |
| `C-0027` | 2026-07-05 | producer fact-fidelity gate | `P4-WP01..03`, `P5-WP04..05` | D-001, D-014, D-019, P-010, P-030, P-040, P-046, P-061, P-069, P-070 | Producers were migrated only after their generated families preserved the actual source facts; no path invented finding status, evaluation/enforcement IDs, state transitions, topology, or completion timing. Scan/finding, asset, egress, discovery, hooks, native OTLP ingest, judge, and administrative paths now use generated v8 ownership, with P-076 retaining an outcome-free observation where the OpenClaw wire cannot prove full topology. | complete |
| `C-0028` | 2026-07-06 | inbound producer architecture | `P3-WP02`, `P4-WP03`, `P5-WP01..03` | D-023, P-058, P-061, P-069..075 | Close the accepted-record ambiguity with one registry-owned compiler-expanded binding catalog rather than arbitrary OTLP pass-through. Every imported occurrence is rebuilt through a private non-floor family context with local field classes/provenance, per-target collection, SQLite-first log persistence, central route redaction, and one local ID; exact connector/GenAI derivations remain explicit. Preserve original semantic process identity separately from immediate forward instance/destination/hop metadata, and terminate cross-instance cycles after four completed legs. | complete |
| `C-0029` | 2026-07-06 | producer fact-fidelity prerequisite | `P5-WP04`, `P4-WP04` | D-001, D-022, P-046, P-069, P-070, P-076 | EventRouter's three Bifrost lifecycle callbacks must not use the generated PR #403 lifecycle-span API: start and terminal frames are separate deliveries, and the current frame lacks the stable lifecycle/execution plus explicit current/root/depth/type facts that family requires. Resolved at `435552bd5` with the narrower outcome-free `log.agent.run.observed` family: each callback emits only literal validated source facts through one request-scoped runtime call, retains no cross-delivery span/context or inferred identity, and no longer uses a legacy owner. | complete |
| `C-0030` | 2026-07-06 | hard-cutover clarification | `P4-WP01..04`, `P7-GATE`, `P8-WP04` | D-020, D-024, P-017..025, P-044, P-046 | The release target is not a dual v7/v8 runtime. Retain v7 only inside the pure upgrade converter/read-only preview, historical fixtures, exact backups/rollback, previous-binary recovery, and generated additive consumer projections. Delete every live gateway/sidecar/producer/router/destination/ordinary-writer v7 branch, direct provider/sink/bridge path, fallback, and runtime-only compatibility test; a target ordinary command presented with v7 source refuses it and points to `defenseclaw upgrade`. Replace earlier “preserve true v7 runtime” wording as transitional development evidence, not final acceptance. | complete |
| `C-0031` | 2026-07-06 | compiler maintainability | `P5-WP02..08`, `P8-WP04` | P-046, P-069, P-070 | Exact semantic/security invariants, deterministic generation, owned-output drift, pinned upstream inputs, lifecycle compatibility, and safety bounds remain mandatory. Current repository-wide inventory totals, declaration/file partitions, compiler-output digests, expanded producer-row totals, and full symbol-table baselines are derived facts and cannot be hand-maintained acceptance knobs. One explicit source-owned output list replaces the generated ownership manifest; producer-to-identity-set relationships resolve from compact indexed tables rather than copied Cartesian rows; descriptor-derived static gates replace duplicated builder inventories; reproducible reports and corpora are validated in memory. | complete |
| `C-0032` | 2026-07-06 | gateway producer cutover | `P4-WP01..04` | D-019, D-024, P-010, P-040, P-046, P-069, P-070 | Direct gateway policy-decision, audit-database-error, and Codex-notify Provider emissions are removed at `59f62846c`. Runtime asset/MCP decisions rely on their canonical evaluation/finding path; database failures and Codex notifications use generated registry metrics with exact correlation and malformed-notification visibility. The destination-specific generated canary remains intentionally direct. This closes only direct gateway callsites: scanner-mediated finding emission and other package-level Provider paths remain subject to the producer-wide hard-cutover gate. | complete |
| `C-0033` | 2026-07-06 | runtime-finding cutover | `P4-WP01..04`, `P6-WP01` | D-001, D-019, D-024, P-010, P-040, P-046, P-069, P-070 | Hook, proxy, tool-call, and asset-policy runtime findings use the audit logger's single generated v8 scan boundary at `de4375471`; no live gateway JSONL or Provider finding fanout remains. One evaluation/scan identity joins forensic rows, finding and scan logs, generated metrics, and matching W3C trace/span context, while mismatched trace/span pairs fail to trace-only correlation. Existing dashboard family names and scanner/connector/severity/rule dimensions remain exact; the 14-dashboard/325-panel audit and 38 dashboard contract tests pass without query changes. Watcher/classic scan and remaining package-level Provider paths stay in the active producer-wide hard-cutover gate. | complete |
| `C-0034` | 2026-07-06 | operational producer cutover | `P4-WP01..04`, `P6-WP01`, `P8-WP02` | D-001, D-019, D-024, P-010, P-017..025, P-040, P-044, P-046, P-069, P-070 | Audit Logger actions/activity/alerts/sink health, watcher admission and rescan signals, judge-body persistence/cache metrics, and webhook delivery/circuit metrics use generated v8 ownership at `c011cee05`. Sticky runtime authority prevents detach or export failure from resurrecting Provider, audit-sink, structured-gateway, or gateway-JSONL duplication. Forensic scan/body stores, mandatory SQLite floors, stable machine failure codes, canonical webhook outcomes, keyed endpoint identifiers, existing metric names/dashboard labels, and exact W3C plus request/session/turn/agent/tool/policy/connector correlation are preserved. The watcher Provider constructor/setter seams are deleted. Full audit/watcher/gateway suites, registry validation, affected-package vet, and the 14-dashboard/325-panel audit pass. Policy and sandbox are closed by C-0035; startup, SQLite-contention, CLI-watchdog, and scanner compatibility boundaries stay in the active producer-wide gate. | complete |
| `C-0035` | 2026-07-06 | policy, admission, sandbox, and redaction cutover | `P4-WP01..04`, `P5-WP04..05`, `P6-WP01` | D-001, D-019, D-024, P-010, P-017..025, P-040, P-044, P-046, P-069, P-070 | Commit `d7d3385dd` removes direct Provider span/metric/event ownership from the policy engine, enforcement admission wrapper, watcher admission decision, and OpenShell wrapper. Watcher admission uses the generated `span.guardrail.apply` family and propagates its evaluation ID into scan/finding correlation. OpenShell uses an audit-owned narrow interface for the exact existing exit metric, a canonical platform-health occurrence, and a compliance activity for successful policy reload. Canonical audit, activity, scan/finding, egress, and health producers now retain source values until the immutable runtime creates per-destination redaction projections; only the unreachable-after-authority compatibility fallback sanitizes at the producer. The generated platform-health schema adds one bounded sensitive `defenseclaw.health.error_summary`, so unredacted routes retain actionable diagnostics while strict/content routes transform the same source independently. Registry drift, all observability packages, audit/sandbox/policy/enforcement/watcher suites, and affected gateway lifecycle tests pass; the one full gateway failure caused by an obsolete pre-route-redaction expectation was corrected and its focused regression passes. | complete |
| `C-0036` | 2026-07-06 | scanner and SQLite producer cutover | `P4-WP01..04`, `P6-WP01`, `P8-WP02` | D-001, D-019, D-024, P-010, P-040, P-044, P-046, P-069, P-070 | Commits `26b9741c8` and `dd9c5e398` make scanner result emission a persistence/correlation boundary instead of a second gateway/Provider fan-out and move audit, judge-body, and inventory SQLite contention metrics through narrow generated v8 observers. Raw forensic scan content remains source truth for the central per-route redaction authority; transaction-batch snapshots and observer detach preserve generation coherence and fail closed. Scanner, audit, inventory, and focused gateway suites pass. | complete |
| `C-0037` | 2026-07-06 | sidecar provider hard cutover | `P3-WP04`, `P4-WP01..04`, `P5-WP03`, `P6-WP03` | D-019, D-024, P-010, P-040, P-044, P-046, P-061, P-069, P-070 | Commit `80215230a` deletes the main Sidecar/API/GuardrailProxy Provider seams, runtime gateway-event fan-out, provider-backed egress counter, duplicate startup SDK span, legacy destination-health renderer, and legacy canary fallback. Gateway schema violations now use the generated `defenseclaw.schema.violations` family; health and targeted canaries are generation-owned only. Registry generation/check, full audit/watcher/gateway/CLI/telemetry suites, and e2e compilation pass. The remaining live hard-cut scope is the standalone watchdog plus process SQLite-capacity ownership, followed by the repository allowlist proof and consumer live gates. | complete |
| `C-0038` | 2026-07-06 | capacity and watchdog runtime ownership | `P3-WP04`, `P4-WP01..04`, `P6-WP01`, `P8-WP02` | D-019, D-024, P-010, P-040, P-044, P-046, P-069, P-070 | Commit `62e91b781` removes Provider-owned capacity scheduling and the process-global SQLite pointer. The sidecar now submits one generation-pinned generated batch for the existing runtime, uptime, and SQLite dashboard families; shared snapshots are constructed lazily only after collection admits a related family. The standalone watchdog no longer builds a second OTel provider: it retries an authenticated loopback notification until the recovered sidecar acknowledges the canonical watcher-restart metric. Full audit, telemetry, CLI, and gateway suites pass. | complete |
| `C-0039` | 2026-07-06 | generated review-surface reduction | `P5-WP08`, `P8-WP04` | P-046, P-068, P-069, P-070 | Preserve family IDs, buckets, attributes, config defaults, database/upgrade behavior, W3C/root-subagent hierarchy, Galileo/local-observability projections, package resource names, and public schema bytes. Generated Go names/file boundaries, compiler IR classes, durable output journals, full unused upstream snapshots, public-view intermediates, and tracked build-staging copies are implementation details and may be removed or compacted. Semantic parity tests—not synchronized counts or self-digests—are the release gate. | complete |
| `C-0040` | 2026-07-06 | Galileo rich-link validation | `P3-WP04`, `P5-WP03`, `P6-WP03` | D-019, P-010, P-030, P-040, P-046, P-056, P-061, P-062, P-069, P-070 | Commit `f9d4a97c9` closes Galileo's remaining link-context gap by parsing and canonicalizing every non-empty W3C link tracestate before network delivery. Valid rich links preserve exact state and dropped-attribute metadata; malformed or duplicate-key state fails before export. Focused Galileo compatibility/destination/integration and v8 configuration suites pass. The credential-gated live conformance test remains a final environment-dependent P3/P8 gate. | complete |
| `C-0041` | 2026-07-10 | staged v8 release boundary | `P7-WP01..06`, `P8-WP05` | P-046, P-069, P-070 | The hard cut cannot be safely driven by the frozen published `0.8.3` controller. Publish `0.8.4` as a protocol-1-reachable, protocol-2-capable controller bridge with no v8 conversion; key the mandatory v8 conversion to `0.8.5`, whose signed manifest requires protocol 2 and minimum/bridge source `0.8.4`. Normal latest-version resolution may automatically stage only sources in `release/upgrade-baselines.json`, must terminate the old execution path and launch a fresh bridge controller, and must retain verified bridge artifacts plus exact state for health-verified rollback. Explicit bridge skips, unsupported sources, package-manager/manual paths without staging, missing rollback capability, and unsealed publication all fail before mutation. Windows has no published bridge for this cut and therefore refuses before mutation. | complete |

A new product choice requires a new decision ID and traceability row. A behavior
change updates its contract and required test in the same change. Deferred release
work must be explicitly excluded by the normative package; before it appears in a
PR follow-up, create and link a GitHub issue as required by `AGENTS.md`.

## Commit and PR Protocol

- Commit after each verified work package or coherent integration milestone.
- Commit messages identify the behavioral scope and verification, not only files.
- Do not mix unrelated work packages in one commit when they can be reviewed
  independently.
- Before every checkpoint: review `git diff`, run proportional tests, update this
  ledger, then commit.
- The final PR targets `main` and follows the repository-required sections:
  stack/base note, Problem, Current Situation, Solution, architecture diagram when
  useful, What Changed table, Breaking Changes, Open Issues/Follow-ups, and exact
  Test Plan results.
- Future work in the PR body must link an existing issue; otherwise write `None`.
- Run CodeRabbit only on an intentional committed/uncommitted scope, address all
  actionable issues, rerun tests, and record the result.
- Do not declare completion until every required CI/CD check on the PR is green.

## Final Completion Audit

Before marking `P8-GATE` and the active Codex goal complete, prove all of the
following against the final commit and live PR state:

- Every release gate in `07-verification-and-acceptance.md` section 17 passes.
- Every work package and phase gate is `DONE`; no release requirement is hidden in
  `TODO`, `READY`, or prose.
- D-/S-/P- decisions are unique, complete, traceable to contracts and tests, and
  reflected in implementation.
- Every current producer is classified and routed through one canonical pipeline.
- Legacy runtime toggles, direct sink fan-out, and duplicate old/new outputs are
  removed or proven inactive as specified.
- The complete migration fixture matrix and fault-injection matrix pass.
- PR #403 lifecycle/traceability and PR #412 dashboard goldens pass without losing
  pre-upgrade history.
- Local observability starts cleanly; all dashboards and live query inventory work.
- Galileo conformance, delivery health, and exact canary work for supported shapes.
- Generated docs/schemas/examples are current and drift-free.
- Security review finds no secret/content leakage or unsafe exporter path.
- The full final command suite is recorded at the final commit.
- The PR is against `main`, follows `AGENTS.md`, has no unlinked follow-ups, and all
  required CI/CD checks are green.

Only after this audit may the root coordinator mark the active goal complete.
