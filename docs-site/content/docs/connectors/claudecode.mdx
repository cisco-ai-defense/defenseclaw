---
title: Claude Code
description: Claude Code connector wires the documented lifecycle hook set plus native OTel. Fourteen current events can return block decisions, and PreToolUse supports native HITL ask.
keywords:
  - DefenseClaw Claude Code connector
  - Claude Code hooks
  - Claude Code PreToolUse
  - Claude Code observability
---

The Claude Code connector wires DefenseClaw into [Anthropic's documented hook surfaces](https://code.claude.com/docs/en/hooks) without inserting a proxy in the data path. Claude Code talks directly to its native upstream; DefenseClaw inspects via hooks + native OTel.

<Callout type="info">
The Claude Code connector is **hook-only**. There is no LLM-proxy data path — Claude Code talks directly to `api.anthropic.com` and DefenseClaw observes via hooks + native OTel.

**`mode=action` is supported on Claude Code's declared blocking hook events.**
`PreToolUse` can return deny or native ask before the tool. Post-result events
cannot undo a completed side effect, and no proxy listener is involved.
</Callout>

## Setup

```bash
defenseclaw setup claude-code                # observe (default) — record only
defenseclaw setup claude-code --mode action  # block on policy hits via PreToolUse deny
```

`setup claude-code` is the dedicated Claude Code setup alias. It uses the same
setup backend as `setup guardrail`, adds or reconfigures `claudecode`, wires
hooks + native OTel, and can join an existing hook-connector roster when you
choose **Add**. No proxy listener binds in either mode.

On macOS, Anthropic supports macOS 13+ and publishes native `arm64` and
`x86_64` binaries through the recommended native installer (and Homebrew).
DefenseClaw resolves the launcher's symlink to the canonical Mach-O, then
checks path permissions, native architecture, quarantine state, Anthropic's
Developer ID signature, version, and SHA-256 before sealing the selected
identity into the hook contract lock. macOS remains **preview** until a green
latest-version packaged + authenticated official-client run is durably
recorded; the validation ledger therefore leaves `last_validated_version`
empty. See the [native setup](https://code.claude.com/docs/en/setup).

On native Windows, install and run Claude Code directly from PowerShell or CMD
using the same official setup.
DefenseClaw registers a native executable with Claude Code's `args` exec form;
Git for Windows is optional and is not part of the connector contract. WSL,
Git Bash, Cygwin, MSYS, containers, and virtual machines are not certification
paths for this connector.

Claude Code native Windows support is a preview. The hook and lifecycle
contract is implemented, but no immutable packaged plus official-client
Windows certification record is persisted.

#### Version contract

The shared `claudecode-hooks-v1` floor is `>=2.1.154`. macOS applies an
additional protected-admission ceiling of `<2.1.219` because 2.1.219 introduced
the release-only `DirectoryAdded` event without a published schema, ordering,
or blocking contract. Windows retains the shared open ceiling. A version match
selects a known hook contract; it does not certify either native platform.

Native OTLP uses managed loopback exporter settings with a Claude-scoped
Authorization bearer. Logs and metrics are enabled on every supported host.
On macOS, DefenseClaw also enables Anthropic's documented enhanced-telemetry
beta trace exporter. Windows retains its existing logs+metrics posture with
traces explicitly disabled. Detailed `claude_code.hook` spans need additional
upstream flags and organization allowlisting and are not claimed. Prompt and
assistant-response content logging are independent vendor opt-ins and
DefenseClaw explicitly defaults both off; downstream redaction settings do not
silently authorize source capture. The scoped
credential cannot authenticate management or another connector and must never
be printed or copied.

Redaction is configured through the v8 bucket/profile policy: edit
`observability.destinations[].routes[].selector.buckets` and
`observability.redaction_profiles`, then follow
[Redaction → Verify policy](/docs/reference/redaction#verify-policy).

The alias defaults to observe mode and accepts the common hook-connector
options, including `--mode`, `--workspace`, `--rule-pack`,
`--human-approval`, `--hilt-min-severity`, `--fail-mode`, `--block-message`,
`--replace`, `--with-local-stack`, and restart controls. Use the
[quick-alias reference](/docs/setup/guardrail/aliases) for that surface. Use
the [full guardrail setup](/docs/setup/guardrail) when you also need scanner,
detection-strategy, or judge-provider configuration. Defaults are documented
once on the [Defaults page](/docs/defaults).

## Files DefenseClaw will modify

<Tabs items={['Native Windows', 'macOS/Linux']}>
  <Tab value="Native Windows">

DefenseClaw manages marked hooks and OTLP environment entries in
`%CLAUDE_CONFIG_DIR%\settings.json`; the default directory is
`%USERPROFILE%\.claude`. Generated runtime and protected connector credentials
live below `%USERPROFILE%\.defenseclaw\hooks`. See the
[Windows path reference](/docs/get-started/windows/paths-troubleshooting#filesystem-reference).

  </Tab>
  <Tab value="macOS/Linux">

<Files>
  <Folder name="~/.claude" defaultOpen>
    <File name="settings.json (hooks block + managed native OTel environment entries)" />
  </Folder>
  <Folder name="~/.defenseclaw" defaultOpen>
    <Folder name="hooks">
      <File name="claude-code-hook.sh" />
      <File name="protected connector credential sidecars" />
    </Folder>
  </Folder>
</Files>

  </Tab>
</Tabs>

DefenseClaw stores a hash-checked backup of `settings.json` before edits.
Teardown restores it byte-for-byte when the identity still matches; if the file
drifted, only DefenseClaw-owned entries are removed.

Claude Code keeps user/local MCP registration state in `~/.claude.json`
(`%CLAUDE_CONFIG_DIR%\.claude.json` when that override is set) and project MCP
state in `<workspace>/.mcp.json`; these are inventory surfaces, not hook setup
files. See Anthropic's [MCP scope reference](https://code.claude.com/docs/en/mcp).
The current preview inventories those local, project, and user manual scopes.
Plugin-provided MCP and claude.ai connector MCP are not attributed from these
files. Marketplace plugin cache artifacts honor
`CLAUDE_CODE_PLUGIN_CACHE_DIR`; retained versions are not presented as active
without semantic client evidence. Managed `enabledPlugins` takes precedence
over user, project, and local preferences; a dynamic `policyHelper` is reported
as unverified instead of guessed. Skill inventory covers the launch directory,
every parent through the repository root, and existing nested
`.claude/skills` roots that Claude can lazily activate after file activity.
Nested skills remain discoverable-unverified until session activity proves
activation; true skills take precedence over legacy commands, and documented
skill-directory symlinks are canonicalized and de-duplicated. Recursive
user/project agents use required frontmatter identity and closest-project
precedence. Managed/plugin agents and session `--agents` remain unverified.
Auto-memory resolves file-based `autoMemoryDirectory` precedence or the shared
repository/worktree default under `~/.claude/projects/<project>/memory`; it is
marked unverified when session `--settings`, remote settings, or native policy
could change the active path.
Runtime `/add-dir` roots remain outside passive inventory until the
release-only `DirectoryAdded` event has a published hook schema.

The read-only inventory also classifies all official customization families:
skills (including legacy compatibility commands), plugins/marketplaces and
their components, agents/subagents, `CLAUDE.md` plus rules, and Claude-owned
auto memory. Session-dependent project, nested, additional-directory, CLI, and
server-managed sources are reported only when their workspace/source is
explicitly known; DefenseClaw does not guess them or emulate Claude's loader.
Model proxying, a native sandbox, firewalling, and egress interception are
explicitly N/A for this connector.

## Hook capabilities

<HookEventsList connector="claudecode" />

<Callout type="info">
Claude Code is one of the few connectors that supports native PreToolUse ask. HITL approvals surface inside the agent UI itself, so the operator never has to leave Claude Code to decide.
</Callout>

`Setup` is observation-only and is not registered. `WorktreeCreate` is not
registered because a handler replaces Claude Code's default worktree creation
and must return the new path. Anthropic's v2.1.219 release notes mention a
`DirectoryAdded` event, but the current hooks reference does not yet publish
its schema, ordering, or blocking behavior, so DefenseClaw does not register it.

## Telemetry channels at boot

<Flow
  direction="LR"
  compact
  caption="Two telemetry channels: hooks for per-tool-call decisions and native OTel for model/token metrics plus privacy-gated events."
>
  <Node id="cc" kind="agent">Claude Code</Node>
  <Node id="hooks" kind="connector">{`Current 28-event\nhook contract`}</Node>
  <Node id="otel" kind="connector">{`Native OTel exporter\n(env-driven)`}</Node>
  <Node id="gateway" kind="gateway">defenseclaw-gateway</Node>
  <Edge from="cc" to="hooks" />
  <Edge from="hooks" to="gateway" />
  <Edge from="cc" to="otel" />
  <Edge from="otel" to="gateway" />
</Flow>

## Disable

```bash
defenseclaw guardrail disable --connector claudecode --yes
```

For the exact native Windows event and fail-mode boundary, see
[Connectors and enforcement](/docs/get-started/windows/connectors-enforcement#claude-code).
