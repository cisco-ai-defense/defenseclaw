# Rollback

Stop the managed launcher, verify the candidate release manifest, then atomically select a prior mandatory-AXIS release. Never restore the legacy unrestricted managed Codex configuration. If health or attestation fails, leave the launcher disabled and retain only the trusted cisco sudo break-glass boundary.
